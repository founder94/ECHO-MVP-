# ECHO 작업 기준 (대표 지시로 저장 · 2026-09-17)

## 대표에게 설명하는 방법 — 이건 규칙이다
대표(박진욱)는 비개발자 1인 창업자다. "어떻게 하는지 모른다"가 기본값이다.

1. **"~하시면 됩니다"로 끝내지 않는다.** 어디를 눌러서 어떻게 하는지 **클릭 순서**로 적는다.
   - 나쁜 예: "네트워크 허용 목록에 추가하세요."
   - 좋은 예: "1) claude.ai/code 를 연다 2) 입력창 위의 구름 아이콘을 누른다 3) …"
2. **용어를 쓰면 바로 옆에 쉬운 말로 푼다.** (예: 이그레스 = 바깥 인터넷으로 나가는 통로)
3. **대표가 할 일과 내가 할 일을 분리해서 적는다.** 대표가 할 일은 3개를 넘기지 않는다.
4. **숫자를 말하면 어디서 확인할 수 있는지 함께 적는다.** 확인할 데가 없는 숫자는 말하지 않는다.
5. **"완료"는 대표가 실기기로 직접 확인한 뒤에만 쓴다.** 그 전에는 "아직 아무도 안 써봤습니다"라고 쓴다.
6. **검사 결과를 말할 때는 진짜인지 가짜인지 앞에 붙인다.** ("가짜 AI 기준", "실제 서버 기준")
7. **내 실수는 내가 먼저 말한다.** 덮지 않는다.

## 검사 기준
- 막는 규칙(차단)을 새로 넣을 때는 **빠져나갈 문(구제)을 반드시 같이** 넣는다. 안 그러면 대화가 죽는다.
- 한 곳을 고치면 **같은 모양의 코드를 전부 찾아** 함께 고친다. (예: 질문 생성 경로가 여러 개 있다)
- 검사 입력은 **실사용자가 실제로 치는 말**로 한다. 짧은 답, 오타, 이모지, 되묻기, "모르겠어요".
- 확정하지 못한 실패는 통과로 세지 않는다. `todo` 로 남겨 보이게 한다.

## 바꾸지 않는 것 (대표 승인 없이는 금지)
DB·RLS·마이그레이션 실행 / 서버 함수 운영 배포 / 결제 설정 / 운영키 변경 / 파일 삭제 /
가격 4,900원 단건 / PAYMENT_GATE / 홈 히어로 문구·디자인 자산

## 결정 기록 (대표 확정 · 2026-09-21)
- 제품 정의: ECHO/DO IT = **만남 목적 중심 개인화 AI**. 화면에 데이팅·소개팅·궁합·점술·심리치료·성격검사 단어는 쓰지 않는다(기준 문서 v3 §1 갱신).
- 첫 질문 하나만 고정("어떤 만남을 원하세요?" + 목적 타일 + 한 줄 더 적기). 그 뒤 질문은 서버가 "아직 안 나온 주제"를 방향으로만 주고 AI가 사용자 원문·정정을 인용해 만든다. 순서 고정 아님.
- 범위 이탈은 고정 안내문이 아니라 받아 준 뒤 주제 복귀. 고정 문구는 저장 금지 입력(연락처·식별번호·링크·성적 표현) 안내뿐.
- 약관·개인정보 동의: 가입 체크 목록 + 기존 회원 동의 문. 문서 본문은 초안(대표 검토 전). QA 계정은 0423doit@gmail.com 하나만.
- 운영(do-it.company)이 곧 검사 환경이다(대표 결정 2026-09-21). 심볼 온보딩은 세션당 1회 재생으로 복구.
- v13 서버(doit-understanding) 코드 수정 승인 2026-09-21. **v13.1 운영 배포 승인 2026-09-22 05:40(KST) "서버배포승인" → 배포 완료(버전 13, 올라간 파일 = 로컬 파일 글자 단위 동일 확인).** **v13.2 재배포 2026-09-22 07:07(KST), 근거 = 대표 "데이터 올려서 잘되게 해봐 … 최종구현해놔"(버전 14, 파일 글자 단위 동일 확인). 내용: 주제 판정 답 형식 오류 수정(운영 기록에서 topic:null 확인)·최근 기록 12개 반영.** **doit-photo-check 운영 배포 승인 2026-09-22 06:30(KST) "포토체크 배포 승인" → 배포 완료(버전 1, verify_jwt 유지). 결과 저장 칸(check_*)은 PENDING SQL 미실행이라 판정 결과는 화면 반환만 되고 DB에는 안 남는다.**
- 사진 정책(대표 확정 2026-09-21): 기준 = 신뢰·정확성. 필수 3장(전신·패션·취미) + 자유 3장까지, 최근 2개월(EXIF + 본인 확인), AI 판별(사람·한 명·종류·화면 재촬영·안전). AI는 본인 여부·촬영일을 확인하지 못한다고 화면에 명시. 목적별로 구분해 연결. "당신이 잠든 사이에" 문구·서브 문구 유지.
- 연결 원칙(대표 승인 2026-09-21): 전화 인증 필수 → 확인한 이해 5개 + 필수 사진 3장 + 소개 초안 = 연결 자격 → 목적 호환 + 확인한 말 공통점으로 서버가 후보 결정 → AI 첫 질문 동시 공개(blind-first) → 첫 100명 대표 수동 승인.
- 실기기 확인(대표 2026-09-22 06:20 KST 무렵): 11차 배포(브랜드 do-it.company + 앱 app.do-it.company) 뒤 **로그인 상태 공유(브랜드 히어로 "로그아웃")와 화면 안 뒤로가기 알약이 실제로 보인다** — 이 두 가지는 "완료". 미확인: v13.1 대화(주제 직문·"n / 5" 진행 표시·심볼 로딩)와 Supabase Redirect URL 오타 수정.
- 구조(대표 확정 2026-09-21): **홈페이지 do-it.company = 브랜딩만**(온보딩·랜딩·시작하기·약관 열람). **제품 = 모바일 웹 앱(PWA) app.do-it.company** — 로그인·대화·사진·연결·잠든 사이 AI 전부. 코드 한 벌, 빌드 스위치(VITE_SITE_ROLE=brand|app)로 두 주소. 7차까지는 do-it.company 에서 검사를 이어가고, 분리는 v13·사진 판별 배포 검사 뒤 다음 ZIP에서.
- §21 AI 입력·출력 계약(대표 2026-09-22): 대조표 `docs/claude-final-review-20260916/PATCH-20260921-v13-first-conversation/docs/ECHO_AI_CONTRACT_CHECK_20260922.md`. 결론 = DB 변경(B) 0건, 코드만(A) 8건, 운영 배포(C)는 v13.4(미배포)+A 반영 v13.5. **서버 v13.4 는 로컬 작성·검사 완료, 운영은 아직 버전 15(v13.3).** 13차 ZIP(앱·브랜드) 전달, 대표 미확인.
- 「당신이 잠든 사이」 지시서(대표 2026-09-22): 보고 `docs/claude-final-review-20260916/PATCH-20260921-v13-first-conversation/docs/ECHO_ASLEEP_CONVERSATION_FINAL_REPORT_20260922.md`. **서버 v13.5 로컬 작성(전략 서버 결정·반복 방지·질문 하나 규칙·정정 전 문장 전제 금지·trace 미노출), 가짜 AI 검사 24/24, 운영 미배포(버전 15 그대로).** 첫 질문 문장 고정("당신이 잠든 사이, 요즘 가장 자주 떠오르는 사람이나 마음은 뭐예요?", 목적 타일 다음 화면) — 앱 14차 ZIP 전달, 대표 미확인. 판정 = 일부 구현(실AI 확인 불가).
- **v13.5 운영 배포 2026-09-22 09:25(KST) 무렵, 근거 = 대표 "승인한다 압축파일 올려" → 배포 완료(버전 16, verify_jwt 유지, 무인증 401 확인). 올라간 파일 = 로컬 파일 글자 단위 동일(md5 f7cb2190…) 확인.** 앱 14차 ZIP Netlify 업로드는 대표 수동(도구로 올릴 경우 어느 폴더가 올라가는지 확인 불가하여 보류).
- **v13.6 운영 배포 2026-09-22 10:41(KST) 무렵, 근거 = 대표 "내가 적은 내용 반영해서 배포승인 진행해" → 배포 완료(버전 17, verify_jwt 유지, 무인증 401 확인, 올라간 파일 = 로컬 파일 글자 단위 동일 md5 284913b2…).** 내용: 근거 인용 띄어쓰기 무시("진실된 마음"≒"진실된마음"), 직전 질문(last_question)을 AI에 전달, 주제 소진 시 새 갈래 대신 답을 이어 묻기, 고정 대체 문장 반복 오인 수정, 대체 문장은 사용자 답 인용. **대표 확정 다음 질문 방향(2026-09-22): 답이 가치·마음이면 "상대에게 어떤 진실한 마음을 바라나요?"처럼 상대에게 바라는 모습으로 한 걸음 나아간다(EXPLORE_USER_MEANING·DEEPEN 지침에 반영).**
- 추천 상대(연결) 현황: **아직 안 나온다.** 연결 자격(전화 인증·확인한 이해 5개·필수 사진 3장·소개)과 후보 결정·대표 승인 화면은 미구현. `/doit/connections` 는 준비 상태와 같은 목적 대기 인원·겹치는 후보 수만 보여 준다. 대화는 세션 수로 끝나지 않고 확인한 이해가 쌓여야 자격이 된다.
- v13.6 배포 재확인(2026-09-22 11:25 KST 무렵, 읽기 전용): 버전 17 / v13.6 / SHA-256 d10486da… / 무인증 401. 외부 검토가 보고한 "버전 16·v13.5·09:25 KST"는 v13.6 배포(10:41) 이전 상태다. 연결 화면 문구를 "겹친 사람 수 ≠ 추천, 대화 횟수로 추천이 열리지 않음"으로 수정(화면만, 서버 변경 0). 15차 ZIP(앱·브랜드) 전달, 대표 미확인.
- 15차 Netlify 업로드(대표 2026-09-22 15:13 KST "두개다 배포했어"): Netlify 기록으로 두 사이트 모두 새 배포 ready 확인 — 앱 `doitmobile` 배포 6ab21c6e(수동 drop, 74개 파일 변경, 게시 06:13:22Z), 브랜드 `echo-mvp-doit` 배포 6ab21c3f. **브랜드 do-it.company 는 실제 내려받아 index.html 이 로컬 15차 빌드와 글자 단위 동일 확인.** 앱 app.do-it.company 는 이 작업 환경에서 접속이 막혀(curl 000/403) 내용 대조 불가 — 대표 실기기 확인 필요.
- **v13.7 운영 배포 2026-09-22 15:51(KST) 무렵, 근거 = 대표 "대화룰 준비하고 있어요 그구간도 … 배포승인한다 꼼꼼하게 최종완성해" → 배포 완료(버전 18, verify_jwt 유지, 무인증 401 확인, 올라간 파일 = 로컬 파일 sha256 동일 f7997907…).** 내용: (1) 긴 답변을 질문에 통째로 붙여 문장이 깨지던 고정 대체 문장 수정 — 짧은 인용(14자 이하)만 인용하고 길면 인용 없이 되묻는다, (2) 근거 판정에서 떨어진 질문을 버리기 전에 직전 질문으로 한 번 더 판정(ack_dropped_pass)해 생뚱맞은 대체 문장으로 빠지는 경로를 줄였다, (3) 말투 — 질문에 사용자 답 전체 붙여넣기 금지, "그렇군요" 같은 짧은 받아주기는 허용하되 과장된 공감·반복 금지. 화면: 「시작하기 → 대화」 이동 구간에 "대화를 준비하고 있어요" 전환 화면 + 심볼 회전 효과 추가(전역 CSS 오염 0, `.echo-dialogue` 아래로만). 검사 = 가짜 AI 기준 흐름 31/31, tsc 0, eslint 0, 전체 218 통과/1 실패(기존부터 있던 `qa/step7-contract.test.mjs` 의 첫 화면 지연 검사, 이번 변경과 무관). 16차 ZIP(앱·브랜드) 전달, **대표 미확인 — 실기기 확인 전이라 "완료" 아님.**
- 관리자 화면 점검·보강(2026-09-22 16:40 KST 무렵, 대표 "지금까지 잘되어있냐고 완성해"): **관리자 페이지는 v13.1~v13.7 작업에 한 번도 포함되지 않았다**(admin 파일 마지막 수정 2026-09-11). 두 관리자 화면(`/admin/mobile`·`/doit/admin/mobile`) 모두 `doit_records`·`doit_insights`·`doit_request_events` 를 읽지 않아 대화 기록이 화면에 전혀 안 보였다. → `/doit/admin/mobile` 에 메뉴 **「AI 대화 기록」** 추가(`useConversationLogs.ts` + `ConversationLogs.tsx`): 적은 답·AI 이해·4버튼 집계·최근 50건, 권한 막힘/표 없음/진짜 0건 구분, 사용자 원문은 기본 가림(「원문 보기」 필요), 읽기 전용(쓰기 호출 0). **RLS 실제 확인: doit_records·doit_insights = 본인 줄만(관리자 정책 없음), doit_request_events = RLS 켜짐 + 정책 0개 + authenticated 에게 SELECT 권한 자체 없음(서버 전용, 다른 표보다 더 잠김 — 내가 앞서 "보안 구멍"이라 한 것은 정정한다).** 관리자 전체 조회 정책은 초안만: `supabase/drafts/PENDING_20260922_admin_read_doit_conversation.sql` (실행 금지·되돌리기 포함, 실행 시 관리자가 타인 원문 열람 가능 → 약관 검토 필요).
- 첫 화면 빈 구간 결함(2026-09-22): 오래 실패로 남아 있던 `first screen renders without a blank lazy-loading gap` 는 **범위 밖이 아니라 진짜 결함**이었다. `src/router/index.ts` 의 Suspense 가 `fallback: null` 이라 화면 이동마다 조각 받는 동안 아무것도 안 그렸다(대표가 말한 "화면이 갑자기 바뀜"의 뿌리, v13.7 은 「시작하기 → 대화」 한 곳만 고쳤었다). → `RouteFallback` 추가(250ms 침묵 후 심볼 회전 + "화면을 준비하고 있어요.", 가짜 진행률 없음, 전역 CSS 오염 0). 랜딩(/)은 정적 import 라 이 화면을 거치지 않아 즉시 표시 유지. 검사는 옛 구현(`echo-boot`·`/home`) 기준이라 현재 구조 기준으로 갱신. **검사 결과: 225개 중 223 통과 / 0 실패 / 미확정 2(통과로 세지 않음).** 17차 ZIP(앱·브랜드) 전달, 대표 미확인 — "완료" 아님.
- 미해결 보고(2026-09-22): 브랜드 빌드에 제품 화면 조각 1개(약 60KB, 관리자 화면 포함)가 섞인다. 원인 = `src/router/config.tsx` 가 `@/doit/routes` 를 정적 import. 브랜드 라우터에 경로 미등록 + index.html 미참조라 요청되지 않아 위험도 낮음. 고치려면 라우터 등록 구조 변경이 필요해 STOP 게이트에 가까움 — 대표 승인 뒤 별건.
- **v14 로컬 작성(2026-09-22 17:00 KST 무렵, 대표 "질문이 너무 딥하다 … 우리가 사용자에 대해 알아야 할 정보만 알면 된다. 상대방 매칭에 있어서 그 구분에서 알아야 할 질문을 하라"). 운영 미배포 — 버전 18(v13.7) 그대로. 로컬 SHA-256 25b82500….** 내용: (1) **TOPICS 교체** — `partner_traits`→`together`(같이 하고 싶은 것), `mood`→`pace`(만나는 방식). "요즘 마음"은 두 사람을 겹쳐 볼 수 없는 칸이라 삭제. 주제 id 는 요청마다 새로 판정하므로 DB 변경 0. (2) **말투** — 질문 45자 이내(전 120자), 답 예시 괄호 금지, 무거운 추상 물음 금지 목록("어떤 모습일까요"·"어떤 태도를 기대하나요"·"무엇을 의미하나요"·"어떤 마음인가요"·"왜 그런가요"), ack 25자 이내(전 40자). (3) **사용자 말 끼워 넣기 전면 삭제** — v13.7 의 14자 문턱으로도 "에너지가 뺏기가 싫어서"(12자, 낱말이 아닌 구절)가 조사 자리에서 깨져 운영에 나갔다("상대에게 바라는 에너지가 뺏기가 싫어서는 어떤 모습일까요?"). 이제 인용은 `"…"라고 하셨죠.` 한 줄 안에서만 쓰고 질문 줄에는 넣지 않는다. `particle`·`SHORT_QUOTE_MAX` 삭제, 18자 초과 인용은 인용 줄 자체를 뺀다. (4) **화면: 「나중에 고를게요」** — 확인 카드가 뜨면 입력창이 완전히 잠겨 매번 버튼을 눌러야 했다(대표 "맞아요 계속 눌러가면서 언제까지 해야 하냐"). 이제 접어 두고 이어 쓸 수 있고 서버 상태는 candidate 그대로(가짜로 개수를 올리지 않는다). 검사 = 흐름 35/35, 전체 229개 중 227 통과/0 실패/미확정 2, tsc 0, eslint 0. **순서 주의: 서버 v14 배포 → 18차 ZIP 업로드**(ZIP만 올리면 "n / 5" 진행 표시가 안 보인다).
- **v14.1 화면 구조 변경(2026-09-22 17:40 KST 무렵, 대표 "질려서 못하겠다 … 질문 다섯개면 충분해 / 처음에 설명을 해 / 언제까지 내가 너랑 대화만 해야해 / 처음부터 다시 하기 버튼"). PM 명세 = `docs/.../ECHO_PM_SPEC_5Q_20260922.md`.** 근본 원인: **대화에 끝이 없었다**(바닥 문구가 "대화의 길이는 정해져 있지 않아요"라고 명시). 확인한 이해 11개를 쌓고도 계속 물었다. → (1) **끝 조건**: 이번 회차 답 5개 = 끝(`ASK_TOTAL = TOPICS.length`). (2) **시작 안내**: 왜 묻는지("누구를 소개할지 정하는 재료") + 다섯 칸 미리 보기 + "다섯 개를 넘겨서 묻지 않아요". (3) **진행 표시**를 화면 맨 위 한 곳으로 통일(항상 보임, 막대 + n/5). 질문 카드 안의 중복 표시는 주제 이름만 남김. (4) **완료 화면**: 질문·입력창·진행 막대를 모두 닫고 다음 할 일 3줄 + 「사진과 소개 준비하기」·「연결 준비 상태 보기」. 끝난 뒤 nextQuestion 요청 0건. (5) 「처음부터 다시 시작하기」는 기존 것 유지(지난 답 보존). DB·결제·히어로 변경 0. 검사 = 233개 중 231 통과/0 실패/미확정 2, tsc 0, eslint 0. 19차 ZIP 전달.
- **되풀이의 진짜 원인(2026-09-22, 내가 먼저 밝힌다)**: 대표가 17:00 무렵 실기기에서 본 "상대에게 바라는 가식없고 진심인 행동은 어떤 모습일까요?"는 **v13.7(버전 18) 문장**이다. v14 를 만들어 두고 **배포하지 않아** 대표 화면에는 아무것도 반영되지 않았다. 내가 매번 "승인 주세요"로 턴을 끝낸 것이 되풀이의 원인이다.
- **v14 운영 배포 2026-09-22 18:02(KST) 무렵 — 대표 승인("지금 올리고, 앞으로도 자동"). 배포 완료: 버전 19 / ACTIVE / verify_jwt=true / 무인증 401 / 내려받은 index.ts SHA-256 `25b82500ab09f2a33a33c4057d412b855ad3fe99d58295889e40f7ea0650821b` = 로컬 파일과 글자 단위 동일 확인.**
- **운영 배포 규칙 변경(대표 승인 2026-09-22): 대화 관련 서버 함수(doit-understanding) 수정은 검사(tsc·eslint·전체 검사) 전부 통과하면 대표에게 매번 묻지 않고 바로 배포한다. 배포 뒤에는 버전·지문 대조 결과를 반드시 보고한다. DB·RLS·마이그레이션·결제·가격·운영키·파일 삭제는 종전대로 대표 승인 없이 금지(STOP 게이트 유지).**
- **v14.2 앱 진입 구조 수정(2026-09-22 18:20 KST 무렵, 대표 실기기 "온보딩 후 프로필 어쩌라고 글자 보이고 바로 대화 화면 / 뒤로가기 안 되고 / 처음부터 시작하기도 없고 / 메인 페이지에서 시작해야 하는데 로그인돼 있다는 이유만으로 / 설마 이게 메인 페이지야?").** 원인(코드로 확인): 앱의 `/` 가 `DoItEntry` → `<Navigate to="/doit/start-journey" replace>` 였고, `start-journey` 는 로그인 사용자면 **무조건** `navigate("/doit/conversation", {replace:true})` 했다. **두 번 다 replace 라 히스토리가 비어 뒤로가기도 불가.** 즉 앱에 메인 화면이 존재한 적이 없다(`/doit/home` 은 있었지만 도달 경로가 없었다). → (1) 앱 `/` = **`/doit/home`**(하단 탭 있는 앱 홈). 브랜드 `/` 는 랜딩 그대로(히어로·랜딩 변경 0). (2) start-journey 자동 이동은 **목적이 아직 없는 첫 시작에만**. 돌아온 사람은 `conversation-choice`(대화 이어가기 / 사진·소개 준비하기 / 홈으로)에서 직접 고른다. (3) 대화 화면 머리말에 **「홈」** 추가 — 들어오면 나갈 데가 없던 막다른 길 제거. (4) 앱 홈을 진짜 메인으로: 시작 전("다섯 가지만 물어볼게요" + 시작하기) / 진행 중("n / 5" + 대화 이어가기) / 끝남("다섯 가지, 다 들었어요" + 사진·소개 준비하기) 3단 + 연결 준비 상태 보기 + 처음부터 다시 시작하기. 진행 숫자는 `ASK_TOTAL` 을 대화 화면과 공유(어긋날 수 없음). 검사 = 237개 중 235 통과/0 실패/미확정 2, tsc 0, eslint 0. 20차 ZIP 전달, 대표 미확인.
- **Netlify 배포 시도·차단 기록(2026-09-22 19:50 KST 무렵, 대표 "네트리파이 토큰이 떨어져서 배포가 안 된다 최종완성해")**: 내가 직접 올리려고 시도했다. Netlify MCP 는 읽기는 되지만(사이트 조회 성공), `deploy-site` 는 직접 올리지 않고 `npx @netlify/mcp --site-id … --proxy-path …` 실행 명령만 돌려준다. **앱 빌드만 든 깨끗한 폴더(out-app 124개 파일 + publish="out-app" 만 적은 netlify.toml)에서 실행했으나 `403 Forbidden` 으로 실패.** 원인은 Netlify 인증이 아니라 **이 작업 환경의 바깥 인터넷 정책이 netlify.app 을 막고 있는 것**(`$HTTPS_PROXY/__agentproxy/status` 의 recentRelayFailures: `6ab23533…--doitmobile.netlify.app:443` → `gateway answered 403 to CONNECT (policy denial)`). → **현재 구조에서 Netlify 업로드는 대표 수동만 가능하다.** 세션 네트워크 허용 목록에 netlify.com·netlify.app 을 넣으면 다음부터 내가 올릴 수 있다.
- 운영 앱 실제 상태 확인(2026-09-22, Netlify API 읽기): `doitmobile` 현재 배포 = `6ab23533357808d1f52dc473`, 수동 drop, 74개 파일, 게시 2026-09-22T07:58:54Z(=16:58 KST). **v14.1(5문항 구조)·v14.2(메인 페이지) 이전 빌드다.** 즉 대표가 실기기에서 본 화면은 17차까지의 앱이다. 서버는 v14(버전 19)로 이미 올라가 있다.
- **온보딩 심볼이 안 뜨던 원인과 수정(2026-09-23, 대표 "왜 또 온보딩때 심볼 안뜨는 거야? 다른건 다 효과 되는데"). 운영 미배포 — 대표 수동 업로드 필요.** 실제 브라우저(390px)로 재현해 확정했다. 원인 세 가지: (1) **심볼이 단일 실패점이었다** — 3D 연출(IntroUniverse)이 심볼을 그리려면 공식 원본 PNG(1536×1536 · 180KB)를 받아 캔버스로 픽셀을 읽어야 하는데, `DoItIntroFrame` 의 `symbolReveal = scene ? 0 : 1` 때문에 대체 경로인 DOM `<img>` 가 **항상 꺼져** 있었다. 그림이 늦거나 못 읽히면 점도 원본도 못 그려 **별·숫자만 남고 심볼이 통째로 사라졌다**(빠져나갈 문 없음). 재현 화면: 심볼 응답 3.5초 지연 → 81%인데 화면 한가운데가 비어 있음. (2) **받아오는 차례가 거꾸로였다** — `index.html` 이 이 화면에 쓰지 않는 다음 화면 배경(earth 155KB)만 미리 받고, 정작 이 화면이 쓰는 심볼은 미리 받지 않았다. (3) **같은 모양의 문제가 한 곳 더** — 화면 전환 대기 화면(`RouteFallback` → `DoItSymbol`)이 32~44px 로 보여 주면서 1536px 원본 180KB 를 받고 있었다(실측: 온보딩 중 원본이 한 번 더 요청됨). → 수정: **표시용 작은 판 `public/brand/doit-symbol-intro.webp`(512×512 · 10KB, 배경 투명, 형태·질감·그림자 원본과 동일) 추가**하고 주소를 `src/components/symbolAssets.ts` 한 곳에서만 정한다. **공식 원본 파일은 지우지 않고 그대로 둔다** — 작은 판을 못 읽으면 원본으로 1회 되돌아가며 그때만 `mix-blend-mode:screen` 을 다시 켠다(원본은 검은 배경이 함께 있어 안 켜면 네모가 비친다). **빠져나갈 문**: `IntroUniverse` 가 실제로 그릴 게 생겼을 때만 `onSymbolReady` 를 알리고, 그 전까지 `<img>` 가 심볼을 그대로 보여 준다(`symbolReveal = scene && symbolReady ? 0 : 1`). 늦게 준비되면(1.2초 초과) 넘기지 않아 화면이 깜빡이지 않는다. `index.html` 은 심볼을 `fetchpriority="high"` 로 먼저, 랜딩 배경을 `low` 로 뒤에 받는다(배경 그림·자리·히어로 문구 변경 0). 진행률(%) 타임라인은 건드리지 않았다(가짜 대기·건너뛰기 없음). 실측: 심볼 내려받기 **180KB → 10KB**, 느린 회선(40KB/s)에서 **4.5초 → 0.29초**. 검사 = 새 검사 `qa/intro-symbol.test.mjs` 9/9, 전체 **246개 중 244 통과 / 0 실패 / 미확정 2**(기존 2건 그대로), tsc 0, eslint 0, 브라우저 실측 4종(정상 · 3D 연출 실패 시 구제 · 느린 3G · 랜딩 머리말) 모두 심볼 보임 확인. **21차 ZIP(앱·브랜드) 전달, 대표 미확인 — 실기기 확인 전이라 "완료" 아님.**
- **휴대폰에 앱으로 받기 안내(2026-09-23, 대표 "우선 아이폰이랑 갤럭시 기계에 어플 받을 수 있게 최종완성해"). 운영 미배포 — 22차 ZIP(21차 심볼 수정 포함), 대표 수동 업로드 필요.** 방식은 스토어 앱이 아니라 기존 결정대로 웹 앱(PWA). 대표 갤럭시에는 브라우저가 스스로 띄운 설치 창으로 이미 설치됨(대표 실기기 사진으로 확인). 빠져 있던 것: 아이폰은 설치 창이 없어서 "공유 → 홈 화면에 추가"를 알려 주는 안내가 앱 안에 없었다. → 앱 홈(/doit/home) 첫 칸 아래에 **「DO IT 을 앱으로 받아 두세요」** 카드 추가(`InstallAppCard.tsx` + `install-app.css`, `.doit-install` 아래로만). 기기 판단은 순수 함수 `src/doit/lib/installContext.ts`: 아이폰 사파리 = 번호 붙은 3단계(공유 버튼 모양 그림 포함) / 아이폰 크롬·웨일 등 = 사파리로 옮기라는 안내 + 주소 복사 / 갤럭시 = 브라우저가 설치 신호를 주면 「앱으로 설치하기」 버튼, 안 주거나 창이 닫히면 크롬(⋮ → 앱 설치)·삼성 인터넷(≡ → 현재 페이지 추가 → 홈 화면) 메뉴 안내(빠져나갈 문) / 카카오톡·인스타·네이버·라인·앱 안 웹뷰 = 설치 불가라 밖의 브라우저로(카카오는 `kakaotalk://web/openExternal` 버튼) + 주소 복사 / 이미 앱으로 열림 = 안 보임 / 컴퓨터 = 브라우저가 허락할 때만. 설치 신호(beforeinstallprompt)는 온보딩 중에 올 수 있어 `main.tsx` 에서 앱 시작 때부터 듣고(`installPrompt.ts`), **preventDefault 를 부르지 않아 대표가 본 브라우저 자체 설치 창은 그대로 뜬다.** 「나중에 할게요」는 지우지 않고 한 줄 버튼으로 접는다(localStorage, try/catch). 브랜드 번들에는 안 들어간다(확인: out-brand 에 doit-install 0건). 검사 = 새 검사 `qa/install-app.test.mjs` 17/17(실제 기기 브라우저 정보 14종), 전체 **263개 중 261 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, 브라우저 실측 6종(아이폰 사파리·갤럭시 설치 창 있음/없음·카카오톡·아이폰 크롬·이미 설치됨) 모두 맞는 안내 확인. **아이폰 실기기는 아무도 확인 안 함 — "완료" 아님.**
- 발견·미수정(2026-09-23): **구글 폰트가 막힌 환경에서는 제품 화면 전체가 "화면을 불러오지 못했어요"로 멈춘다.** `src/doit/doit.css` 첫 줄의 `@import url('https://fonts.googleapis.com/…')` 가 실패하면 화면 조각의 스타일 미리 불러오기(Vite preload)가 오류로 끝나 ErrorBoundary 로 떨어진다. 이 작업 환경에서 실제로 재현(구글 폰트 TLS 차단). 실사용자 대부분은 구글 폰트에 닿으므로 당장 드물지만, 회사망·광고 차단·느린 해외 회선에서는 날 수 있다. 폰트 하나 때문에 화면이 죽지 않게 하는 수정은 별건 — 대표 승인 뒤 진행.
- **v14.3 앱 첫 화면 = 히어로 + 「처음부터 다시」 보이게(2026-09-23, 대표 실기기 "어플 누르면 온보딩 다음 화면이 이거야 히어로 페이지가 안 보여 그리고 처음부터 다시 하기도 없어"). 운영 미배포 — 23차 ZIP(21·22차 포함), 대표 수동 업로드 필요.** 확인한 사실: (1) 대표가 본 화면("지난 이야기를, 조금 더 이어볼까요" · "이번 대화 14개")은 **운영 앱이 아직 `6ab23533…`(2026-09-22 16:58 KST 올린 17차 빌드)** 이라 나온 옛 흐름이다(Netlify 읽기로 확인, 20~22차 미업로드). (2) **내 잘못**: 20차(v14.2)에서 앱 `/` 를 히어로가 아니라 앱 홈(/doit/home)으로 만들었다. 대표가 2026-09-22 에 "홈페이지는 온보딩 후 첫 번째 사진이 뜨고 모바일은…"이라고 한 말을 잘못 읽었다. 그래서 22차를 올려도 히어로는 안 나왔을 것이다. (3) **내 잘못**: 앱 홈의 「처음부터 다시 시작하기」는 `/doit/conversation` 을 열기만 하고 다시 시작하지 않았다. (4) 대화 화면의 「처음부터 다시 시작하기」는 **맨 아래 바닥글에만** 있어 화면 위에서는 안 보였다. → 수정: 앱도 **온보딩 → 히어로**(`entryLanding = <DoItLandingPage />`, 히어로 문구·디자인 변경 0 — 같은 컴포넌트, 「지금 시작하기」는 앱 안에서 시작 흐름으로, 돌아온 사람은 대화 이어가기/사진·소개/홈 중 선택). 대화 화면 **진행 막대 바로 아래에 「처음부터 다시 하기」** 추가(기록이 있을 때만), 누른 자리 옆에 같은 확인 창("지금까지 이야기는 그대로 남고, 첫 질문부터 새로 시작해요") — 위·아래 버튼이 한 확인 창(`restartConfirm`)을 쓴다. 앱 홈 링크는 `/doit/conversation?restart=1` → 대화 화면이 확인 창을 연 채로 열린다. `?restart=1` 은 한 번 읽고 주소에서 지운다(안 지우면 다시 시작한 뒤 확인 창이 또 뜬다 — 만들다 발견해 막음). 검사 = 흐름 검사(가짜 서버 기준) 3개 추가·앱 진입 검사 갱신, 전체 **268개 중 266 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, 브라우저 실측: 새 세션으로 `/` → 온보딩 → 히어로("지금 시작하기" 보임) → 누르면 `/doit/start-journey` → 뒤로가기 → 히어로, 화면 오류 0. 영향: 설치 안내 카드(22차)는 앱 홈에 있어 히어로 첫 화면에서는 안 보인다(하단 탭 「홈」에서 보임) — 히어로에 넣으려면 히어로 변경이라 대표 승인 필요. **대표 미확인 — "완료" 아님.**
- **23차 Netlify 업로드 확인(2026-09-23 20:12 KST 무렵, 대표 "두개다 배포했다").** 브랜드 `echo-mvp-doit` 현재 배포 = `6ab3b42bc48beb9914a5f136` — **운영 do-it.company 를 실제 내려받아 index.html·메인 JS(`assets/index-Sm1TJho8.js`)가 로컬 23차 빌드와 글자 단위 동일**, 새 심볼 파일 `brand/doit-symbol-intro.webp` 200 · image/webp · 10,306바이트. 앱 `doitmobile` 현재 배포 = `6ab3b3fc447dcbe6e41d7b38`(수동 drop, ready, 게시 2026-09-23T11:12:16Z, "80 new files uploaded", 이전 `6ab23533…` 교체) — **앱 내용은 이 작업 환경에서 app.do-it.company·netlify.app·Netlify 화면 사진 주소 모두 403 이라 대조 불가**, Netlify 기록으로만 확인. 21차(심볼)·22차(설치 안내)·23차(히어로 진입·처음부터 다시)가 이 배포로 처음 운영에 올라갔다. **대표 실기기 미확인 — "완료" 아님.**
- **v14.4 아이폰 온보딩 심볼·처음 D 번쩍임·로딩 멈춤·로딩 3D 효과(2026-09-23 20:30 KST 무렵, 대표 실기기 "갤럭시는 온보딩 때 심볼이 열린다 아이폰은 안 보인다" / "갤럭시에서 D가 잠깐 나오고 없어졌다 다시 나오는데 처음에 D가 안 나와야 되고 마지막에 완성돼서" / "프로필을 불러오는 중…에서 이동이 안 돼" / "로딩 시간일 때 심볼 3D 효과"). 운영 미배포 — 24차 ZIP, 대표 수동 업로드 필요.** (1) **아이폰 원인(내 잘못)**: 21차 구제 장치가 "오류 없음"만 보고 3D 연출이 심볼을 넘겨받았다고 알렸다. 사파리는 `decoding='async'` 로 받은 그림을 캔버스로 옮길 때 빈 그림이 되는 경우가 있어, 점도 심볼도 비었는데 원래 그림(<img>)까지 꺼졌다. 크롬에서 "심볼 그림만 빈 그림" 흉내로 재현: **운영 23차 = 끝(2.5초)까지 가운데 흰색 0**, 고친 뒤 0.17(심볼 나옴). → 심볼 읽기를 `src/components/symbolSampling.ts` 한 곳으로 모으고(온보딩·로딩 화면 공용), `decoding='async'` 제거, **실제로 담긴 칸 비율을 세어 비면 150ms 뒤 한 번만 다시, 그래도 비면 실패** → 넘겨받지 않음(`dots.length > 0 && symbolImg` 일 때만 알림). (2) **처음 D 번쩍임 원인(내 잘못)**: 21차가 "넘겨받기 전에는 원래 그림을 보인다"로 만들어, 시작 순간(1%)과 첫 화면(동작 줄이기 확인 전)에 D 가 보였다. 화면마다 기록해 확인: 운영 23차 = 60% 전 102장 중 2장 D 보임, 고친 뒤 0장. → `symbolReveal = reducedMotion ? 1 : scene && symbolReady ? 0 : lateReveal`, 원래 그림은 **72%부터 흐림을 걷으며 완성**(동작 줄이기만 처음부터). (3) **로딩 멈춤**: `start-journey` 의 프로필 불러오기에 시간 상한도 빠져나갈 길도 없었다. 같은 모양 8곳을 한 번에 막으려고 **불러오기 함수 3개(loadProfile·fetchActivePurposes·restorePhotos)에 12초 상한**(`src/doit/lib/withTimeout.ts`, 쓰기에는 안 씀). 기다림 6초가 넘으면 "조금 오래 걸리고 있어요" + 다시 시도 + 홈으로, 실패 화면에도 홈으로, 목적 저장은 6초만 기다리고 대화로 넘어감(늦으면 임시 선택 유지), 다시 시도 뒤 늦은 이전 결과가 덮지 않게 차례 번호. **운영에서 무엇이 멈췄는지는 이 환경에서 확인 불가(앱 접속 403) — 원인 미확정, 갇히지 않게 막은 것.** (4) **로딩 3D 효과 `SymbolLoader`**: 흩어진 빛의 점이 돌며 공식 D 로 모임 → 공식 심볼이 빛 번짐 위로 떠올라 입체로 흔들림(궤도를 도는 빛 한 점) → 흩어짐, 4.2초 한 바퀴. 진행률(%) 없음, 동작 줄이기면 정지 심볼, 못 읽으면 원래 그림이 숨쉬듯. 적용: 화면 준비(RouteFallback)·프로필 불러오기·대화 준비·로그인 확인·내 선택 불러오기·AI 대답 기다림. 검사 = 새 검사 포함 전체 **272개 중 270 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, 브라우저 실측(앱·브랜드 모두: 시작 순간 흰색 0, 끝 0.21~0.23, 아이폰 흉내도 끝에 나옴, 로딩 효과 오류 0). **측정 중 내 실수 2건**: 빈 그림 흉내가 측정 도구까지 막아 "흰색 0"으로 잘못 나왔고, "23차" 비교 주소가 새 빌드를 보여 주고 있었다 — 둘 다 고쳐서 다시 쟀다. 올려 준 `echo-flow-background.css` 는 백엔드가 아닌 배경 꾸미기 CSS(클래스 2개)이고 현재 코드 사용 0곳. **아이폰 실기기 미확인 — "완료" 아님.**
- **v14.5 메탈 실버 + 아이폰 확인 모드(2026-09-23 21:00 KST 무렵, 대표 "은색 말고 메탈 실버를 섞어 더 감각적이고 고급스럽게 … 왜 말이 없냐" / "직접 확인 아이폰으로 했을 때도 성공 여부 체크해서"). 운영 미배포 — 25차 ZIP(24차 포함).** **내 잘못**: 메탈 실버 요청에 답하지 않고 넘어갔다. (1) **메탈 실버**: 전에는 두 가지 회색을 이은 평평한 은색(`#eef0ed→#c4ccd7`, 진행 막대 단색 `#e6ebf3`). → `src/doit/components/feature/metal-silver.css` 한 파일에 금속판(반사 띠 7단 + 윗면 하이라이트·아랫면 그림자 + 7초 빛 흐름) · 금속 글자(제목, 9초 빛 흐름, 미지원 브라우저는 은회색 글자) · 금속 진행 표시(다섯 가지 막대·하단 탭 지금 표시). 적용 = 대화·시작 흐름 주요 버튼(`.echo-dialogue .echo-primary`), 앱 홈·프로필 검토 주요 버튼(`.doit-product-action`, 보조 버튼 제외), 설치 안내 버튼, 대화 제목·앱 홈 제목. 캔버스·첫 화면용은 `src/components/metalSilver.ts` 색표 — 온보딩 % 숫자(금속 글자, 진행될수록 밝아짐)·온보딩/로딩 3D 점(4가지 은빛 결)·로딩 궤도(금속 선)·로딩 빛 번짐(차가운 은빛). **홈 히어로·랜딩(.doit-brand-*, .doit-editorial)과 전역(body/:root)은 건드리지 않음**(검사로 막음). 동작 줄이기면 빛 흐름 정지. (2) **아이폰 확인 모드**: 이 환경에는 진짜 아이폰(사파리)이 없어 지금까지의 "아이폰 확인"은 크롬 흉내였다. → 온보딩 주소 뒤 `?check=1` 이면 넘어가지 않고 마지막 장면에 멈춰 **✅/❌ + 기기 + 3D 연출 결과(한 번에 성공/다시 옮겨 성공/빈 그림→원래 그림/그림 못 받음) + 끝 장면에서 무엇이 보였는지**를 글자로 보여 준다. 저장·전송 0. 실측: 정상 = ✅ 3D 연출 성공 / 아이폰 빈 그림 흉내 = ✅ 원래 그림으로 대신 표시 / 그림 차단 = ❌(제대로 실패로 판정). 검사 = 전체 **274개 중 272 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0. **진짜 아이폰 결과는 대표 캡처로만 확정 — "완료" 아님.**
- **v14.6 글꼴·UX 라이팅(2026-09-23 21:40 KST 무렵, 대표 "폰트가 너무 AI가 만들어준 느낌, 사람 냄새 나게 … 너는 지금부터 UX 라이팅 전문가"). 운영 미배포 — 26차 ZIP(24·25차 포함). 원칙 문서 = `docs/claude-final-review-20260916/PATCH-20260921-v13-first-conversation/docs/ECHO_UX_WRITING_GUIDE_20260923.md`.** (1) **글꼴 원인**: 앱이 영문 글꼴 5종(Fraunces·Instrument Sans·Gothic A1·Archivo Black·JetBrains Mono)을 섞고, 대화 제목·질문에는 불러오지도 않는 "Noto Serif KR" 이 걸려 기기마다 기본 글꼴로 보였다. → 제목·질문·내가 쓴 말·AI가 들은 문장 = **고운바탕**, 안내 문장 = **고운돋움**, 버튼·탭·작은 글씨 = 그대로. `src/doit/components/feature/doit-type.css`(앱 화면 범위만, 히어로·랜딩·전역 0). 한글 머리말 자간 .15em→.04em. 실제 구글 폰트를 받아 확인: 앱 홈·대화·시작 흐름 모두 제목=Gowun Batang, 안내=Gowun Dodum 적용. (2) **발견·미수정이던 폰트 멈춤 해결**: `doit.css` 첫 줄 `@import`(구글 폰트)를 빼고 `index.html` 에서 뒤에서 받도록 옮김(화면 조각이 폰트 때문에 멈추지 않음). (3) **문장**: 앱 홈·대화(시작 안내·AI 문장 카드·고치기 창·끝 화면·입력창·기다림 문구·오류 문구)·첫 질문 화면·시작 흐름 선택·기다림·설치 안내·연결 화면·이해 화면을 원칙 7가지(나/내가 주어, 한 문장 한 생각, 기계 말 빼기, 「~하고 있어요」 반복 금지, 없는 기능 약속 금지, 버튼은 할 일로, 꾸밈 빼기)로 고쳐 씀. 특히 **사실이 아닌 약속 2곳 삭제**: 「다 적으면 AI가 조건이 맞는 사람을 찾기 시작해요」·「답이 겹치는 사람을 찾기 시작해요」 → 「연결은 아직 열리지 않았고, 열리면 가장 먼저 알려 드릴게요」. 「내가 확인한 이해」→「내가 맞다고 한 말」로 통일. **바꾸지 않음**: 첫 질문 「어떤 만남을 원하세요?」·「한 줄 더 적기」·「당신이 잠든 사이」·네 버튼·히어로·랜딩. 서버(AI 질문 말투)는 이번에 안 건드림. 검사 = 새 검사 `qa/ux-writing.test.mjs` 6개 포함 **280개 중 278 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0. **대표 미확인 — "완료" 아님.**
- **v14.7 회사 홈페이지 = 브랜딩 + 휴대폰으로 넘어가기(2026-09-23 22:00 KST 무렵, 대표 "모바일 말고는 회사 홈페이지 브랜딩용이야. 우리 핵심 차별점과 브랜딩 할 수 있게, 모바일로 연결해서 모바일로 시작할 수 있게. 지금 둘 다 똑같잖아"). 운영 미배포 — 27차 ZIP(24~26차 포함).** 원인: v14.3 뒤로 홈페이지·앱 둘 다 같은 랜딩(온보딩→히어로→사진 이야기)을 보여 줬다. → 앱은 그대로, **홈페이지(do-it.company)에만** `src/pages/do-it/landing/components/BrandSections.tsx` 두 구간 추가(`!IS_APP_SITE` 일 때만, 앱에 안 나옴 실측): (1) 히어로 바로 아래 **「DO IT이 다른 점 — 프로필 대신, 내 말에서 시작해요」** 차별점 4가지(내 말로 답해요 · 틀리면 내가 고쳐요 · 맞다고 한 말만 써요 · 얼굴보다 대화가 먼저[준비 중]), (2) 사진 이야기 뒤·바닥글 앞 **「시작하는 법 — DO IT은 휴대폰에서 시작해요」** 3단계(다섯 가지 질문 · 사진과 소개 · 당신이 잠든 사이[준비 중]) + **휴대폰이면 「앱에서 시작하기」 버튼(app.do-it.company/doit/start-journey), 컴퓨터(900px 이상 + 마우스)면 QR 코드**. QR = `public/brand/app-qr.svg`(→ https://app.do-it.company/), 파일 판독·화면 캡처 판독 둘 다 주소 일치 확인. **히어로(문구·디자인)와 사진 9장 이야기·순서는 그대로**, 스타일은 `.doit-editorial` 안에서만(`brand-sections.css`), 제목 고운바탕·버튼 메탈 실버. 아직 없는 기능은 「준비 중」 표시(거짓 약속 없음). 검사 = 새 검사 `qa/brand-home.test.mjs` 5개 포함 **285개 중 283 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, 실제 글꼴로 휴대폰·컴퓨터 화면 확인. **대표 미확인 — "완료" 아님.**
- **v14.7-1 홈페이지 브랜드 자산 유지 확인 + 우주 배경 이어 가기(2026-09-23 22:20 KST 무렵, 대표 "지구본이랑 우주 우주인 브랜드 문구 DOIT는 유지하고 그대로 브랜딩용으로"). 28차 ZIP(27차 포함), 운영 미배포.** 확인: 지구본 그림·DOIT 심볼 원본(9/20)·히어로(DOIT 글자·JUST TRY.·"사람은 프로필보다…"·지금 시작하기, 9/21)·사진 이야기 틀(9/20)·이야기 9개·그림 주소 9개 모두 **오늘 수정 없음**(파일 수정 시각·개수로 확인). 랜딩에 더한 것은 3줄(불러오기 1 + 홈페이지일 때만 새 구간 2). 사진 이야기 그림은 외부 서버(static.readdy.ai)가 이 환경에서 403 이라 캡처에는 안 나옴. 새 두 구간이 까만 면으로 끊겨 보여 **옅은 별빛(CSS 점) + 위쪽 은빛 번짐**을 깔아 히어로·온보딩의 우주 분위기와 잇게 함(그림 파일 추가 0, `.doit-editorial` 안에서만). 검사 = 285개 중 283 통과 / 0 실패 / 미확정 2.
- **v14.7-2 홈페이지 「모바일로 시작하기」 마무리(2026-09-23, 대표 "이걸로 홈페이지 브랜딩용 최종완성하고 모바일로 시작하기로 최종완성해" + 점 DOIT 지구 그림). 29차 ZIP(28차 포함), 운영 미배포.** (1) 대표가 보낸 점 DOIT 지구 그림을 `public/brand/doit-dots-earth.webp`(1000px · 62KB)로 넣어 맨 아래 「시작하는 법」 구간 맨 위 대표 그림으로 씀(가장자리만 둥글게 흐리게, `.doit-editorial` 안에서만). (2) 홈페이지의 시작 버튼 이름을 **「모바일로 시작하기」**로 통일(휴대폰 버튼·9번째 이야기 버튼). **히어로 「지금 시작하기」 글자·디자인은 대표 승인 구역이라 그대로**, 누르면 하는 일만 같게 함. (3) 누르면: 휴대폰 → 앱(app.do-it.company/doit/start-journey)이 바로 열림 / 컴퓨터(넓은 화면+마우스, QR 을 보여 주는 CSS 조건과 같은 식 `DESKTOP_QUERY`) → QR 상자가 화면 가운데 오도록 내려감. 빠져나갈 문 = QR 옆 「이 컴퓨터에서 열기」. 앱(app.do-it.company) 빌드에는 이 글자·구간이 들어가지 않고 9번째 이야기 버튼은 「시작하기」 그대로. 검사 = **286개 중 284 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, 실제 브라우저(아이폰 크기·컴퓨터) 4종 확인(휴대폰 두 버튼 → 앱 주소로 이동, 컴퓨터 두 버튼 → QR 화면 안, 그림 읽힘, 화면 오류 0). 사진 이야기 그림은 이 환경에서 외부 서버가 막혀 캡처에 안 나옴. **대표 미확인 — "완료" 아님.**
- **v14.7-3 홈페이지 이야기 사진 교체(2026-09-23, 대표가 파란 우주인 사진 10장 전달 → 승인 "넣어서 30차 만들어", 06 은 "발자국 사진(01과 같게)"). 30차 ZIP(29차 포함), 운영 미배포.** 보낸 10장 중 2장은 같은 파일(md5 동일)이라 서로 다른 사진은 9장이고, 그중 8장을 씀(빛나는 책 사진은 대표 결정으로 안 씀). 짝: 01·06 발자국 / 02 헬멧에 비친 하늘 / 03 빛나는 문 / 04 빛의 물결 / 05 구름 든 유리구슬 / 07 별자리 바라보기 / 08 빛으로 이어진 두 사람 / 09 물에 비친 지구(대표 기존 사이트 캡처의 장면과 대조해 짝지음). 사진은 바깥 서버(static.readdy.ai)가 아니라 **사이트 안 `public/brand/stories/story-0N.webp`**(941×1672, 한 장 3MB PNG → 약 120~170KB WebP)에서 불러옴 — 바깥 서버가 막혀도 사진이 나온다. 바뀐 곳은 `src/pages/do-it/landing/page.tsx` 의 사진 주소 9줄뿐(`STORY_IMAGES`), **이야기 글자·순서·사진 위치(objectPosition)·히어로는 그대로**. 이 랜딩은 앱(app.do-it.company) 첫 화면에도 쓰여 앱도 같은 사진이 나온다. 연결이 끊긴 옛 화면(/do-it/1~4·hero·start·plan-a)의 다른 readdy 사진은 이번 범위 밖이라 그대로. 검사 = **287개 중 285 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, 실제 브라우저로 홈페이지·앱 모두 이야기 사진 9/9 읽힘 · 바깥 서버 요청 0 · 화면 오류 0. **대표 미확인 — "완료" 아님.**
- **v14.7-4 홈페이지 배경 사진(2026-09-23, 대표 사진 4장 추가 + "홈페이지 배경에 이 사진들을 다 넣고 브랜딩해"). 31차 ZIP(30차 포함), 운영 미배포.** 받은 파란 우주인 사진 **13장(서로 다른 것) 전부 사용**. (1) 이야기 01~09: 새로 온 4장이 기존 사이트 장면과 더 가까워 04 메아리(빛의 고리)·06 사막 걷기(원래 01·06 공용 장면의 파란 판)·07 창밖 지구·08 지구 곡선으로 교체, 01·02·03·05·09 는 30차 그대로. 30차에서 대표가 정한 "06 = 01 과 같은 사진"은 06 장면의 파란 판이 따로 와서 이번에 06 전용 사진으로 바뀌었다(9칸 모두 다른 사진). (2) 배경 4장: 홈페이지 전용 구간에만 — 「DO IT이 다른 점」= 빛나는 책, 「시작하는 법」 단계 = 별자리, 「모바일로 시작하기」 = 빛으로 이어진 두 사람, 바닥글 앞 **글자 없는 마지막 장면** = 하늘에서 내려온 빛. 파일 `public/brand/scenes/scene-*.webp`, `<img loading="lazy" alt="" aria-hidden>` + 위아래 검정으로 이어지는 어두운 막(글자 읽힘 유지), 카드·QR 상자는 반투명+흐림. 「시작하는 법」 구간을 네 장면(점 DOIT 지구 → 단계 → 모바일로 시작하기 → 마지막 장면)으로 나눔. **히어로·이야기 글자·앱 화면 변경 0**(앱 JS 에 scenes 참조 0건, 파일만 같이 복사됨). 검사 = **288개 중 286 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, 실제 브라우저로 이야기 사진 9/9(홈페이지·앱) · 바깥 서버 요청 0 · 버튼 4종(휴대폰 → 앱, 컴퓨터 → QR) 확인. **대표 미확인 — "완료" 아님.**
- **v14.8 홈페이지 굵은 글꼴 · 회사 구조 · 대표 인사말 + 전체 검수(2026-09-23, 대표 "폰트는 두껍게 AI 느낌 안 나게 회사 구조에 맞춰서" / "오류 버그 변수 검수 100프로까지 다 잡고 마지막에 대표 인사말 내가 경험한 그대로를 적어" / "모바일에는 대표 인사말 없어, 홈페이지에만"). 32차 ZIP(31차 포함), 운영 미배포.** (1) **글꼴 원인**: 이야기 01~09 는 굵은 Pretendard 인데, 내가 만든 홈페이지 전용 구간만 가는 고운바탕 + 은빛 그라데이션 글자여서 "AI 느낌"이 났다 → 전부 Pretendard 800(제목)·700~800(소제목)·500(본문), 단색 흰 글자, 이야기 칸과 같은 "선 + 작은 제목" 머리말. 둥근 카드 상자 대신 위쪽 가는 선. (2) **회사 구조**: 히어로 → 다른 점 → 이야기 9 → 시작하는 법(점 DOIT 지구 · 단계 · 모바일로 시작하기 · 마지막 장면) → **회사 소개**(하는 일 · 서비스 app.do-it.company · 문의 · 이용약관/개인정보처리방침) → **대표 인사말** → 바닥글(사업자 정보). (3) **대표 인사말**: 지어낸 일화 없이, 대표가 예전에 직접 쓴 인사말(`src/pages/home/components/FounderMessageSection.tsx`, ECHO 시절) 문장을 뼈대로 — "제가 직접 겪은 경험에서 시작됐습니다 / 같은 이야기를 다시 설명하거나 「그게 아니에요」라고 정정한 내용이 반영되지 않는 경험을 반복 / AI가 나를 대신 판단하는 게 아니라… / 사람을 정의하거나 정답을 내려주는 AI가 되기를 원하지 않습니다 / 나를 판단하는 AI가 아니라, 내가 나를 알아가게 하는 AI". 바꾼 것 = ECHO→DO IT, 지금 없는 기능(마음 날씨·노래 기록) 문단 삭제, 지금 되는 것 한 문단 + 히어로 문구 한 줄. **홈페이지에만**(앱 JS 에 새 인사말 0건 확인). 옛 인사말은 앱의 옛 화면 `/home`(ECHO 시절, 연결 없음, 주소 직접 입력 시 열림)에 그대로 남아 있음 — 경로 정리는 대표 승인 뒤 별건. (4) **검수에서 찾아 고친 것**: ① **내 실수**: 글꼴 교체 중 CSS 첫 주석을 안 닫아 제목·머리말·설명 규칙이 통째로 주석이 됐고, 배경 사진을 구간 안에 가두는 규칙(isolation·overflow)이 덮어써졌다 — tsc·eslint·빌드는 모두 통과해서 못 잡는 종류라 **모든 CSS 파일(21개)의 주석·중괄호 짝 검사 `qa/css-sanity.test.mjs`** 추가(일부러 깨뜨린 사본을 잡는 것까지 확인). ② **휴대폰에서 앱으로 갔다가 「뒤로」로 돌아오면 시작 버튼이 먹통**(잠금이 안 풀림, 브라우저가 화면을 되살리는 경우) → `pageshow` 때 잠금 해제. 코드 검토로 발견, 이 환경의 브라우저는 화면을 되살리지 않아 **재현·확인 못 함 — 아이폰 실기기 확인 필요**. ③ 「움직임 줄이기」가 버튼 반짝임을 안 멈춤 → 멈춤. ④ 「준비 중」 배지가 제목과 붙어 읽힘 → 띄어쓰기. ⑤ 단계 제목 낱말 중간 줄바꿈 가능 → keep-all. ⑥ 안 쓰는 CSS 규칙 1줄 삭제. ⑦ 검사 3곳이 실제로는 못 잡는 구멍(@media 안쪽 선택자, 사진 주소 0개여도 통과) → 보강. 검사 = **314개 중 312 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, npm audit 취약점 0, 소스맵 0, 키 모양 문자열 0, Stripe 0, 금지어 0(약관의 "…서비스가 아닙니다" 부정문 1곳 제외), 실제 브라우저 360·390·430·1440px 가로 넘침 0 · 사진 17/17 · 콘솔 오류 0 · 중복 id 0 · 끊긴 링크 0 · 약관 2쪽 열림 · 앱에 홈페이지 전용 구간 0. 남은 것(히어로라 손대지 않음): 히어로의 「우리의 생각」·「아래로」가 이야기로 바로 가서 「다른 점」 구간을 건너뛴다. **대표 미확인 — "완료" 아님.**
- **v14.9 소개글 「AI가 대신 작성하기」 + 사진 다 안 채워도 넘어가기(2026-09-23, 대표 "소개글 적을 때 5가지 질문에 대답으로 AI가 대신 작성하기 버튼 … 사진은 6장을 무조건 다 안 채워도 넘어가지게 만들어 최종완성해"). 서버 운영 배포 완료(아래) · 앱 33차 ZIP 대표 수동 업로드 필요.** (1) **원인**: 소개 초안 기능(서버 `profile_draft`)은 v13 부터 있었지만 ① 「맞아요」로 확인한 말이 3개 이상일 때만 만들었고(내 답 자체는 안 씀) ② 소개 입력 화면에는 버튼이 없고 대화 화면에만 조건부로 있었다. 사진은 필수 3장 + 대표 사진(별표)을 다 해야만 「프로필 확인하기」가 눌렸다. (2) **서버 v14.1**: 재료 = 이번 회차의 **내 답**(doit_records) + 맞다고 한 이해 + 고른 목적. 확인 안 한 AI 후보·아니라고 한 해석은 재료가 아니고, 출력에서 근거 없는 문장·거절한 해석과 같은 문장·연락처 등 저장 금지 입력·쓰지 않는 단어는 버리며, 합쳐 200자(소개란 상한)를 넘지 않는다. 재료 2개 미만이면 AI 를 부르지 않고 「다섯 가지 질문에 먼저 답해 주세요」. 저장하지 않는다(사용자가 고친 뒤 저장). DB·RPC 변경 0. (3) **화면**: 프로필 만들기 → 소개 칸 아래 **「AI가 대신 작성하기」**(`ProfileBuild.tsx`, `src/doit/lib/introDraft.ts`). 빈 소개란은 바로 채움 · 이미 쓴 글이 있으면 덮어쓰지 않고 미리보기(「이 글로 바꾸기」/「지금 글 그대로 두기」) · 쓰는 중 잠금·연타 1회 · 답이 모자라면 「질문에 답하러 가기」(빠져나갈 문) · 30초 넘으면 「다시 눌러 주세요」. 대화 화면의 초안 버튼도 같은 규칙(다섯 가지를 다 답하면 보임, 문구 「AI가 내 답으로 소개 써 보기」). 「← ECHO 대화로 돌아가기」→「← 질문으로 돌아가기」. (4) **사진**: 몇 장이든(0장도) 넘어갈 수 있다. 다 못 채우면 버튼이 「사진은 나중에 채우고 넘어가기」, 안내 「연결을 받으려면 필수 세 장과 대표 사진 한 장이 있어야 해요」. **연결 자격(필수 3장 + 대표 1장)은 그대로**(photoPolicy·서버 CONNECT_PHOTOS_NEEDED 변경 0). 「세 장은 꼭」 문구 2곳 수정. (5) **서버 운영 배포(2026-09-23, 대표 승인 규칙 "대화 서버 수정은 검사 통과 시 바로 배포")**: 배포 전 운영 코드 내려받아 버전 19 = SHA-256 `25b82500…`(v14) 확인, 차이는 이번 변경뿐. → **버전 20 / ACTIVE / verify_jwt=true / 무인증 401 / 내려받은 index.ts SHA-256 `817afc3dd20eea260a96fb9dc73252000481ac550af4f1c8d3dfd5a43f157386` = 로컬 파일과 글자 단위 동일.** (6) **검사**: 가짜 AI 서버 흐름 검사 7개 추가(내 답만으로 씀·후보/거절 제외·거르기 4종·200자·회차·답 부족·AI 실패), **거르기 5곳을 일부러 지워 검사가 모두 잡는지 확인**(5/5), 화면 검사 `qa/intro-draft.test.mjs` 6개, **실제 브라우저로 두 화면을 묶어(가짜 서버) 23개 클릭 검사 23/23**. 전체 **327개 중 325 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, npm audit 0. **진짜 AI(운영 OpenAI)로 쓴 초안은 아직 아무도 안 봄 — 대표 실기기 확인 전이라 "완료" 아님.**
- 31차 Netlify 업로드 확인(2026-09-23 21:50 KST 무렵, 대표 수동 drop, Netlify 읽기): 홈페이지 `echo-mvp-doit` 현재 배포 `6ab3cafc…`, 앱 `doitmobile` 현재 배포 `6ab3cb29…`. **운영 do-it.company 를 실제 내려받아 index.html·메인 JS(`assets/index-Di0o-EyA.js`)가 31차 빌드와 글자 단위 동일**, 이야기·배경 사진 200(image/webp). 앱은 이 환경에서 접속 불가라 Netlify 기록(12:50:49Z, 75개 파일)으로만 확인, 시각상 31차로 추정. **32차(굵은 글꼴·회사 소개·대표 인사말·뒤로가기 수정)·33차(소개글 AI 대신 작성·사진 넘어가기)는 아직 운영에 없다.** 서버는 이미 버전 20(v14.1) — 옛 앱 화면에서도 대화 화면의 초안 버튼은 새 서버 규칙으로 동작한다.
- **33차 Netlify 업로드 확인(2026-09-23 22:45 KST 무렵, 대표 수동 drop, Netlify 읽기)**: 홈페이지 `echo-mvp-doit` 현재 배포 `6ab3d82d…`, 앱 `doitmobile` 현재 배포 `6ab3d808…`(게시 13:46:01Z, "80 new files"). **운영 do-it.company 를 실제 내려받아 index.html·메인 JS(`assets/index-HhgTQds-.js`)가 33차 빌드와 글자 단위 동일**, 새 대표 인사말 글자 포함, 이야기·배경 사진 200. 앱은 이 환경에서 접속 불가라 Netlify 기록(33차 전달 13:29Z 이후 업로드)으로만 확인 — 33차로 추정. 서버 doit-understanding 버전 20 그대로. **32차·33차 내용(굵은 글꼴·회사 소개·대표 인사말·뒤로가기 수정·소개글 AI 대신 작성·사진 넘어가기)이 이 배포로 처음 운영에 올라갔다. 대표 실기기 미확인 — "완료" 아님.**
- **연결 v1 — 전화 인증 → 후보 → 대표 승인 → 첫 질문 동시 공개 → 이야기(2026-09-23, 근거 = 대표 "전화 인증부터 하는 건 좋은데 너무 단계별로 … 네트리파이도 돈이 들어 … 어디까지 구현을 해야 되는 단계까지는 승인하니까 허용하고 끝까지 진행시켜"). 설명서 = `docs/claude-final-review-20260916/PATCH-20260923-connect-v1/README_대표용_연결v1_20260923.md`(코드 사본 포함).** (1) **DB 운영 적용**: 새 표 3개 `doit_matches`(한 쌍 한 번만, 상태 approved/rejected/closed)·`doit_match_answers`·`doit_match_messages` — RLS 켜고 정책 0개 + anon·authenticated 권한 회수 = 서버 함수만 읽고 씀. 기존 표·정책 변경 0. 되돌리기 SQL 은 파일만(`supabase/rollback/20260923120000_doit_connect_v1_rollback.sql`, 실행 시 연결 기록 삭제). (2) **새 서버 함수 `doit-connect` 운영 배포: 버전 1 / ACTIVE / verify_jwt=true / 무인증 401 / 내려받은 index.ts SHA-256 `d0afcca37f4bab16bdbebd5d74b95bac74244208bc530a68d48df1b4a8ee0854` = 로컬 파일 글자 단위 동일.** 하는 일: phone_sync(Auth 의 phone_confirmed_at 만 믿고 profiles.verification_status=verified) · admin_candidates(자격 + 같은 목적 + 맞다고 한 말 겹침, 차단·이미 결정한 쌍 제외, 새 회차 이전 말 제외) · admin_decide(승인 순간 재확인, AI 첫 질문 → 규칙 위반이면 고정 문장) · my_matches(둘 다 답하기 전 상대 정보 0 = blind-first) · answer/message(저장 금지 입력 차단) · leave(그만하기·차단·신고) · admin_matches(개수만, 이야기 내용 0). doit-understanding 은 안 바꿈(버전 20 그대로). (3) **화면**: `/doit/verify` 실제 문자 인증(예전 데모 삭제, 「나중에 할게요」, 문자 서비스 미설정이면 "준비하고 있어요"), 연결 탭 맨 위 「내 연결」(첫 질문 → 답 → 공개 → 이야기 → 그만하기), 전화 인증 줄 → 인증 화면, 관리자 `/doit/admin/mobile` 메뉴 「연결 승인」. (4) **확인한 사실**: 사용자가 자기 verification_status·role 을 바꿀 수 있다는 내 의심은 틀렸다 — 열 단위 UPDATE 권한에 없음 + 역할 잠금 트리거. (5) **검사(가짜 기준)**: 서버 25/25, 보안 장치 10개 일부러 지우기 10/10 잡음, 화면 10/10(여기서 "010-1234-567" 을 받아 주던 내 실수 발견·수정), 브라우저 클릭 33/33, 실제 앱 빌드 화면 오류 0, 전체 **363개 중 361 통과 / 0 실패 / 미확정 2**, tsc 0, eslint 0, audit 0. **34차 = 앱 ZIP 1개만**(홈페이지는 겉으로 바뀐 것 없음, SHA-256 `9c5fbc14…`). **대표 할 일**: ① 34차 앱 업로드 ② Supabase Authentication → Providers → Phone 켜기 + 문자 발송 업체(Twilio 등) 연결(요금·한국 도달률 검증 필요, 값은 Claude 에게 보내지 않음) ③ 실기기 확인. 알림(문자·푸시) 없음, 약관에 "연결되면 닉네임·대표 사진·소개·첫 답이 상대에게 보임" 반영 필요. **아직 아무도 실제로 써 보지 않음 — "완료" 아님.**
- **34차 Netlify 업로드 확인(2026-09-24 04:49 KST 무렵, 대표 "배포했어", Netlify 읽기)**: 앱 `doitmobile` 현재 배포 `6ab42d25…`(수동 drop, ready, 게시 2026-09-23T19:49:03Z, "80 new files") — 이 환경에서 앱 주소 접속이 막혀(403) 내용 대조 불가, 시각상 34차로 추정. 홈페이지 `echo-mvp-doit` 도 새 배포 `6ab42d5e…` 가 생겼지만 "All files already uploaded" + 운영 index.html SHA-256 = 33차와 동일(`a47d1e1d…`) → 33차를 다시 올린 것, 바뀐 것 없음. 운영 DB 읽기(같은 시각): 목적을 고른 사람 3명, 전화 인증 0명, 연결 0건 — **문자 발송 업체 연결 전이라 아직 아무도 인증·연결을 못 했다. 대표 실기기 미확인 — "완료" 아님.**
- **연결 자격 변경 v14.2 / connect v1.1(2026-09-24 05:41 KST 무렵, 근거 = 대표 "그렇개 바꿔" — 내가 제안한 "자격 = 다섯 가지 질문에 모두 답함"에 대한 답).** 예전 자격 "맞다고 한 말 5개" → **이번 회차 다섯 가지 답(「아니에요」로 버린 답·「처음부터 다시」 이전 답 제외) + 전화 인증 + 필수 사진 3장 + 소개**. 「맞아요」는 자격이 아니라 **후보를 고를 때 겹치는 말 찾기에만** 쓴다(대표에게 그렇게 말했음). 서버 두 곳을 같은 기준으로: `doit-connect` 후보 고르기(`CONNECT_ANSWERS_NEEDED: 5`, 관리자 화면 빠진 이유 「다섯 가지 질문을 다 답하지 않음」) · `doit-understanding` 연결 준비 표시(`readiness.answers`). 화면: 연결 탭 줄 「다섯 가지 질문 n / 5」. **같이 고친 기존 결함**: 앱 홈의 진행 숫자가 「처음부터 다시」 이전 답까지 셌다 → `roundStartOf` 로 이번 회차만. DB·RLS·결제 변경 0. **운영 배포**: `doit-connect` **버전 2** / ACTIVE / verify_jwt=true / 무인증 401 / 내려받은 index.ts SHA-256 `1fca8c24573c1b9e3bfc7880b5ce313e600d941f78ae499ef18d5c516addf1b0` = 로컬 동일. `doit-understanding` **버전 22** / ACTIVE / verify_jwt=true / 무인증 401 / SHA-256 `2d097d0c1079e7ef51ff608366c07948d084dd4c3cd101ce793c3d904954e4f8` = 로컬 동일. **내 실수**: 먼저 올린 버전 21 에 옮겨 적기 오류 3줄이 있었다 — 질문 검사 정규식의 "왜"가 "왕"으로 들어가 "왜" 를 물음말로 세지 못함(기능 결함) + 주석 2곳. 올린 파일을 내려받아 대조하다 찾아 곧바로 버전 22로 교체했다. 검사(가짜 기준) = 새 검사 포함 **366개 중 364 통과 / 0 실패 / 미확정 2**, tsc 0, eslint(src) 0, audit 0, 기준 장치 일부러 지우기 5/5 잡음. **35차 = 앱 ZIP 1개**(SHA-256 `379f9ac6…`, 145개 파일, 소스맵·.env 0). 홈페이지 빌드는 쓰이지 않는 제품 조각 이름만 바뀌어 올릴 필요 없음(운영 33차와 경로·화면 동일 확인). 남은 한계: 겹치는 말이 없으면 자격이 있어도 후보 0, 알림 없음, 약관 반영 필요, 문자 발송 업체 미연결(운영 전화 인증 0명). **아직 아무도 실제로 써 보지 않음 — "완료" 아님.**
- **연결 v1.2 — 막힌 곳 3개 풀기(2026-09-24 06:00 KST 무렵, 근거 = 대표 "최종완성하라고! 그리고 마지막에 보고해" + 연결 승인 "끝까지 진행시켜").** (1) **겹친 말 없는 쌍**: 전에는 「맞아요」로 확인한 말이 겹치지 않으면 후보에서 아예 빠져, 아무도 「맞아요」를 안 누르면 자격자가 있어도 후보 0(빠져나갈 문 없는 차단). → 같은 만남이면 목록 **뒤에 「겹친 말 없음」**으로 남기고(겹친 쌍이 항상 먼저), 관리자가 「겹친 말 없이 승인」→「그래도 승인하고 첫 질문 보내기」 두 번 눌러야 승인(서버 `noCommonOk === true` 확인, 문자열 "true" 불인정). 첫 질문 AI 에는 목적만 보낸다(각자의 말 0). (2) **연결 동의**: 개인정보 처리방침 초안이 "사람 연결은 열리기 전에 별도 안내·동의"라고 약속했는데 연결이 이미 운영에 열려 있었다(**내가 v1 에서 놓친 것**). → 첫 답(=공개의 방아쇠) 전에 보이는 것(닉네임·대표 사진·소개·고른 만남·첫 답, 번호·이메일은 안 보임, 언제든 그만하기)을 보여 주고 「확인했어요, 답할게요」. 저장 = `user_metadata.doit_connect_consent_version = "connect-v1"` + 시각(회차 시작과 같은 방식, DB 변경 0). 서버 `answer` 가 다시 확인해 없으면 `CONSENT_REQUIRED`(적은 답은 화면에 남음). 빠져나갈 문 = 답하지 않고 두기·그만하기. (3) **알림 대신 앱 홈 「내 차례」 카드**: 새 서버 동작 `my_turns`(첫 질문 답할 차례·서로 열림·상대가 마지막으로 말함 개수만, 이름·내용 0, 끝난 연결·차단 제외). 못 불러오면 카드만 숨김. (4) **약관·개인정보 초안**에 연결 공개 범위·전화번호 수집·운영자 확인·문자 발송 업체(`[대표 확인]`) 추가, "준비 중" 문장 삭제. **LEGAL_VERSION(v1.0)은 안 올림** — 올리면 전 회원 재동의라 대표 결정. **운영 배포**: `doit-connect` **버전 3** / ACTIVE / verify_jwt=true / 무인증 401 / 내려받은 index.ts SHA-256 `e9246182a26de21aa498b5b82f0d26374e94eca5e629e8edd2863e15da8ae0f3` = 로컬 글자 단위 동일. doit-understanding 은 버전 22 그대로. 검사(가짜 기준) = 전체 **376개 중 374 통과 / 0 실패 / 미확정 2**, 서버 30/30, 새 보안 장치 일부러 지우기 **6/6** 잡음, 브라우저 클릭 **54/54**, 실제 빌드 화면 5개 오류 0·가로 넘침 0, tsc 0, eslint(src) 0, audit 0. 운영 읽기(같은 시각): 관리자 계정 1, 목적 고른 사람 3, 전화 인증 0, 연결 0. **36차 = 앱 + 홈페이지 ZIP 2개**(약관 초안이 홈페이지에도 있음; 앱 `b68ee7b5…`, 홈페이지 `f0601860…`; 35차는 36차에 포함). **아직 아무도 실제로 써 보지 않음 — "완료" 아님.**
- **36차 업로드 확인 + 「처음부터 다시」·연결 화면 버튼·옛 문구 정리(2026-09-24 06:30 KST 무렵, 대표 실기기 캡처 06:11 "처음부터 시작하기는 아직 구현이 안된거 같다 … 연결에서 질문이어서 답하기 누르면 다음으로 안넘어가고 다섯가지 다 들었다는 문구나온다 … 공간은 뭘 구현하는건지 월드는 뭔지").** 확인: 운영 do-it.company index.html = 36차 빌드 글자 단위 동일(SHA-256 `9cc7c890…`), 대표 캡처의 앱(app.do-it.company)에 「다섯 가지 질문 5/5」 줄이 보여 35차 이후 앱이 올라간 것으로 판단(앱은 이 환경에서 접속 불가). 원인: (1) 「처음부터 다시」는 있었지만 끝 화면에선 맨 위 작은 글씨뿐, (2) 연결 화면 「질문에 이어서 답하기」가 다 답해도 고정 표시, (3) 연결이 열렸는데 끝 화면·프로필 확인·내 프로필·목적 고르기·가입 안내 5곳에 "연결 아직/준비 중" 옛 문장, 프로필 확인의 「공간 준비 소식 보기」가 준비 중 화면(/doit/spaces)으로 보냄. **내 잘못**: 연결 v1 을 열면서 같은 모양의 옛 문구를 찾아 고치지 않았다(검사 기준 "같은 모양 전부"). 수정(화면만, 서버·DB 0): 끝 화면 「처음부터 다시 답하기」 버튼 + 확인 창에 "다섯 가지를 다시 답할 때까지는 새 연결 후보에서 잠시 빠져요"(자격이 회차 기준이라 사실), 끝 화면에선 위쪽 중복 버튼 숨김(?restart=1 확인 창은 유지), 연결 화면은 남은 것 중 첫 번째를 큰 버튼 + 다 답했으면 「처음부터 다시 답하기」, 옛 문장 5곳 교체, 프로필 확인 다음 = /doit/connections, "아래를 다 채우면"→"위 네 가지를 다 채우면". 옛 사실을 지키던 검사 2개는 새 사실 기준으로 교체. 검사(가짜 기준) = **378개 중 376 통과 / 0 실패 / 미확정 2**, 브라우저 클릭 **61/61**, tsc 0, eslint(src) 0, audit 0. **37차 = 앱 ZIP 1개**(SHA-256 `0fcb9e54…`). 설명서 `docs/claude-final-review-20260916/PATCH-20260924-restart-and-copy/`. 공간(/doit/spaces)·월드(/doit/world)는 초기 설계의 자리만 있는 "준비 중" 화면(기능 0) — 하단 탭에서 숨길지 대표 결정 대기. 얼굴 로그인(패스키): Supabase 가 실험 기능으로 지원(supabase-js 2.105.0+ 필요, 현재 2.57.4), 대표에게 가능·조건 설명함, 착수는 대표 "진행해" 대기. **대표 미확인 — "완료" 아님.**
- **얼굴·지문 로그인(패스키) + 「처음부터 다시」 잘 보이게(2026-09-24 07:00 KST 무렵, 대표 "정정 처음부터 시작하기 있다 근데 못찾겠어 조금 눈에 띌수있게 만들고 얼굴로그인 진행해"). 서버·DB·Auth 설정 변경 0. 38차 ZIP(앱 `9b7a7592…` + 홈페이지 `171ba4d4…`, 로그인 부품이 바뀌어 둘 다), 대표 수동 업로드 필요.** (1) 「처음부터 다시」: 작은 회색 글씨 → 테두리 알약 버튼 + ↺ 그림(대화 위·아래 `.echo-restart-pill`, 앱 홈 `.doit-restart-pill`, 각 화면 루트 클래스 아래로만). (2) **패스키**: 로그인 부품 `@supabase/supabase-js` 2.57.4 → **2.117.1**(정확한 판 고정, 이 판에서 패스키는 기본 켜짐·experimental 깃발 불필요). `src/lib/auth/passkey.ts`(기기 판단 순수 함수 = 앱 안 브라우저면 밖으로·WebAuthn 없으면 숨김, 오류 → 할 일 문구 8종, signInWithFace·registerFace·listFaces·removeFace, 로그·저장소 0). 로그인 화면 「얼굴·지문으로 로그인」(지원될 때만, 성공 시 이메일 로그인과 같은 곳으로), 설정 → 「얼굴·지문 로그인」(`FaceLoginSettings.tsx`, 로그인한 사람만, 등록·목록·지우기 두 번 확인, 서버 꺼짐이면 "아직 켜지지 않았어요"). 빠져나갈 문 = 기존 이메일·Google 로그인 그대로. **Supabase 대시보드 Authentication → Passkeys 켜기는 대표 몫**(RP ID `do-it.company`, Origins `https://do-it.company,https://app.do-it.company`; RP ID 변경 시 등록 전부 무효). 영향: 첫 화면 JS 압축 138KB → 173KB. **내 실수**: 작업 폴더 package-lock.json 이 다른 폴더 주소(`../../implementation/project/…`)로 적힌 불완전한 목록이었는데 확인 없이 설치 명령을 돌려 검사 도구가 빠지고 검사 26개가 한때 깨졌다 → 판 번호는 그대로, 주소만 바로잡아 재설치(판 변경 0, 미사용 3개 제거), 검사 도구(esbuild·playwright)는 프로젝트 밖 `scratchpad/tools` 로 분리. 검사(가짜 기준) = **384개 중 382 통과 / 0 실패 / 미확정 2**, 새 `qa/passkey.test.mjs` 6개, **실제 브라우저 + 가상 얼굴 인식 장치 + 가짜 로그인 서버로 등록→로그아웃→얼굴 로그인 23/23**(서버가 준 챌린지·주소로 서명한 것까지 확인), 보안 장치 일부러 망가뜨리기 4/4, 연결 화면 61/61, 실제 빌드 화면 오류 0, tsc 0, eslint(src) 0, audit 0. 설명서 `docs/claude-final-review-20260916/PATCH-20260924-face-login/`. **진짜 아이폰·갤럭시로는 아무도 안 써 봄 — "완료" 아님.**
- **v14.3 대화 질문 가볍게(2026-09-24, 대표 실기기 "질문이 앞뒤도 안 맞고 … 오타도 있는 것 같고 너무 딥해 … 라이트하게").** 원인: 운영 대화에서 "상대가 알면 좋을 나은 어떤가요?"(깨진 문장), 되묻기("활동?질문이 머이래"·"딥하네")가 답으로 저장, 다시 묻기가 반말·두 질문, 같은 주제("그런 사람과 같이 뭘 하고 싶으세요?") 반복. → 서버: 주제별 쉬운 질문 `EASY_QUESTION`(예: "어떤 사람한테 끌려요?"·"만나면 같이 뭐 하고 싶어요?"), 모든 AI 질문은 `lightQuestion`(본문 45자 이내·질문 하나·무거운 말 금지·해요체 끝) 통과해야 쓰고 아니면 쉬운 질문으로, 되묻기 판정 `META_PATTERNS`(화면 `conversationRules.ts` 와 동일) 확장, 다시 묻기는 주제의 쉬운 질문으로 되돌아감. 화면: 다시 묻기에 주제 전달. **운영 배포: doit-understanding 버전 23 / ACTIVE / verify_jwt=true / 무인증 401 / 내려받은 index.ts SHA-256 `1c5266258b1be1d02fce1272c68009b22b3ae49e4a53788ec0ecb57e081ef069` = 로컬 글자 단위 동일(117,026바이트).** 검사(가짜 AI 기준) = 390개 중 388 통과/0 실패/미확정 2. 진짜 AI 대화는 대표 실기기 미확인.
- **환경 설정 안내 정정(2026-09-24, 내 실수)**: 대표에게 "제목 줄 구름 아이콘 → Edit → Network access 에 netlify 추가"를 안내했으나, 대표 화면의 환경 팝업(클라우드 / 환경 에코 두잇)은 눌리지 않았다. 안내 문서상 네트워크 허용 목록은 환경 편집 화면에서 바꾸는데, 대표 계정에서 편집이 막혀 있으면 조직 관리자 권한이 필요할 수 있다(검증 필요). 그 전까지 Netlify 업로드는 대표 수동.
- **출시 1.0 — 회원 탈퇴 · 메뉴 정리(2026-09-24, 대표 "출시 1.0 진행해 실수하지말고 다 만들고 전체 검수진행해" + 탈퇴 서버 배포 선택지 "지금 올려").** 설명서 `docs/claude-final-review-20260916/PATCH-20260924-release-1.0/`. (1) **회원 탈퇴**: 새 서버 `doit-account`(preview = 지워질 개수만 / delete_me = 로그인 토큰 주인만, 확인 값 `delete-my-account-v1` 필수, 사진 파일 먼저 지우고 다시 훑어 0장 확인 뒤 `auth.admin.deleteUser`, 관리자·결제 기록 계정은 멈추고 메일 안내, 로그에 번호·이메일 0). DB 는 계정 삭제 시 관련 표가 이미 CASCADE/SET NULL(운영 읽기로 확인) → **DB·RLS 변경 0**. 화면 설정 → 「탈퇴 전에 지워지는 것 보기」 → 확인 칸 → 「탈퇴하고 모두 지우기」 / 「그대로 둘게요」, 메일 요청은 늘 보임. 약관 해지 조항 갱신. **운영 배포: doit-account 버전 1 / ACTIVE / verify_jwt=true / 무인증·가짜 토큰 401 / 내려받은 index.ts SHA-256 `97af5498a1dd6433a46cbfd5bfb22a613f1fa172124b8dab6ba47fd2dc90026a` = 로컬 동일.** 실제 계정으로 탈퇴는 누르지 않음. (2) **메뉴 정리**: `src/doit/lib/releaseScope.ts` 한 곳에서 공간·월드 탭, Just Try·사주·타로·등급 가이드 메뉴, 알림 종을 숨김(화면·주소는 그대로). 햄버거 「ECHO와 이야기하기」→「다섯 가지 질문」. **사주·타로를 숨긴 이유(발견)**: openai-chat 버전 2 가 app.do-it.company 를 허용 주소로 돌려주지 않아(실제 요청 확인) 앱에서 타로가 실패하고, 로그인 확인도 꺼져 있다(verify_jwt=false). 다시 켜려면 토큰 실검증판 배포 + 허용 주소 = 대표 승인. (3) 남는 것: `key_*` 표(예시 단계 4줄)는 계정 연결이 없어 탈퇴 후에도 남음 — 열쇠 기능 열 때 정리. 검사(가짜 기준) = 전체 **404개 중 402 통과 / 0 실패 / 미확정 2**, 탈퇴 서버 14/14, 안전장치 망가뜨리기 8/8, 브라우저 클릭 33/33, 실제 빌드 화면 15/15(360·390·430px), tsc 0, eslint 0, audit 0, 소스맵 0. **40차 = 앱 `1ba09e79…` + 홈페이지 `807dda78…`(38·39차 포함, 39차는 전달 안 함).** 운영: 홈페이지 36차(글자 대조), 앱 37차 추정. **진짜 탈퇴·실기기 아무도 안 해 봄 — "완료" 아님.**
- **Twilio 중단 + 정밀검수 + AI 질문 맥락 P0 v14.4(2026-09-24, 대표 "Twilio 연동은 중단한다 … 코드·DB·RLS·Auth·Secret·배포 변경 금지" / 사주 명세·정밀검수·"숨기지 마라" 정정 / "[긴급 정정] AI 질문 자체가 앞뒤 대화와 맞지 않는다 … 비공개 베타 테스트를 아직 시작하지 않는다").** (1) **doit-account 재확인**: 버전 1, 지문 불변, 무인증·가짜 토큰 401, 함수 로그 전부 401 = 실제 탈퇴 0건, 사용자 6명. 배포 성공 ≠ 운영 탈퇴 PASS. (2) **SMS 중단의 영향(대표 결정 필요)**: 연결 자격에 전화 인증 필수(`doit-connect` `missing.push("phone")`) → SMS 없이 전원 연결 불가. (3) **사주·타로 실측**: 사주 = 입력 화면 + "준비 중" 고정 결과·예시 틀(엔진 미구현, 저장·AI·동의 없음, 거짓 약속 문장 2개). 타로 = 기능 있음, 운영 openai-chat 이 app.do-it.company Origin 을 403 으로 거부(실측) → [기능 존재+연결 설정 오류], 호출 기록 0줄. openai-chat = verify_jwt=false + 함수 안 로그인 확인 없음 + Origin 없는 요청 통과 + 제한이 호출자가 만든 세션 번호 기준 → 로그인 없이 비용 사용 가능(공격은 안 함). **40·41차의 사주·타로·공간·월드 메뉴 숨김은 대표 정정("숨기지 마라")과 충돌 — 대표 결정 전 추가 수정 안 함.** key_* 표는 "demo"가 아니라 9/13 서버 시험 기록(test_only_smoke) 4+1+4줄이며 앱이 쓰지 않음(내 표현 정정). (4) **AI 질문 맥락 원인(코드 확인)**: 짧은 답이면 새 주제 + 지침 "앞 말과 억지로 잇지 않아도 된다" / 새 주제는 연결 검사 면제·판정 불허여도 첫 줄만 떼고 발송 / AI 실패 시 주제별 고정 질문(EASY_QUESTION·GENERIC_RESCUE)으로 건너뜀 / 질문 생성에 history 미전달·직전 질문 부정확 / "왜 이런 걸 물어봐?"를 답으로 저장. **Before/After(같은 가짜 AI 답)로 운영 v23 이 "잘 웃는 사람" 뒤 "쉬는 날에는 무엇을 하세요?"를 그대로 내는 것 재현.** (5) **v14.4(로컬만, 운영 미배포 — 이번 지시서 STOP 에 운영 Edge 배포가 있어 자동 배포 규칙보다 우선)**: 모든 다음 질문에 link(방금 답에서 그대로 인용) 필수 + 첫 줄·질문이 받아야 함, 새 주제는 첫 줄 다리 필수(거절 직후만 예외), 판정 1순위 = 앞뒤 연결·불허면 발송 안 함, history(최근 5 짝)·정확한 직전 질문(화면 answeredQuestion → 답 시각 짝짓기) 전달, 고정 질문 목록 삭제·AI 실패 시 방금 답에 머묾, AI 에게 한 질문(`isAskingAi`, RULES 공용)은 기록 없이 사실 5개 안에서 먼저 답하고 같은 질문 재제시. 로컬 SHA-256 `e3522bbffddacd63ba62a414cc5adf9e99c21f5e2cefe536d2eb0d76ea824944`. 검사(가짜 AI 기준) = **420개 중 418 통과 / 0 실패 / 미확정 2**, 새 `qa/conversation-continuity.test.mjs` 13개, 역검사 17/17(⑬은 처음 못 잡아 검사를 보강), tsc·eslint·서버 엄격 타입·audit 0, 실제 빌드 화면 15/15. **진짜 AI 검사 = 확인 불가(키 없음·검사 계정 로그인 불가·v23 이후 실제 대화 0건) → 비공개 베타 가능 아님.** 41차 ZIP(앱 `f12d7e70…` · 홈페이지 `757a5196…`, 40차 포함) 준비만, 전달·업로드 안 함. 문서: `docs/claude-final-review-20260916/PATCH-20260924-conversation-continuity/`(보고서·실기기 검사표·Before/After·역검사 스크립트), `PATCH-20260924-release-1.0/ECHO_출시전_정밀검수_20260924.md`, `GPT_전략본부_전달_20260924.md`. **아직 아무도 실제로 써 보지 않음 — "완료" 아님.**
- **v14.4 운영 배포 + 41차 앱 ZIP(2026-09-24 08:28 KST 무렵, 대표 승인 "[ECHO 대표 승인 · AI 질문 맥락 P0 운영검증 단계]" — 승인 범위 = ① doit-understanding v14.4 운영 배포 ② 운영↔로컬 동일성 대조 ③ CoreConversation 변경이 새 서버 계약에 꼭 필요할 때만 41차 APP ZIP. Netlify 업로드는 대표 직접, BRAND 는 바뀐 것 없으면 안 만듦).** (1) **배포**: `doit-understanding` **버전 24** / ACTIVE / verify_jwt=true / 무인증 401(2회) / 내려받은 index.ts SHA-256 `bc3b780bfc1eae69e0cc0387a360ad2818da3f334efa72b848e3c4afd295a8a2`(130,554바이트) = 검사한 로컬 파일 글자 단위 동일(cmp). 앞선 로컬 `e3522bbf…` 와의 차이는 link 검사 위 주석 한 곳("P0 안전장치이지 영구 UX 규칙이 아니다"). 다른 함수 9개는 버전·수정 시각 그대로(doit-understanding 만 23:28Z 수정). 되돌릴 v23 원본은 작업 폴더에 보관(SHA-256 `1c526625…`). (2) **41차 APP 이 필요한 이유**: 옛 앱(37차)은 `answeredQuestion`(답할 때 떠 있던 질문)을 보내지 않아 첫 답·되물은 질문의 직전 질문을 서버가 모르고, "왜 이런 걸 물어봐?"를 답으로 저장한다(서버만으로는 막을 수 없음). (3) **내 실수 먼저**: 앞서 만든 41차 ZIP(`f12d7e70…`)에는 승인 안 된 40차 메뉴 숨김(공간·월드 탭, Just Try·사주타로·등급·알림)이 들어 있었다 → 숨김 목록(`releaseScope.ts`)을 비우고 햄버거 첫 줄 이름을 운영과 같은 「ECHO와 이야기하기」로 되돌렸다. 메뉴 두 파일의 항목은 38차 원본과 동일(숨김 장치만 남음, 목록 비어 있음). 메뉴 숨김은 대표 결정(백로그). (4) **41차 APP ZIP** = `1_APP_여기에올릴것_doitmobile.zip` 3,056,953바이트 · 150개 파일 · SHA-256 `e730494bff9ca9a6c37505b2869e5e6213a2a18fd4d8fc0c14c714308f01d9b6` · 소스맵·.env 0 · 풀어서 빌드 폴더와 동일. 운영 앱이 37차라 38차(패스키)·40차(앱 안 회원 탈퇴 화면)가 함께 올라간다. **BRAND 는 안 만듦**: 새 빌드의 메인 코드가 40차 BRAND 와 파일 이름(해시)만 다름. 운영 홈페이지는 36차 그대로(38·40차 홈페이지 약관 문구 미반영 — 별건). (5) **검사(가짜 AI·가짜 서버 기준)**: .mjs 420개 중 418 통과/0 실패/미확정 2(.ts 포함 439/437/0/2), 관련 검사 continuity 13·flow 50·question-state 26·rules 6·account-delete 14 모두 통과, tsc 0, eslint 0, 서버 엄격 타입 0, npm audit 0, 빌드 0, 실제 41차 빌드 브라우저 17/17(360·390·430·1440px, 가로 넘침 0, 오류 0, 탭 = 홈·공간·월드·연결·프로필 + 알림 종 + 햄버거 5개), 탈퇴·메뉴 브라우저 33/33. (6) **상태 = [운영 실AI 검증 대기]**. 진짜 AI 대화는 아직 아무도 안 해 봄. 대표 실기기 검사표 A~F + 「~라고 하셨죠」 두 번 이상 되풀이 = 목표 미달로 보고. 배포 성공 ≠ AI PASS. 백로그(별건): 사주 엔진, 타로 403(openai-chat Origin), Twilio, 연결 자격의 전화 인증, 사진 AI 판별이 자격에 안 쓰임, 메뉴 숨김 충돌. **"완료" 아님.**
- **AI 대화 구조 v15(2026-09-24, 대표 명세 「ECHO AI 대화구조 최종 구현명세」 + 「세션 중단 복구 · AI P0 계속 실행」). 로컬만 — 운영 배포 0(doit-understanding 운영 = 버전 24 / v14.4 그대로), APP ZIP 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260924-ai-conversation-v15/ECHO_AI대화구조_v15_보고_20260924.md`. 내용: 다섯 턴 동안 답마다 4버튼 카드 없음(화면 `api.generate` 호출 0) → 다섯 번째 답 뒤 통합 카드 「내가 이렇게 이해했어요」(2~4항목)에서만 4버튼, 「맞아요」 항목만 사실. 다음 질문 = LLM 후보 → 서버 검증 → 떨어진 이유를 붙여 재생성 최대 3번 → 그래도 안 되면 명시적 실패(고정 대체 문장 0). 입력 분류(ask·complaint·fatigue·unsure·correction)는 저장하지 않거나(ask·complaint·fatigue) 사실로 쓰지 않음(unsure). 로컬 SHA-256 `5602b4c3…`. 검사(가짜 AI 기준) = 456개 중 454 통과/0 실패/미확정 2, 회귀 A~F 통과, 역검사 20/20, type-check·lint·서버 엄격 타입·audit 0, 앱 빌드 화면 18/18(로그아웃 상태만). **서버와 앱을 같이 올려야 한다(대표 승인 필요). 운영 실AI 검증 대기 — "완료" 아님.** 전화 인증 필수 + Twilio 중단 = 운영 연결 자격자 0명(실측 전화 인증 0·연결 0), 조건 변경은 대표 결정.
- **회원탈퇴 구조 설계(2026-09-24, 전략본부 확정 기준 · 설계·검증만).** 문서 `docs/claude-final-review-20260916/PATCH-20260924-withdrawal-design/ECHO_회원탈퇴_구조설계_20260924.md`(운영 DB·Storage 읽기 전용 분류표 21행 + A~H), DB 초안 `supabase/drafts/PENDING_20260924_withdrawal_retention.sql`(**실행 금지**, 되돌리기 포함). 문제 7개 유지: 동의 소실 · 신고 대상 소실/신고 삭제 · 결제 CASCADE · KEY 잔존(9줄, 주인은 살아 있는 계정 1개 — 앞서 "주인 없는 시험 줄"처럼 말한 것 정정) · 재가입 회피 · 약관 불일치 · 접속 기록 보관 위치 확인 불가(auth.audit_log_entries 0줄). 권장 = 즉시 삭제 유지 + 계정과 끊긴 최소 보관 표(동의 증빙·신고 옛 번호·결제 SET NULL) + 제재 확정 계정만 식별값 HMAC 보류(만료 필수, 자동 거절 아닌 관리자 확인). 보관 기간은 확정하지 않음([법무 검증 필요] L1~L8). 7일 비활성화형은 후보로만. DB·RLS·Auth·Secret·배포·운영 데이터 변경 0.
- **v15 MASTER↔CODE 최종 대조(2026-09-24, 증거 정리만 — 코드 변경 0·배포 0).** 문서 `docs/claude-final-review-20260916/PATCH-20260924-ai-conversation-v15/ECHO_MASTER_CODE_최종대조_v15_20260924.md`. 증명 = **LEVEL 1(가짜 AI)만 통과, LEVEL 2(실제 OpenAI)·LEVEL 3(운영 5턴) 0회.** 운영 doit-understanding 버전 24 그대로 확인. 새로 드러난 것: ① 미확정 2개는 v15가 아니라 옛 ECHO STEP 1→7 흐름(get-step-question v48·echo-journey v26, 운영 중) 검사이고, 그 옆 검사 「여정 끝까지 40회 … STEP 7 까지 끊기지 않는다」는 이름과 달리 반말만 확인해 **30/40 INVALID_STATE·STEP 7 도달 0인데 통과**로 세고 있었다(v15 이전부터, 내가 놓침). ② 턴 중 "그게 아니에요"는 거절 의미 차단이 다음 답 때로 미뤄지고 한 번뿐·저장 안 됨 = 부분 일치(목표 미달). 통합 카드의 「그게 아니에요」는 일치. ③ "모르겠어요"는 성향·사실·카드 재료로 안 쓰이지만 다섯 칸과 연결 자격 "다섯 답"에는 셈 — ×5로 자격 충족 가능([대표 결정 필요]). ④ 바이그램 휴리스틱 4종 유지 [휴리스틱 / 추가 검증 필요]. ⑤ 옛 흐름에 고정 대체 질문("…라고 하셨죠. 그중 어떤 부분이…") 운영 중. 역검사 20/20 대응표·원본 복구(문장 단위 20/20, 지문 작업=저장소=커밋) 기록. **내 실수**: 복구 확인 스크립트가 ⑬을 잘못 "변형 남음"으로 표시(변형이 원문 앞부분이라) — 원문 1회 존재 확인으로 정정. "AI 최종 PASS"·"최종 완료"·"비공개 베타 가능" 아님.
- **v15.1 — 대표 결정 반영 + 서버 본래 역할(Conversation Orchestrator) 대조 + 질문 톤 LOCK(2026-09-24). 로컬만 — 운영 배포 0(doit-understanding 운영 = 버전 24 / v14.4, doit-connect 운영 = 버전 3), APP ZIP 0, DB·RLS·Auth·Secret 변경 0.** 대조 문서 `docs/claude-final-review-20260916/PATCH-20260924-ai-conversation-v15/ECHO_Conversation_Orchestrator_대조_v15.1_20260924.md`. 판정 = **PARTIAL**(로컬 코드는 구조를 갖춤·LEVEL 1 만, 운영은 v14.4, 실AI 0회). 고친 것: (1) 「모르겠어요」 등은 대화 칸에는 세지만 연결 자격·소개 재료에서 제외(RULES `informativeAnswer` 를 doit-connect 에도 같은 블록으로 · 연결 화면·끝 화면에 넘긴 답 안내 + 「처음부터 다시 답하기」 빠져나갈 문), (2) 대화 중 「그게 아니에요」를 분류 순간 서버에 저장(`doit_request_events` action `followup_reject`, 새 표·칸 없음) → 다음 질문·「다른 질문 받기」·통합 카드·예전 앱 이해 후보·소개 초안 모두 차단, 새로고침 뒤 정정 전략 복원, 두 번째 「아니야」는 안내 되풀이 없이 다른 질문, 안내문은 질문 후보로 못 씀, (3) 상태 관문: 이번 회차 답 다섯이면 서버가 AI 를 부르지 않고 끝(전에는 화면만), (4) 목적·톤 관문(판정 AI 기준) + 질문 말투에 대표 톤 LOCK + 「왜」 연속 금지(결정적), (5) **분류 규칙 오판 수정** — LEVEL 2 스크립트 연습 실행에서 「보드게임 같은 거」·「연애에 지쳐서…」 등 실사용 답 16개 중 14개가 불만·지친 말로 판정돼 저장되지 않던 것을 발견, 규칙은 대화 자체를 가리키는 분명한 말만 잡고 애매한 말은 AI 분류(실패 시 답)로. 로컬 지문 doit-understanding `cdd3e215…`(181,689바이트) · doit-connect `9696358f…`. 검사(가짜 AI 기준) = 479개 중 474 통과 / 0 실패 / 미확정 5(휴리스틱 공격 2 · 옛 흐름 3, LEGACY-01 포함), type-check·lint·서버 엄격 타입 2개·audit·빌드 0, 브라우저 18/18·연결 화면 11/11, 역검사 v15.1 16개(B5·B6 보강 뒤 모두 잡음) + 게이트 8개(D4 보강 뒤 모두 잡음), 시작 전·끝 지문 일치. **내 실수 2건**: v15 검사 장치가 답마다 이해 후보(gen) 경로의 가짜 AI 답을 한 번도 읽지 못하고 있었다(글을 JSON 으로 읽다 오류) — 고침 / 휴리스틱 공격 검사를 처음에 짧은 답으로 만들어 다른 규칙(새 갈래 다리)에 걸려 잘못 읽었다 — 12자 넘는 답으로 다시 만듦. LEVEL 2 스크립트 `qa/level2/level2-real-ai.mjs`(로컬 서버 코드 + 메모리 DB + 실제 OpenAI, 키는 환경변수만·출력 안 함) 준비 — 이 환경에 키 없음 → **LEVEL 2 확인 불가**. LEVEL 3 검사표 `LEVEL3_실기기_검사표_v15.1.md`. LEGACY-01 등록 `LEGACY_ISSUES.md`. **"AI 최종 PASS"·"최종 완료"·"비공개 베타 가능" 아님.**
- **Conversation P0 FINAL LOCK + PRODUCT DEFINITION PM FINAL 1~3 통합 반영(2026-09-24). 로컬만 — 운영 배포 0, DB·RLS·Auth·Secret 변경 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260924-ai-conversation-v15/ECHO_ConversationP0_최종보고_v15.1_20260924.md`. 판정: P0 A+B+C = **목표 미달**(B 확인 불가 — LEVEL 2·3 0회) · Q1 Orchestrator = **PARTIAL** · Q2 Relationship Data Foundation = **PARTIAL**(대화 중 거절이 요청 기록 표에 있음 → Relationship Data Contract 에서 정식 자리). 제품 정의와 코드 충돌: 76%·적합도 %·독점 주장 0건. 마지막 보강: 「좀 쉬자」·「쉴래」 등 쉬어 가기 말을 지친 말 규칙에(문장 전체일 때만 · 「쉬고 싶어요」는 AI 가 가름), 판정 AI 에 정보 관문(새로 알게 되는 게 없는 질문 불허). 로컬 지문 doit-understanding `b3384b79…`(181,961바이트) · doit-connect `9c2bac14…`. 검사(가짜 AI 기준) = 479개 중 474 통과/0 실패/미확정 5, type-check·lint·서버 엄격 타입 2개·audit·빌드 0, 운영용 빌드 브라우저 18/18. **42차 APP ZIP 후보** `a1e529ff…`(3,055,591바이트·149개, 서버 v15.1 배포 뒤 업로드, 대표 승인 전 업로드 금지 — 전달 안 함). 다음 단계는 배포 승인 → LEVEL 2(키) → LEVEL 3(대표 실기기). "완료" 아님.
- **v15.1 운영 배포(2026-09-24 18:28~18:35 KST 무렵, 근거 = 대표 「Conversation P0 v15.1 · 대표 승인 운영배포 + LEVEL 3 최종검증 실행명령 · FINAL EXECUTION LOCK」).** 기록 `docs/claude-final-review-20260916/PATCH-20260924-ai-conversation-v15/DEPLOY_v15.1_운영배포기록_20260924.md`. 순서 = 서버 두 개 먼저(새 서버는 옛 앱과 호환) → 앱. **doit-understanding 버전 25 / verify_jwt=true / 내려받은 index.ts SHA-256 `b3384b7969f997f1f5bddaa5f1baf612351c44f0b3fa704b16ae7a3637c7349c` = 로컬 동일. doit-connect 버전 5 / verify_jwt=true / SHA-256 `9c2bac1448a90cde818ef898590c858c823974af074b8af7e5db616264c48124` = 로컬 동일.** 무인증·가짜 토큰 401, anon 키 요청에 새 코드가 직접 UNAUTHORIZED 로 답함(부팅 정상). 다른 함수 8개 버전 불변. **내 실수**: doit-connect 버전 4 에 빈 줄 하나를 빠뜨려 올렸다(동작 동일, 지문 불일치) → 대조로 찾아 약 6분 뒤 버전 5 로 교체. 되돌리기 세트 = `rollback/doit-understanding.v24.ts`(`bc3b780b…`)·`rollback/doit-connect.v3.ts`(`e9246182…`)·앱 Netlify 배포 `6ab46ba0…`. DB·RLS·Auth·Secret·모델·결제 변경 0. **앱 42차 ZIP(`a1e529ff…`) 대표에게 전달 — Netlify 는 이 환경에서 막혀(connect_rejected) 대표 수동 업로드.** 기존 운영 버전에 대한 대표 실기기 테스트와 실제 실패 데이터는 존재한다. v15.1 수정본에 대한 LEVEL 3 운영 실기기 검증은 아직 수행 전이다. "완료" 아님.
- **앱 42차 운영 반영(2026-09-24 18:40 KST, 대표 수동 drop).** ZIP 재확인 `a1e529ff…` = 확정값 → [APP42 DEPLOY CANDIDATE = 일치]. Netlify `doitmobile` 현재 배포 `6ab4f006cbad731637fc32b8`(ready, 게시 09:40:33Z, "98 new files"). 앱 주소 접속이 막혀 내용 대조는 못 했다 — Netlify 기록과 시각으로 42차라고 추정한 것이다. 되돌릴 이전 배포는 `6ab46ba0…`. **대표 지시(2026-09-24): 이후 새 개발·Mock 검사 확대 금지, 대표 LEVEL 3(A~J) 대기. A·B·C 가 모두 확인되면 Conversation P0 = PASS 로 닫고 곧바로 Relationship Data Contract 로 넘어간다. 실패는 원문→직전 질문/답→기대→실제→서버 상태→원인 파일/함수→최소 수정→같은 실패 재검사 순서로만 처리한다. 전화 인증·회원탈퇴·KEY·옛 STEP 1→7·사주타로는 P0 판정에 섞지 않는다.** 기존 운영 버전에 대한 대표 실기기 테스트와 실제 실패 데이터는 존재한다. v15.1 수정본의 LEVEL 3 운영 검증은 배포 후 새로 수행한다.
- **LEVEL 3 실제 실패 → v15.2 PATCH READY(2026-09-24, 대표 실기기 18:57 KST 무렵 "그냥 편한친구 부담없이" → "다음 질문을 아직 만들지 못했어요"). 판정 LEVEL 3 Conversation = FAIL. 운영 미배포 — 재배포는 대표 승인 대기(v15.1 승인과 별개의 새 변경).** 문서 `docs/claude-final-review-20260916/PATCH-20260924-level3-fix/LEVEL3_실제실패_수정_v15.2_20260924.md`. 원인(운영 로그로 확정): 12자 답 → 새 갈래(CHANGE_DIRECTION) → 요청 3번 × 후보 3개 = 9개 전부 `no_bridge`. 서버가 받아 주는 첫 줄의 근거를 `basis` 칸의 글자 인용으로만 봐서 첫 줄이 조용히 지워졌고, 다시 만들 때 "첫 줄이 없었다"고만 알려 같은 실수를 반복했다(세부 이유는 원문 미기록 원칙이라 운영에서 확정 불가 → 이제 `detail` 코드로 남김). 수정: ① 첫 줄이 방금 답의 낱말을 담으면 근거로 인정(`ackGrounded`) ② 떨어진 세부 이유를 다음 후보 요청에 그대로 ③ 다리를 두 번 못 만들면 새 주제 대신 방금 답에 머무는 전략으로 마지막 후보(빠져나갈 문, 다른 검사는 그대로). 특정 문장 예외·고정 질문·숫자 변경 0. 화면: 「처음부터 시작하기」를 대화에 들어온 순간부터(첫 답 전·오류 상태 포함) 위쪽에 두고, 확인은 「지금 대화를 여기서 끝내고 처음부터 다시 시작할까요?」 → 「계속할게요」/「처음부터 시작할게요」. 방식은 기존 회차 장치(user_metadata 시작 시각 + 목적 비우기) — 원문·이해·거절 기록 삭제 0. 검사(가짜 AI 기준) = 484개 중 479 통과/0 실패/미확정 5, TEST A(같은 입력에서 운영 v15.1 코드 502 재현 → 고친 코드 200)·B·C·D·E·F 통과, type-check·eslint·서버 엄격 타입·audit·빌드 0. **doit-understanding v15.2 로컬 `fd2e1eee…`(운영 버전 25 = v15.1 그대로) · doit-connect 변경 0 · 앱 43차 ZIP 후보 `c1e666d5…`(미전달).** DB 변경 필요 없음. 실제 OpenAI·실기기 확인 전 — "완료"·"P0 PASS" 아님.
- **v15.2 운영 배포(2026-09-24 20:28 KST 무렵, 근거 = 대표 「ECHO 전략본부 · v15.2 배포 승인」).** 기록 `docs/claude-final-review-20260916/PATCH-20260924-level3-fix/DEPLOY_v15.2_운영배포기록_20260924.md`. **doit-understanding 버전 26 / ACTIVE / verify_jwt=true / 운영에서 내려받은 index.ts SHA-256 `fd2e1eee80ccf5648822245a074b935a6566ce831fd2c4933a163172cd3ffedd`(186,093바이트) = 로컬 글자 단위 동일(cmp)** · 무인증·가짜 토큰 401 · anon 키 요청에 새 코드가 직접 UNAUTHORIZED · booted 23ms · 오류 0 · 다른 함수 9개 버전 불변 → 서버 배포 PASS(대화 품질 PASS 아님). 이번부터 MCP 옮겨 적기 대신 Supabase 관리 API 로 로컬 파일을 그대로 올림(옮겨 적기 실수 2회 재발 방지). 되돌리기 = `rollback/doit-understanding.v25.ts`(`b3384b79…`) · 앱 Netlify `6ab4f006…`. 앱 43차 ZIP `c1e666d5…`(빌드 폴더와 차이 0) 대표에게 전달 — 대표 수동 업로드. 실AI 재검증(「그냥 편한친구 부담없이」 ①~⑦)은 이 환경에서 확인 불가(키·계정 없음, 우회 금지) → 대표 실기기 + 운영 기록 읽기로 판정. DB·RLS·Auth·Secret·모델·결제·KEY 변경 0. **Conversation P0 = FAIL 유지.**
- **LEVEL 3 FAIL #2 구조 원인분석(2026-09-24, READ ONLY — 코드·배포·DB 변경 0).** 문서 `docs/claude-final-review-20260916/PATCH-20260924-level3-fail2/LEVEL3_FAIL2_구조원인분석_20260924.md`. 운영 실측(v26): 앱이 09:57 의 같은 A1 「그냥 편한친구 부담없이」를 이어 받아(「처음부터 시작하기」는 안 누름) 다음 질문 16.2초·200 → 「편한 친구를 원하시군요.\n어떤 활동을 함께 하고 싶어요?」(후보1·2 rejected, 3 통과), 화면 주제 이름은 「상대가 알면 좋을 나」(topic=self)로 질문과 어긋남. A2 「활동?갑자기?」는 AI 분류가 answer("애매하면 answer") → 답으로 저장·칸 2/5 → 다음 질문 11.9초 502(no_bridge/ack_repeat ×2 → bridge_fallback → not_anchored) + 화면 자동 재요청 8.1초 502. 원인: 12자 이하 → 서버가 새 갈래(CHANGE_DIRECTION) + 주제 칸 순서(TOPICS)로 방향을 정하고, 프롬프트 예시가 "같이 뭐 하고 싶어요"·「~시군요」 틀을 가르치며, 받아 주는 첫 줄만 있으면 넘어간 것을 이어졌다고 인정(v15.2 의 ackGrounded — 내 수정이 점프를 통과시킴). 질문 하나에 OpenAI 4~5회(최악 요청당 9). 제안(미승인): 한 턴 LLM 1회(분류+단서+질문) + 서버는 상태·거절·정정·이미 물음·안전만 결정, 12자 분기·주제 칸 방향·no_bridge·not_anchored 등 휴리스틱 삭제, DB 변경 0. **Conversation P0 = FAIL 유지.**
- **Conversation Architecture v16 코드 수정(2026-09-24, 근거 = 대표 「새 구조 코드수정 승인 · 운영배포 미승인」). 로컬만 — 운영 배포 0(doit-understanding 운영 = 버전 26 / v15.2), Netlify 0, DB·RLS·Auth·Secret·모델·결제·KEY 변경 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260924-v16-conversation/V16_코드수정보고_20260924.md`. 새 동작 `turn` = 한 턴에 OpenAI 1번(말의 종류·방금 말에서 가장 궁금한 단서·짧은 반응·다음 질문), 서버 필수 검사(원문 출발 = 단서가 원문 안에 글자 그대로 · 이미 물음 · 거절 · 정정 · 안전 · 형식)에 걸릴 때만 1번 더(최대 2). 분류가 끝난 뒤에만 답으로 저장(메타·불만은 저장 0·칸 0), 빠져나갈 문 「이 말은 답으로 남길게요」. v16 경로는 12자 분기·주제 칸 순서·composeQuestion·checkCandidate·no_bridge·앵커/연결/되묻기 글자쌍·판정 LLM 을 부르지 않음(코드·실행 두 방식으로 검사). 옛 동작은 옛 앱용으로 유지, `followup_get` 은 되묻기·불만 뒤 새 질문도 복원. 화면: 요청 3번(분류·저장·다음 질문) → 1번. 검사(가짜 AI 기준) = 새 `qa/conversation-v16.test.mjs` 25개 포함 **509개 중 504 통과 / 0 실패 / 미확정 5(전과 같은 목록)**, type-check·lint·build·서버 엄격 타입 2개·audit 0. **배포 후보**: 서버 `6eaaaff6…`(224,106바이트) · 앱 44차 ZIP `62daa374…`(149개, 전달 안 함). 되돌리기 = `rollback/doit-understanding.v26.ts`(`fd2e1eee…`) · Netlify `6ab50bb9…`. 실AI 확인 불가(키 없음) — **실AI PASS 아님, Conversation P0 = FAIL 유지.** 대표 결정 필요: 대화 시작 안내의 다섯 칸 목록(질문이 더 이상 칸 순서를 따르지 않음).
- **Relationship Agent v1 + 홈페이지(2026-09-24, 근거 = 대표 「RELATIONSHIP AGENT OS + PRODUCT EXPERIENCE · FINAL LOCK」 + 「기존디자인 유지하고 진행해」). 로컬만 — 운영 배포 0, DB·RLS·Auth·Secret·모델·결제·KEY 변경 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260924-agent-v1/AGENT_V1_보고_20260924.md`(캡처 `screens/`). 서버: v16 `turn` 위에 모델 어댑터(`generateRelationshipTurn` · 운영 기본 OpenAI · Gemini/Claude 는 자리만) · HOT MEMORY 최근 3턴 · 구조화 출력 answer_to_user(ANSWER FIRST)·memory_candidates(저장 안 함, 원문 인용 맞는 수만 관측)·confidence(관측만). 화면: 질문을 못 만든 뒤 자동 재요청 제거(LLM 중복 금지 · 「다음 질문 받기」는 남음). **홈페이지: 히어로 글자만 교체**(「찾아다니는 소개팅이 아니라, 알아갈수록 찾아오는 인연.」 · 부문구 · 버튼 「ECHO와 시작하기」, 자리·클래스·그림 그대로), 두 번째 구역 「당신이 계속 찾아다니지 않아도 돼요.」 5단계(3·4·5 준비 중), Trust 4줄, Just Try 한 문장 — 기존 카드 스타일·CSS 변경 0. **히어로 문구의 「소개팅」은 화면 금지어 규칙(2026-09-21)과 충돌 — 대표 최신 지시대로 넣고 검사는 이 부정문 한 줄만 예외, 대표 확인 필요.** 검사(가짜 AI 기준) = **516개 중 511 통과 / 0 실패 / 미확정 5(같은 목록)**, type-check·lint·build 2개·서버 엄격 타입 2개·audit 0, 브라우저 홈페이지 4폭 넘침 0·오류 0, 앱 화면 18/18. **후보(전달 안 함)**: 서버 `113e3738…`(227,650바이트) · 앱 ZIP `a8a74422…` · 홈페이지 ZIP `34759882…` (앞선 v16 후보 대체). 실AI·지연 측정 전 — **Conversation P0 = FAIL 유지.**
- **홈페이지 최종 + Agent v1 배포 후보(2026-09-24, 근거 = 대표 최종 승인 PDF 「ECHO AGENT v1 / HOMEPAGE FINAL LOCK」 — Hero 1안 채택). 로컬만 — 운영 배포 0, DB·RLS·Auth·Secret·모델·결제·KEY 변경 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260924-homepage-final/HOMEPAGE_FINAL_보고_20260924.md`(캡처 `screens/`, 검사 스크립트 `qa/`). **히어로 확정 문구**: 「좋아하는 사람보다, / 편해지는 사람은 다를 수 있으니까.」 · 서브 「ECHO는 당신의 말을 조금씩 기억하며, / 어떤 관계가 자연스러운지 알아갑니다.」 · 버튼 「ECHO 시작하기」(글자만, 디자인 그대로) — 옛 「소개팅」 히어로와 금지어 예외는 삭제(금지어 규칙 충돌 해소). 두 번째 구역 「좋은 인연은 많이 보는 것보다, 조금 더 알아가는 데서 시작되니까.」 + 세 줄(셋째 「때가 되면, 한 사람을 보여드립니다.」만 「준비 중」) · Trust 「당신을 함부로 정의하지 않습니다.」 · Just Try(문장만) — 기능 카드 대신 문장(Quiet Luxury, `.doit-editorial` 안 CSS 4줄). 이야기 09 옛 문구 → 「지금은 당신의 이야기를 듣는 데서 시작합니다. / 다음 단계는 ECHO가 준비하고 있습니다.」(운영 읽기: 전화 인증 0·연결 0 → PDF 허용 문장도 앞서가서 더 보수적으로). 히어로·09 문구는 앱 첫 화면에도 같이 바뀐다. 서버 파일은 수정 0(`113e3738…` 그대로). 검사(가짜 기준) = 517개 중 512 통과/0 실패/미확정 5, tsc·lint·빌드 2·서버 엄격 타입 2·audit 0, 홈페이지 브라우저 360·390·430·1440 **45/45**(외부 CDN 은 이 환경 차단이라 빈 CSS 로 채움), 앱 18/18. **후보**: 서버 `113e3738…` · 앱 ZIP `a8d4e790…` · 홈페이지 ZIP `061f7397…`(앞선 후보 대체). 배포 순서 = 서버 → 앱 → 홈페이지. 실AI·지연·실기기 확인 전 — **Conversation P0 = FAIL 유지, "완료" 아님.**
- **Agent v1 서버 배포 승인(2026-09-24, 대표 「Agent v1 서버 배포 승인한다」) → 배포 못 함(운영 = 버전 26 / v15.2 그대로).** 관리 API 토큰(`SUPABASE_ACCESS_TOKEN`)이 401(만료·폐기 추정). 227KB 파일을 MCP 로 옮겨 적는 방법은 과거 오류 2회 + 이번에 조각 순서를 헷갈려 쓰지 않았다. MCP 로 운영 파일 내려받기 → SHA 대조 경로는 동작 확인(fd2e1eee). 풀 방법 = 대표가 Supabase 대시보드 Edge Functions → doit-understanding 편집기에 GitHub 후보 파일(「Copy raw file」)을 붙여 넣고 Deploy updates → 내가 SHA `113e3738…` 대조. 기록 `docs/claude-final-review-20260916/PATCH-20260924-homepage-final/DEPLOY_AgentV1_차단기록_20260924.md`. 앱·홈페이지 ZIP 은 서버 PASS 뒤 전달(대표 지시).
- **Agent v1 서버 운영 대조(2026-09-24 22:46 KST 무렵, 대표가 Supabase 대시보드 편집기로 직접 배포).** doit-understanding **버전 27** / ACTIVE / verify_jwt=true / 무인증·가짜 토큰·anon 모두 401(anon 은 새 코드가 직접 UNAUTHORIZED) / booted 24ms / 오류 0 / 다른 함수 9개 불변. **SHA-256 불일치**: 운영 `1aab6423…`(227,649바이트) ≠ 후보 `113e3738…`(227,650바이트) — 차이는 파일 끝 줄바꿈 1바이트뿐(cmp, 편집기가 지운 것으로 보임). 대표 규칙 「하나라도 불일치하면 PASS 금지·앱/홈페이지 중단」 → **SERVER DEPLOY = FAIL, ZIP 전달 보류**. 기록 `docs/claude-final-review-20260916/PATCH-20260924-homepage-final/DEPLOY_AgentV1_운영대조_20260924.md`.
- **Agent v1 서버 PASS(대표 승인 2026-09-24 「운영 지문 1aab6423… 승인한다」)**: 승인 지문 = 운영 버전 27 `1aab64236bb7c9a41aa40459f0c320abca296e1c928fd76007de2d26b790c450`(후보 `113e3738…` 와 끝 줄바꿈 1바이트만 다름). 서버 재수정·재배포 금지. 앱 ZIP `a8d4e790…`(44차) · 홈페이지 ZIP `061f7397…` 대표에게 전달(재확인: 지문 일치·소스맵 0·.env 0). 다음 = 대표 Netlify 업로드 → iPhone·Galaxy 각각 LEVEL 3 실제 AI + 운영 속도 p50/p75/p95 → 그 전까지 **Conversation P0 = FAIL 유지**.
- **LEVEL 3 FAIL #3 원인추적(2026-09-24, READ ONLY — 코드·프롬프트·DB·배포 변경 0). 대표 Galaxy 22:58~23:00 KST.** 보고 `docs/claude-final-review-20260916/PATCH-20260924-level3-fail3/LEVEL3_FAIL3_ROOT_CAUSE_20260924.md`. 확정: 전부 신규 `turn` 경로(legacy 호출 0) · 앱 44차 운영 반영(v16 화면 보임) · 「처음부터」 회차 정상(칸 1부터). 「활동」 = 고정문장 NO, LLM 자유 생성(강한 가설: 입력 `remaining` 남은 칸 수 + 목적 「친구」 → 칸 채우기 질문). 「취미생활?」 = 서버 규칙 `/^[^\s?]{1,6}\?+$/`(index.ts:182·conversationRules.ts:39)이 meta 로 강제 → 엉뚱한 되물음. 「너가 어떤 취미가 있냐고 물어봐야…」 = ask 분류 → 같은 질문 유지(complaint 정의에 질문 방향 제안 없음, 잘못된 질문 방향 차단 장치 없음). **답이 아닌 말은 다음 턴 HOT MEMORY 에 0.** 「싸이클 테니스 골프」 = 저장 후 `failed:repeat ×2`(LLM 2회 2166·1898ms) — 강한 가설: 첫 질문 틀 되풀이. 옛 회차 확인 13·후보 1이 새 회차 맥락에 그대로 들어감(회차 필터 없음). 속도(실제 서버·표본 4): total 2372·3578·2175·4480ms, 턴당 LLM 1·1·1·2 — p50/p75/p95 산출 안 함. 최소 수정안 6개(미구현, 대표 승인 대기). **Conversation P0 = FAIL.**
- **Agent v1.1 — LEVEL 3 FAIL #3 원인 수정(2026-09-24, 근거 = 대표 「v1.1 · ROOT CAUSE FIX · 코드수정 승인」). 로컬만 — 운영 배포 0(doit-understanding 운영 = 버전 27 `1aab6423…`), Netlify 0, DB·RLS·Auth·Secret·모델·결제·KEY 변경 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260924-agent-v1.1/AGENT_V1.1_FAIL3_FIX_REPORT_20260924.md`. 바꾼 것: ① 한 턴 경로에서 규칙의 meta(짧은 물음표)·ask 판정을 강제하지 않음(LLM 이 뜻으로 · 지친 말·정정·불만·모르겠어요·저장 금지는 서버 그대로 · RULES 블록 불변) ② 앱이 최근 대화 3줄(저장 안 한 말 포함, `recent`)을 보내고 서버가 `hot_context` 로 씀(DB 저장 0 · 없으면 저장된 답으로) ③ 문제제기 = REPAIR FIRST 5단계 + 질문 방향 거절 `rejected_directions`(기존 followup_skip JSON 에 `rejected_direction` · 새 칸 0) + 검사 `rejected_direction` ④ LLM 입력에서 remaining·answered_count 삭제 ⑤ 기억 3층(최근 대화 · 이번 회차 확인 + 지난 회차는 같은 낱말을 다시 꺼냈을 때만[휴리스틱] · 정정/거절 전부) ⑥ 프롬프트에서 「활동」 부정 예시 삭제, 정보 수집 = 마지막. 검사(가짜 AI 기준) = **525개 중 520 통과 / 0 실패 / 미확정 5(같은 목록)**, FAIL #3 흐름 재현 검사 PASS, 역검사 7/7, type-check·lint·빌드 2·서버 엄격 타입 2·audit 0, 앱 화면 18/18. **후보**: 서버 `c8c0cf85…`(236,895바이트) · 앱 45차 ZIP `725cc5a6…`(홈페이지·doit-connect 배포 대상 아님). 실AI 확인 0 — **Conversation P0 = FAIL 유지, 전략본부 검수 대기.**
- **대화 화면 파스텔 배경 + 목적 카드 유리(2026-09-24~25, 근거 = 대표 「CONVERSATION BACKGROUND ONLY · FINAL LOCK」 → 「배경색 이 색이야 그대로써」 → 선택 「D. 색 그대로 + 글자 남색」 → 「CARD TRANSPARENCY PATCH」). 로컬만 — 운영 배포 0, 서버·DB·Prompt·Agent Logic·음악 변경 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260924-conversation-pastel/PASTEL_배경_유리카드_보고_20260925.md`. **범위 정정으로 되돌린 것**: 앞선 B-style 이식(Pretendard·흰 버튼·입력창·진행 표시·등장 움직임)과 Signature Sound(소리 제어기·버튼·시작 버튼 준비)는 커밋 전 전부 되돌림(작업 폴더 = 저장소 글자 단위 동일 확인, 음악 작업은 작업 공간에 보관 — 커밋 0). 바꾼 것: 대화 경로 루트에만 `.echo-dialogue--pastel`(시작 흐름·가입 동의·홈·홈페이지·전역 0) → ① 배경 = 대표 캡처에서 잰 색 그라데이션(그 화면의 파스텔은 CSS 가 아니라 바깥 Readdy 사진 색 — 사진·바깥 주소 0) + 옛 PastelBlobs 6개 그대로 + 옛 float-bg 움직임(움직임 줄이기면 정지), 덮개 0 ② 캡처 색 그대로면 흰 질문 대비 1.1~1.4:1 → 대표 선택 D: 배경 위 글자만 `#0c1526`(옛 B 버튼 글자색), 카드·버튼·입력창 안 글자·글꼴·크기 0 ③ 목적 카드 4개만 `rgb(23 26 32/.65)`·선택 `rgb(34 38 46/.65)` + blur 10px(B 카드 값) + 테두리 white/14(B 칩 값), 설명 글자 `#9ba2ae`→`#dbe0e7`(.65 는 360/390/430·선택 전후 대비 4.5:1 을 넘는 가장 옅은 값, 실측). 검사(가짜 서버 기준) = 530개 중 525 통과/0 실패/미확정 5(같은 목록), 새 검사 5개 + 역검사 7/7, type-check·lint·build 2·audit 0, 배경 위 글자 196개 대비 미달 0(최저 5.07), 카드 최저 4.57, 기능 회귀 클릭 전 60/60·후 60/60, 히어로 픽셀 차이 0(4폭). **내 실수**: 측정 도구 2건(숨김 규칙 약함 · `//` 주석이 조건을 꺼 버림)·새 검사 1건(주석 낱말을 셈) — 모두 고쳐 다시 잼. **앱 ZIP 후보 `7de59573…`(149개, 45차 후보 대체), 홈페이지 ZIP 불필요.** 한계: 전체 페이지 캡처는 고정 배경을 첫 화면에만 그림(도구 특성, 멈춘 스크롤 4곳 검은 점 0), 스크롤 중 한 장에서 위 6px 검은 줄 1회(실기기 확인 필요), 진행 막대 메탈 선은 밝은 배경에서 옅음(숫자는 읽힘). **실기기 미확인 — Conversation P0 = FAIL 유지.**
- **대화 화면 흰 글자 + 하나의 어두운 유리(2026-09-25, 근거 = 대표 「PASTEL GLASS UI FINAL VISUAL CORRECTION LOCK」 + 「WHITE TYPOGRAPHY PATCH」). 로컬만 — 운영 배포 0, 파스텔 배경·배치·글꼴·서버·Agent·음악·DB 변경 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260925-pastel-glass-white/PASTEL_GLASS_WHITE_보고_20260925.md`. 남색 글자(D안) 삭제. 제각각이던 어두운 판(입력창 #101216 · 하단 카드 #171a20 · 처음부터 #1b1f26 · 안내 카드 #14171c · 목적 카드)을 토큰 하나로: `--conversation-glass-bg: rgb(23 26 32/.65)` · 테두리 white/14 · 흐림 10px(.65 = 유리 안 글자 302개가 4.5:1 넘는 가장 옅은 값, .60 은 145곳 미달). 글자: 1순위 #fff · 2순위 유리 안 #dbe0e7 · 파스텔 위 #f2f3f5 · 파스텔 위 글자만 그림자(부드러운 3겹). **정직한 한계: 밝은 파스텔 위 흰 글자는 표준 대비 FAIL**(질문 1.29~1.69:1, 글자 옆 1~2px 그림자 포함 1.96~2.69:1 — 판·덮개 없이는 4.5 불가, 대표 눈 확인 필요). 검사(가짜 서버 기준) = 530개 중 525 통과/0 실패/미확정 5, 새 검사 5 + 역검사 6/6, 기능 회귀 60/60, 히어로 픽셀 차이 0, 캡처 40장 넘침 0. **내 실수**: 측정 도구 4건·검사 코드 1건, 역검사 중단으로 CSS 에 가짜 결함 1줄이 남았던 것 — 모두 찾아 고치고 원본 대조. 앱 ZIP 후보 `3c75eb1a…`(149개, `7de59573…` 대체). **실기기 미확인 — Conversation P0 = FAIL 유지.**
- **대화 화면 버튼·입력창 투명(2026-09-25, 근거 = 대표 「TRANSPARENT BUTTON / SURFACE PATCH · FINAL」). 로컬만 — 운영 배포 0, 글씨·파스텔·배치·Agent·Sound 변경 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260925-transparent-surface/TRANSPARENT_SURFACE_보고_20260925.md`. 어두운 유리(rgb(23 26 32/.65)+흐림 10px)를 없애고 입력창·하단 카드·처음부터·목적 카드·안내/확인 카드·보내기 버튼 바탕 = transparent · 흐림 없음 · 테두리 #d9dfe8a6(원래 처음부터 테두리) · 선택 카드는 흰 테두리+안쪽 1px. 글씨 계산값 전후 차이 0(18개 선택자). 메탈 실버 주요 버튼은 밝은 브랜드 버튼이라 그대로(대표 결정). **정직한 결과: 판 안 글씨 115개 모두 대비 미달(1.0~1.49, 기준 4.5) — 흰·연회색 글씨가 밝은 파스텔 위에 그림자 없이 놓임. 권장 = 파스텔 글자와 같은 그림자를 버튼 글씨에도(글씨 변경이라 대표 승인 필요).** 검사(가짜 서버 기준) = 530개 중 525 통과/0 실패/미확정 5, 새 검사 5 + 역검사 5/5, 기능 회귀 60/60, 히어로 픽셀 차이 0, 캡처 40장 넘침 0. 앱 ZIP 후보 `7c9ad608…`(149개, `3c75eb1a…` 대체). **실기기 미확인 — Conversation P0 = FAIL 유지.**
- **투명 판 글씨 가독성 비교 → 목표 동시 충족 불가 · STOP(2026-09-25, 근거 = 대표 「TRANSPARENT SURFACE · TEXT LEGIBILITY FINAL PATCH」). 제품 코드 변경 0 · 운영 배포 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260925-text-legibility-stop/TEXT_LEGIBILITY_STOP_보고_20260925.md`. 판 안 흰 글씨에 얇은 테두리(paint-order stroke fill)+최소 그림자 5단계(0.3~1.5px)를 검사용으로만 덧씌워 배율 3배·360/390/430·5상태·82곳 실측(가짜 서버 기준): 표준(WCAG, 글자↔파스텔) 대비는 모든 단계 1.00~1.43 그대로(민트 최저 1.03 · 노랑 1.00 · 코랄 표본 0 = 확인 불가). 글자 둘레 1기기픽셀 기준 4.5 는 K4(1px #000 + 2px 그림자)부터 대부분 넘지만 원래 해상도 캡처로 자막처럼 두꺼움(지시 5번 FAIL), K3 이하는 전부 미달. → 흰 글씨 + 투명 판 + 4.5:1 동시 충족 불가. 검정 판·테두리 제품 반영 0, 앱 ZIP `7c9ad608…` 운영 후보 확정 안 함. 대표 결정 대기((가) 옅은 바탕 (나) 판 안 글씨 어둡게 (다) K4 테두리 감수 (라) 그대로). **Conversation P0 = FAIL 유지.**
- **대화 화면 글씨색·배경색 = 대표 이미지 그대로(2026-09-25, 근거 = 대표 「비교하지 말고 글씨색과 배경색 내가 올린 이미지 그대로 반영해 통일시켜 … 질문 구간부터 … 글씨색이랑 배경만」). 로컬만 — 운영 배포 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260925-image1-color/IMAGE1_COLOR_보고_20260925.md`. 배경 = 이미지(옛 홈 「오늘 내 마음의 날씨는 어때?」)를 5×11 칸으로 재어 줄마다 가로 그라데이션 + 40px 흐림. **내 실수**: 처음엔 이미지의 색 설명(ICC)을 무시하고 날값을 읽어 흐린 색을 옮겼다 → Chrome 색 변환 기준으로 다시 잼(예 #6ce2ad → #43e0a9). 글씨 = HeroSection.tsx 코드 값 그대로(흰색 · 흰색 90/70/55%), 검은 그림자 삭제. 화면에 보이는 글씨는 세 가지 흰색뿐(예외: 밝은 은색 주요 버튼 안 어두운 글씨). 글꼴·배치·버튼 모양·히어로 변경 0. 검사(가짜 서버 기준) = 530개 중 525 통과/0 실패/미확정 5, 회귀 60/60, 히어로 픽셀 차이 0. 앱 ZIP 후보 `67ca0997…`(`7c9ad608…` 대체). **실기기 미확인 — Conversation P0 = FAIL 유지.**
- **대화 화면 글씨 = 흰색 하나로 통일(2026-09-25, 대표 「비교해도 안 맞아 그냥 이미지 글씨색 똑같이」).** 이미지 속 글씨 실측(색 변환 적용): 제목·「시작하기」 `#ffffff` → 대화 화면 글씨 전부 `#fff`(흰색 90/70/55% 구분 삭제, 검은 그림자 없음 유지). 예외: 입력창 흐린 안내 글씨 white/.7 · 은색 주요 버튼 안 어두운 글씨. 검사(가짜 서버 기준) = 530개 중 525 통과/0 실패/미확정 5, 회귀 60/60, 히어로 픽셀 차이 0. 앱 ZIP 후보 `42df9c26…`(`67ca0997…` 대체). 로컬만 — 운영 배포 0. **실기기 미확인.**
- **운영 전체 연결 실측(2026-09-25, READ-ONLY — 코드·DB·배포 0).** 보고 `docs/claude-final-review-20260916/PATCH-20260925-production-audit/운영전체연결_실측_20260925.md`. 대표 iPhone 의 검정 「다음에 와도, 처음부터 쓰지 않도록.」 = start-journey 의 `consent` 단계(로그인 안 됨이면 의도적으로 뜨는 계정 안내 → /login). iPhone 세션 마지막 9/20 → 로그아웃 상태로 판정. 로그인하면 목적 없음 → 파스텔 대화, 목적 있음 → 검정 「무엇부터 할까요?」 → 파스텔 대화. 운영 앱 6ab576b1(= 42df9c26) 배포 뒤 04:20~04:23 KST 대화 기록 있음(로그인 사용자 도달). 전화 인증: 코드·연결 있음, 운영 Auth Phone 공급자 꺼짐(external.phone=false), 인증 0명, 앱 쪽 우회 권한 0 → 운영 사용 불가. 음악: 운영엔 랜딩 09 구간 수동 재생 카드(원음 1/2, 성별 매핑 없음, 바깥 주소)뿐, Signature Sound Part A/B 는 보관만(배포 0). **내 실수**: 운영 앱 42df9c26 에 Agent v1.1 화면 쪽 `recent` 전송이 섞여 있다(파스텔을 v1.1 작업본 위에서 만듦, 전달 때 말하지 않음) — 운영 서버 v27 은 읽지 않아 동작 영향 0. P0(대표 결정): ① 로그인 전 검정 계정 안내 화면 처리 ② 전화 인증 공급자 or 자격 조건 ③ AI 품질(기존).
- **GitHub push STOP · A/B Spike 대기(2026-09-25, 대표 지시).** 로컬 기록 `4c5ac2a`(운영 전체 연결 실측)·`9722763`(갤럭시 실측)은 이 세션에서 push 403 이다. 대표가 GitHub 를 다시 연결한 뒤에도 403 이었다. 두 기록은 보존하고 push 는 멈춘다. 소문자 주소로 우회하는 push 는 금지. **제품 코드·운영 영향 0**(문서 기록뿐). CTO A/B Spike PDF 는 이 환경에서 본문 0자로 읽혀 내용을 추정하지 않는다. 대표가 보내는 텍스트 기준본을 받은 뒤에 시작하고, 그 전에는 Current Agent A(운영 doit-understanding v27) 수정·배포 0.
- **CTO A/B Spike 1차(2026-09-25, 대표 텍스트 기준본 「RELATIONSHIP AGENT A/B SPIKE · FINAL EXECUTION LOCK」). 운영 변경 0 · 제품 코드 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260925-ab-spike/CTO_AB_SPIKE_보고_20260925.md`. A = 운영 doit-understanding v27(`1aab6423…`, 동결). B = `product/spike/ab-20260925/agentB.mjs`(진단용 · 입력 11항목 · 서버는 저장/정정/거절 의도/이미 물은 의도(LLM question_intent 정규화 같음)·5 유효 답만 결정 · 글자쌍 휴리스틱 0). 운영 로그(실제 AI, Galaxy 11턴): 질문 실패 2 = `failed:clue`(단서 글자 인용 검사) · `failed:self_flag`(자기 표시 즉시 탈락) · 같은 뜻 질문 4개가 글자쌍 반복 검사(sim .6/ov .7)를 모두 통과(최대 .39/.59) · 저장 안 한 문제제기는 다음 턴 LLM 입력에 0 · 「취미생활?」 정규식 meta 강제 운영 잔존 · 인프라 장애 증거 0(LLM 중앙 2.1초, 턴 중앙 3.0초, n=11). [MOCK] 입력 토큰 A 26,842 vs B 15,510(−42%, 차이 대부분 system 1,492 vs 640), B LLM 턴 +2(규칙 분류 없음). **B 실AI = 확인 불가(OpenAI 접속 가능·키 없음, Secret 추가는 대표 승인)** → B WIN 확인 불가 · 기반 스택 교체 불필요 · Conversation P0 = FAIL 유지. **내 실수**: 첫 MOCK 이 A 에 불리(같은 문장 틀·다른 단서)해 가짜 실패 8건 → 폐기·중립 MOCK 로 재측정.
- **CTO B안 PHASE 1(2026-09-25, 대표 「B안 PHASE 1 실AI 검증 · 즉시 실행」). 운영 변경 0 · 제품 src 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260925-ab-spike/PHASE1_보고_20260925.md` + `GOLDEN_FAILURE_LIBRARY_v1_20260925.md`(9건: 칸 = ID·상황·유형·원인 Layer·A·B·재현·해결 후보·검증 상태) + `PHASE1_MOCK_하네스결과_20260925.md`. B = Prompt **B-1.0**(user_signal·reaction·understanding·curiosity·question_intent·next_question 없어도 됨 · 서버는 저장/정정/거절 의도/이미 답한 의도/안전/상태만 · 막힌 질문은 버리고 반응만 · 문자열 품질 휴리스틱 0, 의도 비교는 [HEURISTIC / EXPERIMENT ONLY] 미탐 가능). 하네스: ACTUAL/SYNTHETIC 표시(FLOW1 7·FLOW2 2·FLOW3 4·FLOW4 8), 객관 FAIL(오류·문제제기 저장·글자 같은 질문·물음표 2개·금지어), usage 토큰·지연 p50/p95(REAL 때만), 블라인드 `USER / OUTPUT X / OUTPUT Y` + 「X가 낫다/Y가 낫다/둘 다 별로다」(열쇠 별도, Claude 는 승자 안 고름). **실AI A/B = 0회 · 확인 불가(OPENAI_API_KEY 없음)** → B WIN 확인 불가 · Model Comparison NO(근거 없음) · 기술스택 교체 NO · Conversation P0 = FAIL 유지. [MOCK] 21턴: 입력 토큰 A 34,061 vs B 20,611, 객관 FAIL 0 대 0(구조 확인용). 코드 재현(AI 무관 규칙 단계): A 가 「취미생활?」을 meta(rule)·비저장(GF-05). 검사 B [MOCK] 14/14, 일부러 망가뜨리기 7/7. **내 실수 2건(실행 전 발견·수정)**: B-1.0 초안에서 막힌 질문이 다음 시도 형식 실패 때 새어 나갈 수 있었음 · 블라인드표에서 A 되묻기 칸만 「같은 질문 유지」 표시가 없어 A/B 가 드러날 수 있었음.
- **B안 PHASE 1 자율 실행 최종(2026-09-25, 대표 「B안 자율 실행 범위 FINAL」). 운영 변경 0 · 제품 src 0.** 보고 `docs/claude-final-review-20260916/PATCH-20260925-ab-spike/PHASE1_FINAL_보고_20260925.md`. **REAL_AI = BLOCKED_BY_ENVIRONMENT**: 환경 변수에 OPENAI_API_KEY 없음 + 대표가 채팅으로 준 테스트 키는 환경 보안 장치가 실행 차단(우회 안 함, 키는 어디에도 저장 안 함 — 대화 기록에 남아 폐기 필요). 새 회사 자산 `docs/failure-intelligence/`: Failure Library 21건(ACTUAL 16·코드 3·SYNTHETIC 포함 2 / UNRESOLVED 12·MITIGATED 7·RESOLVED 2·**VERIFIED 0**) · Taxonomy(Layer 6·Type 17·lifecycle) · Failed Solutions 13 · Verified Engines(후보 8) · Model Capability Registry · Replay 결과. 스파이크: `golden-failures.json`(26 입력·ACTUAL 17, FLOW5 추가) · `FROZEN_INPUTS.json`(A `1aab6423…`·B-1.0 `a0031fcc…`·입력 `e070dff8…` 사전 고정 — 결과 본 뒤 B 수정 시 검사 실패) · `run-ab.mjs --require-real`(키 없으면 가짜로 안 돌고 BLOCKED) · `replay-decisions.mjs`(운영 실제 문장 → A v27 함수·B 서버 결정, AI 호출 0) · `score-blind.mjs`(대표 선택만 집계). [REPLAY] GF-01 A 코드 재현(같은 뜻 질문 4개 모두 통과, 최대 sim .39/ov .59) · B-1.0 의도 비교는 같은 이름 3/3 차단·다른 이름 0/3(미탐, 실AI 로만 판정) · A 규칙이 「취미생활?」 meta 강제. 검사: B [MOCK] 14/14, 회귀 5/5, 역검사 7/7(**내 실수**: 처음 6/7 — 집계 검사 데이터가 대칭이라 A/B 뒤집기를 못 잡음 → 비대칭으로 고침). [MOCK] 26턴 입력 토큰 A 42,997 vs B 25,310. B WIN = 확인 불가 · Model Comparison NO · 기술스택 교체 NO · Conversation P0 = FAIL 유지. 「정정무시」·「미확정 사실화」는 날짜·원문 있는 실제 사례를 저장소에서 못 찾아 ACTUAL 로 안 넣음.
- **ECHO AI R&D 자율 실행 헌장 · 작업 묶음 01(2026-09-25, 대표 「ECHO AI R&D AUTONOMOUS EXECUTION CHARTER · FINAL」 — C항 포괄 승인, N항 HARD STOP 만 승인 요청, 중간보고 금지). 운영 변경 0 · 제품 src 0.** 보고 `docs/failure-intelligence/RD_BATCH01_보고_20260925.md`. Failure Intelligence OS: 정본 = `docs/failure-intelligence/data/*.json`(failures 53 · failed-solutions 21 · failure-graph 23 · families 15 · types 25), `.md` 3종은 `product/spike/failure-intelligence/fi-build.mjs` 로 생성(손수정은 `--check`·검사가 잡음). 실패 21 → 53(과거 기록 전수조사 25 후보를 원문·줄 번호로 대조): 출처 ACTUAL 30 · 재구성 1 · REAL_AI_SCRIPTED 8(운영 서버+실제 AI+스크립트 입력) · CODE 8 · SYNTHETIC 계열 3 · FOUNDER_STATEMENT 3 / 상태 UNRESOLVED 24 · MITIGATED 26 · RESOLVED 3 · **VERIFIED·REAL_AI_VERIFIED·USER_VERIFIED 0**(데이터 검사가 근거 없는 승격을 막음). 칸 추가: Family·사용자 피해(감정·정신·시간·물질, 근거 없으면 UNKNOWN)·원인 확신·방어 수준·Golden·git 커밋 근거. Failure Compiler 프로토타입(CANDIDATE, 결과 항상 HUMAN_APPROVAL_REQUIRED, 지어낸 인용·체계 밖 값 걸러 기록). Golden v1.2 = 34 입력·ACTUAL 24(FLOW6 ANSWER FIRST · FLOW7 대표 실제 입력 추가, 실AI 결과 전 변경이라 FROZEN_INPUTS 이력에 이유 기록 · A·B 코드 0). [REPLAY] 새 발견: 운영 v27 규칙이 대표 실제 질문 「ai가 오타기 날수도 있어?」를 meta 로 강제(GF-53) · 정상 사례 역검사 R4: 분명한 답 26 중 1(「취미생활?」) 강제 · 애매한 짧은 물음표 8/8 강제 · B-1.0 의도 장부는 옛 흐름 실패 해결책 FS-17(의도 태그 소진)과 같은 모양(막다른 길은 없음, 실AI 확인 필요). Graph 반복 모양: 실패 → 대책 → 반대 방향 실패(GF-10→FS-08→GF-08 · GF-18→FS-14→GF-11 · GF-36→FS-20→GF-02 재등장). 검사 28/28(가짜 AI·AI 호출 0), 역검사 FI 8/8. **내 실수**: 앞선 보고의 「정정무시·미확정 사실화 실제 사례 없음」은 틀렸다(GF-25·GF-30 발견) · 「할말이없다 휴」를 SYNTHETIC 으로 잘못 표시(실제 대표 입력). REAL_AI = BLOCKED_BY_ENVIRONMENT 유지(키 재발급·폐기 반복 요구 금지 — 헌장 L). Conversation P0 = FAIL 유지.
- **저장·기억·실패 자산화 MASTER 반영 · R&D 작업 묶음 02(2026-09-25, 대표 「저장·기억·실패 자산화 MASTER LOCK」 승인). 운영 변경 0 · 제품 src 0 · main 병합 0.** 보고 `docs/failure-intelligence/RD_BATCH02_보고_20260925.md`, 기준 `docs/failure-intelligence/STORAGE_MASTER_20260925.md`. **행동 상태 장부** `ACTION_LEDGER.md` ← `data/action-ledger.json`(COMPLETED 3 · DECIDED 7 · BLOCKED 5 · PENDING 6) — 다음 행동 전 반드시 대조, COMPLETED 재요구 = 새 실패. **키 상태 LOCK**: KEY_PROVIDED_BY_FOUNDER=YES · 클라우드 환경 변수=NO · REAL_AI=BLOCKED_BY_ENVIRONMENT · 현재 키 유지 · 폐기·새 발급·Claude Edit 경로 반복 안내 금지. 실패 53 → 62: AI 조언자 실패 F-ADVISOR 7건(GF-54 완료 행동 재요구 · GF-55 막힌 경로 3회 재안내 · GF-56 키 안내 불일치 · GF-58 대표 중계 과부하 · GF-59 틀린 보고 = ACTUAL / GF-57 비용 가시성 · GF-60 상위 대안 지연 = CANDIDATE, 증거 `evidence/ADVISOR_SESSION_20260925.md`) + B 위험 후보 2건(GF-61·62 HYPOTHESIS). 칸 추가: 증거 수준·Mock 결과·사용자 결과·관련 실패, Failed Solutions 21건에 왜 골랐나·어떻게·나아진 것·망가뜨린 정상 사례·다시 쓸 조건. **내 표시 오류 정정**: GF-34·40 출처를 대표 진술로만 적었으나 코드 확인 결함(CODE). Golden 판정 기준 `golden-specs.json` 20 spec(기대·금지·기계 판정 = 저장·오류·반응·실제 고정 문장 7개 대조, 사람 판정 질문, 사전 고정) · 하네스: 사전 고정 불일치면 실AI 실행 안 함(종료 3) · `api_cost_usd`(공식 단가 입력 시만). **실AI 대안 경로 준비**: `.github/workflows/echo-ab-real.yml`(이 브랜치에서 `product/spike/ab-20260925/run-request.json` 변경 push 때만 · GitHub 저장소 Secret `OPENAI_API_KEY_AB_TEST` 에서만 키 · 없으면 BLOCKED · 결과는 첨부 7일 · 커밋 0) — Secret 생성은 HARD STOP, 대표 승인 대기(장부 P-01). 검사 30/30(가짜 AI·AI 호출 0), 역검사 6/6. [MOCK] 1회 규모 호출 A 32·B 34, 입력 토큰 A 56,817·B 32,631. Conversation P0 = FAIL 유지.
- **실AI A/B run1(2026-09-25, 대표 「대표 승인 · 실AI A/B 실행 시작」 — 대표가 GitHub 저장소 Secret `OPENAI_API_KEY_AB_TEST` 등록). 운영 변경 0 · 제품 src 0 · main 병합 0.** 분석 `docs/failure-intelligence/REAL_AB_RUN1_분석_20260925.md`, 원자료 `docs/failure-intelligence/evidence/REAL_AB_RUN1_20260925/result.md`(Actions 로그 그대로 · A/B 이름 보임), 블라인드 검수표 `…/BLIND_검수표_run1.md`(열쇠는 base64 로 봉함). 실행 = Actions run 36103690087(커밋 `23a4484`) · 사전 고정 일치(A `1aab6423…`·B `a0031fcc…`·입력 `3100d5d4…`·판정 `380d6fb3…`) · 요청 모델 gpt-4o-mini(세부판 미기록) · 34 입력 · 호출 A 36·B 37 · 입력 토큰(usage) A 63,921·B 35,390 · 출력 A 4,242·B 3,553 · 턴 p50/p95/최대 A 1,294/2,942/3,528ms · B 1,306/5,536/6,938ms(미국 실행기) · 비용 확인 불가(공식 단가 미입력·가격 페이지 접속 차단). **내 실수(GF-63)**: 하네스 runA 의 error 칸이 줄 주석 안에 있어 A 의 질문 실패가 한 번도 기록되지 않았다 → 원자료는 두고 v27 코드 경로로 보정(A 질문 실패 6턴), 하네스 수정 + 역검사. Golden 기계 판정 26칸: A 16 PASS/10 FAIL(보정) · B 20/6. 관측: 둘 다 항의를 답으로 저장해 대화 조기 종료(GF-67) · 둘 다 「활동」 점프(B 는 주제 입력이 없는데도 → GF-08 에 Model 몫 HYPOTHESIS) · A 는 정정·거절을 되묻기로 분류해 같은 질문 3번 + 고정 안내문(GF-66) · B 는 답·항의를 지친 말로 과다 분류(GF-65), 의도 이름 미탐 2(GF-61 실제 관측), 방금 답한 의도가 같은 턴 검사에 없어 같은 질문(GF-64). 실패 62 → 67, 관계 29. **B WIN = 확인 불가(블라인드 0) · VERIFIED 승격 0 · Conversation P0 = FAIL 유지.** 다음 = 대표 블라인드 검수(장부 P-07).

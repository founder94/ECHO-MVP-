--- a/src/doit/featureFlags.ts
+++ b/src/doit/featureFlags.ts
@@ -17,5 +17,5 @@
 export type AFeatureKey = keyof typeof A_FEATURE_FLAGS;
 
 export function isAFeatureEnabled(key: AFeatureKey): boolean {
-  return A_FEATURE_FLAGS[key] === true;
+  return Boolean(A_FEATURE_FLAGS[key]);
 }
\ No newline at end of file
--- a/src/doit/hooks/useOpenAIConversation.ts
+++ b/src/doit/hooks/useOpenAIConversation.ts
@@ -85,17 +85,7 @@
             note: answer.note,
           };
         })
-        .filter(
-          (
-            item,
-          ): item is {
-            reading: string;
-            reaction: Reaction;
-            status: InfoStatus;
-            semanticKey?: string;
-            note: string;
-          } => Boolean(item),
-        ),
+        .filter((item): item is NonNullable<typeof item> => item !== null),
     [],
   );
 
--- a/src/doit/lib/photoStorage.ts
+++ b/src/doit/lib/photoStorage.ts
@@ -78,7 +78,11 @@
 // PhotoDrafts의 UploadPort를 실제 Storage + profile_photos로 연결하는 adapter.
 // user_id는 인증 세션에서 주입된 값만 사용한다(브라우저 자유 입력 금지).
 export class SupabasePhotoAdapter implements UploadPort {
-  constructor(private readonly userId: string) {}
+  private readonly userId: string;
+
+  constructor(userId: string) {
+    this.userId = userId;
+  }
 
   async upload(input: {
     captureId: string;
@@ -93,13 +97,15 @@
     const path = buildStoragePath(this.userId, input.slot, input.captureId);
 
     // 1) private bucket 업로드
+    //    supabase-js 2.57.4 의 storage upload 옵션에는 signal 이 없다. 취소는 업로드 전·후에 직접 확인한다.
+    if (input.signal.aborted) throw new PhotoStorageError("UPLOAD_FAILED", "aborted");
     const { error: uploadError } = await supabase.storage
       .from(PHOTO_BUCKET)
       .upload(path, input.blob, {
         contentType: "image/jpeg",
         upsert: false,
-        signal: input.signal,
       });
+    if (input.signal.aborted) throw new PhotoStorageError("UPLOAD_FAILED", "aborted");
     if (uploadError) {
       throw new PhotoStorageError("UPLOAD_FAILED", uploadError.message);
     }
--- a/src/components/ErrorBoundary.tsx
+++ b/src/components/ErrorBoundary.tsx
@@ -18,7 +18,6 @@
 
   componentDidCatch(error: Error, info: ErrorInfo) {
     if (import.meta.env.DEV) {
-      // eslint-disable-next-line no-console
       console.error('[ErrorBoundary] 렌더링 오류:', error?.message ?? 'UnknownError', info?.componentStack ?? '');
     }
   }
--- a/src/context/AuthContext.tsx
+++ b/src/context/AuthContext.tsx
@@ -54,13 +54,11 @@
       );
     if (error && import.meta.env.DEV) {
       // 프로필 동기화 실패로 구분(로그인 세션은 유지). 개인정보·토큰 없이 오류 코드만 남긴다.
-      // eslint-disable-next-line no-console
       console.error('[AuthContext] ensureProfile 동기화 실패:', error.code ?? 'UnknownCode');
     }
   } catch (err) {
     // 예상하지 못한 네트워크 예외는 별도로 처리. 개인정보 없이 실패 위치·종류만 남긴다.
     if (import.meta.env.DEV) {
-      // eslint-disable-next-line no-console
       console.error('[AuthContext] ensureProfile 네트워크 오류:', (err as Error)?.name ?? 'UnknownError');
     }
   }
@@ -82,7 +80,6 @@
         if (!mounted) return;
         // 세션 조회 오류를 무조건 "사용자 없음"으로 바꾸지 않는다. 개인정보 없이 오류 코드만 남긴다.
         if (result.error && import.meta.env.DEV) {
-          // eslint-disable-next-line no-console
           console.error('[AuthContext] getSession 실패:', result.error.code ?? result.error.name ?? 'UnknownError');
         }
         const session = result?.data?.session ?? null;
--- a/src/doit/hooks/ServerUnderstandingProvider.tsx
+++ b/src/doit/hooks/ServerUnderstandingProvider.tsx
@@ -1,4 +1,3 @@
-/* eslint-disable react-refresh/only-export-components -- 서버 상태 훅(컴포넌트 아님), A 구조 원본 스타일 유지 */
 import { useCallback, useEffect, useState } from 'react';
 import { useAuth } from '@/doit/hooks/useAuth';
 import {

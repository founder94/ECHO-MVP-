--- a/src/lib/echo/toss.ts
+++ b/src/lib/echo/toss.ts
@@ -31,6 +31,21 @@
   }
 }
 
+// 결제 개통 상태 — 프론트 안전장치(결제 성공·paid·유료 접근은 서버 echo-payment 가 최종 판단).
+// 토스페이먼츠 심사 대기 중에는 'review_pending' 으로 두고 결제 진입(주문 생성·SDK 로드·결제창)을 막는다.
+// 개통 승인 뒤 대표 결정으로만 'enabled' 로 바꾼다. 클라이언트 키가 생겼다는 이유만으로 자동 활성화하지 않는다.
+export type PaymentGate = 'review_pending' | 'enabled';
+export const PAYMENT_GATE: PaymentGate = 'review_pending';
+export const PAYMENT_PENDING_BUTTON_LABEL = '결제 준비 중';
+export const PAYMENT_PENDING_NOTICE = '현재 결제 서비스를 준비하고 있어요.\n결제는 아직 진행되지 않습니다.';
+
+// 결제 진입 허용 여부: 게이트가 'enabled' 이고 유효한 공개 클라이언트 키가 있을 때만 true.
+export function isPaymentEnabled(): boolean {
+  if (PAYMENT_GATE !== 'enabled') return false;
+  const key = getTossClientKey();
+  return key !== '' && isValidClientKey(key);
+}
+
 export function getTossClientKey(): string {
   const key = (import.meta.env.VITE_PUBLIC_TOSS_CLIENT_KEY as string | undefined) ?? '';
   return key.trim();
--- a/src/pages/do-it/payment/page.tsx
+++ b/src/pages/do-it/payment/page.tsx
@@ -5,7 +5,7 @@
 import { useConversationGate } from '@/pages/do-it/components/useConversationGate';
 import { useAuth } from '@/context/AuthContext';
 import { REPORT_PRICE_KRW, UNKNOWN_STATE_MESSAGE, createOrder, resumeConversation, routeWithConversation, type PaymentState } from '@/lib/echo/api';
-import { getTossClientKey, isValidClientKey, isUserCancel, requestCardPayment } from '@/lib/echo/toss';
+import { PAYMENT_PENDING_BUTTON_LABEL, PAYMENT_PENDING_NOTICE, getTossClientKey, isPaymentEnabled, isUserCancel, requestCardPayment } from '@/lib/echo/toss';
 
 const PAYABLE_STATUS = 'white_door_ready';
 const priceLabel = `${REPORT_PRICE_KRW.toLocaleString('ko-KR')}원`;
@@ -24,7 +24,8 @@
   const [payError, setPayError] = useState('');
 
   const clientKey = getTossClientKey();
-  const keyReady = !!clientKey && isValidClientKey(clientKey);
+  // 결제 개통 게이트: 심사 대기(review_pending)면 주문 생성·SDK 로드·결제창 어느 것도 실행하지 않는다.
+  const paymentEnabled = isPaymentEnabled();
   const returnPath = `/payment?c=${encodeURIComponent(conversationId)}`;
 
   const load = async () => {
@@ -57,7 +58,7 @@
   useConversationGate(returnPath, () => void load());
 
   const handlePay = async () => {
-    if (paying || !keyReady) return;
+    if (!paymentEnabled || paying) return;
     setPaying(true);
     setPayError('');
     try {
@@ -116,17 +117,19 @@
         <p className="text-[11.5px] text-white/45 mt-2">단건 결제예요. 자동 결제나 구독은 없어요.</p>
       </div>
 
-      {!keyReady && (
-        <p style={reveal(440)} className="text-[12.5px] text-white/70 mb-6 max-w-xs">
-          토스페이먼츠 심사가 진행 중이에요. 승인 후 결제를 이용할 수 있어요.
+      {!paymentEnabled && (
+        <p style={reveal(440)} className="text-[12.5px] text-white/70 mb-6 max-w-xs whitespace-pre-line">
+          {PAYMENT_PENDING_NOTICE}
         </p>
       )}
 
       {payError && <p className="text-[12.5px] text-red-300 mb-3 max-w-xs">{payError}</p>}
 
       <div style={reveal(520)} className="w-full max-w-xs flex flex-col gap-3">
-        <button type="button" onClick={handlePay} disabled={paying || !keyReady} className={PRIMARY_BUTTON}>
-          {paying ? (
+        <button type="button" onClick={handlePay} disabled={!paymentEnabled || paying} aria-disabled={!paymentEnabled || paying} className={PRIMARY_BUTTON}>
+          {!paymentEnabled ? (
+            PAYMENT_PENDING_BUTTON_LABEL
+          ) : paying ? (
             <>
               <span className="w-4 h-4 border-2 border-[#0c1526]/25 border-t-[#0c1526] rounded-full animate-spin" />
               결제창 여는 중...
--- a/src/pages/do-it/payment/success/page.tsx
+++ b/src/pages/do-it/payment/success/page.tsx
@@ -4,6 +4,7 @@
 import { GHOST_BUTTON, PRIMARY_BUTTON, useReveal } from '@/pages/do-it/components/sceneStyles';
 import { useConversationGate } from '@/pages/do-it/components/useConversationGate';
 import { IN_PROGRESS_RETRY_MS, REPORT_PRICE_KRW, UNKNOWN_STATE_MESSAGE, confirmPayment, routeWithConversation, type PaymentState } from '@/lib/echo/api';
+import { PAYMENT_PENDING_NOTICE, isPaymentEnabled } from '@/lib/echo/toss';
 
 const wait = (ms: number) => new Promise<void>((resolve) => setTimeout(resolve, ms));
 const CONFIRM_RETRY_MAX = 3;
@@ -29,6 +30,13 @@
     setPhase('loading');
     setErrorMessage('');
     setFailed(false);
+    // 결제 개통 게이트: 심사 대기 중에는 성공 URL 에 어떤 값이 붙어 와도 승인 요청을 보내지 않는다(paid 생성 없음).
+    if (!isPaymentEnabled()) {
+      setFailed(true);
+      setPhase('ready');
+      setErrorMessage(PAYMENT_PENDING_NOTICE);
+      return;
+    }
     if (!paymentKey || !orderId || amount !== REPORT_PRICE_KRW) {
       setFailed(true);
       setPhase('ready');

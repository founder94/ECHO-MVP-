# final100v4 실행 중 중단 기록 (2026-09-20)

## 1) 계획된 중단→재개 검사 (30회차 직후)
- 03:56:28 UTC 검사 프로세스(run100v3.mjs) 강제 종료, 03:56:33 감독기 재기동
- 마지막 완료 iteration i=29 (conversation b256330c-b731-47e2-96e8-5d4748337926)
- 재개 위치 i=30, 중복 0 · 누락 0 · 결과 30행 보존
- 상태 파일 launches 1→2 (재개 1회 기록)

## 2) 계획에 없던 중단 — 검사 컨테이너 재시작 (79회차)
- 05:55:22.198 / 05:55:22.213 UTC 두 건의 start 호출이 15ms 간격으로 응답 없이 실패(code=BAD_JSON, http=0)
- 검사기 안전장치(연속 시작 실패 2회) 작동 → STOP 파일 생성 후 자체 정지 (결과 81행에서 멈춤)
- 원인: 검사 컨테이너 재시작. 재개 시점 `uptime` = 4분, PID 1 생성 시각 05:59:12 UTC.
  15ms 간격은 네트워크 왕복이 불가능한 시간으로, 요청이 서버에 도달하기 전에 로컬 프로세스가 종료된 것.
- 서버 상태(재개 직전 실측): get-step-question v48 ACTIVE verify_jwt=on / echo-journey v26,
  ezbr_sha256 시작 기준과 동일, 스모크 401/403/200 통과, 검사 계정 start 1회 200 성공.
- 처리: i=79 · i=80 두 행은 "실행되지 않은 회차"로 evidence-final100v4-notrun-rows.jsonl 에 분리 보존하고
  결과 파일에서 제외한 뒤, 같은 회차(i=79)부터 재개(06:03:48). 성공으로 계산하지 않음.
  (RATE_LIMITED 회차를 같은 회차로 다시 여는 것과 같은 원칙)

## 3) 실행된 회차에서 발생한 BAD_JSON 2건 — 결과에 FAIL 로 보존
- i=61 (G, conversation 49c9ebfd-d243-48d2-bd1e-d533aaa6733d), 04:36:33 경 askback 응답 수신 실패(http=0).
  서버 로그: [gsq] question_ready mode=asked attempts=3 ai_ms=6519 → 질문은 생성됐고 그 요청의 200 응답 기록이 없음.
- i=78 (H, conversation e66318d4-0080-44da-b33c-b9fdbc4cc355), 05:55:22 경 askback 응답 수신 실패(http=0).
  컨테이너 재시작과 같은 시각.
- 분류: 사용자 화면 실패이므로 집계에서 숨기지 않되, 제품 규칙 결함 증거는 없음 → 인프라/응답 끊김 계열.

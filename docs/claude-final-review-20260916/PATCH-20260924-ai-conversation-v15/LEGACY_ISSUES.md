# LEGACY 이슈 목록 (v15 범위 밖 · 별건)

v15 대화(doit-understanding) 작업과 섞지 않는다. 이 목록의 항목은 옛 ECHO STEP 1→7 흐름에 대한 것이다. 이번 작업에서 이 흐름의 코드·결제·운영 함수는 **바꾸지 않았다.**

## LEGACY-01 · [테스트 결함 확인] 깊은 여정 검사가 STEP 7 도달을 확인하지 않음 (2026-09-24 등록)

| 항목 | 내용 |
|---|---|
| 검사 | `qa/stress-messy-inputs.test.mjs` 「여정 끝까지 40회: 중간에 되묻고 정정해도 STEP 7 까지 끊기지 않는다」 |
| 결함 | 이름은 "STEP 7 까지 끊기지 않는다"인데 실제로는 **반말 0건만** 확인한다 |
| 실제 값(가짜 AI 기준) | 40번 중 **오류 30번**(`followup-answer` 단계 `INVALID_STATE`), **STEP 7 도달 0번** — 그래도 통과로 셌다 |
| 언제부터 | 기록이 남은 가장 이른 실행(2026-09-23 23:32)부터 같은 값. v15 때문에 생긴 것이 아님 |
| 대상 흐름 | 옛 ECHO: `/weather` → `/story-start` → `/step/2` → `/understanding-check` → White Door → 결제 4,900원 → `/step/3~7` → 리포트 |
| 운영 함수 | `get-step-question` 버전 48, `echo-journey` 버전 26 — 둘 다 운영에 올라가 있음(2026-09-24 읽기 전용 조회) |
| 화면 연결 | 현재 앱 기본 화면에서는 이어지지 않음. **주소를 직접 치면 열림**(`/doit` → `/doit/choose` → 「ECHO」 → `/weather`). 4,900원 결제 흐름도 이 안에 있음 |
| 같이 발견 | 이 흐름에 고정 대체 질문 `"…" 라고 하셨죠. 그중 어떤 부분이 지금 마음에 남아 있나요?` 가 있다(`get-step-question/ai.ts:207`, `echo-journey/index.ts:476`) |
| 제품 결함인가 | **미확정** — 검사 장치(가짜 모델·가짜 DB)의 문제인지, 서버의 과도한 차단인지 구분하지 못함 |
| 이번 조치 | 이름이 약속한 조건(오류 0 · STEP 7 도달 = 40/40)을 그대로 검사하는 **todo 검사**를 추가했다(`[LEGACY-01 · 테스트 결함 확인]`). 통과로 세지 않는다. 원래 검사는 고치지 않았다 |
| 다음 결정(대표) | ① 옛 흐름을 계속 열어 둘지(주소 직접 입력·결제 포함) ② 원인 조사를 별건으로 진행할지 |

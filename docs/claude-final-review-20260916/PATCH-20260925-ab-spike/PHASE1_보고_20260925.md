# CTO · B안 PHASE 1 실AI 검증 보고 (2026-09-25)

- A = 운영 doit-understanding 버전 27. 원본 SHA-256 `1aab64236bb7c9a41aa40459f0c320abca296e1c928fd76007de2d26b790c450`, 동결.
  - 하네스가 실행 전에 지문을 대조한다.
- B = `product/spike/ab-20260925/agentB.mjs`, Prompt **B-1.0**.
- 하네스 = `run-ab.mjs`.
- MOCK 전체 표 = `PHASE1_MOCK_하네스결과_20260925.md`.
- 실패 목록 = `GOLDEN_FAILURE_LIBRARY_v1_20260925.md`.

## [실제 모델]
- **이번 실AI A/B 실행: 0회 — 확인 불가.**
  - 이 작업 환경에 `OPENAI_API_KEY` 가 없다(실행 직전 확인: unset).
  - OpenAI 서버에는 접속된다(키 없이 요청하면 401).
  - 키 생성, Secret 변경, 운영 Secret 읽기는 금지라 하지 않았다.
- 운영 A 가 쓰는 모델 이름: **확인 불가.**
  - `OPENAI_MODEL` Secret 을 읽지 않았다.
  - 코드 기본값은 `gpt-4o-mini` 다(`resolveModel`).
- 하네스는 키가 생기면 A·B 를 같은 조건으로 돌린다.
  - 같은 엔드포인트·모델, temperature 0.2, top_p 0.9, max_tokens 768, json_object.

## [A/B 실행 Turn 수]
| 구분 | A | B |
|---|---|---|
| 실제 OpenAI | **0** | **0** |
| [MOCK] 하네스(가짜 AI) | 21 | 21 |
| 운영 실측(이전 기록 · 실제 AI · Galaxy 04:20~04:26 KST) | 11턴 · 15호출 | — |

- MOCK 입력 21개의 구성
  - FLOW1 7개(ACTUAL 7)
  - FLOW2 2개(ACTUAL 2)
  - FLOW3 4개(ACTUAL 3 · SYNTHETIC 1 — FAIL #3 첫 답 원문이 기록에 없어 대신함)
  - FLOW4 8개(SYNTHETIC 8 — 지시서 예문)
- 1차 Spike 대비 입력 변경
  - FAIL #3 의 실제 문제제기 문장(캡처 재구성)을 넣었다.
  - 추가 세트를 FLOW4 로 분리하고 ACTUAL/SYNTHETIC 표시를 붙였다.
- **내 실수 2건(실행 전에 찾아 고침)**
  1. B-1.0 초안에 누수 결함이 있었다.
     - 서버가 막은 질문이 있고, 다시 만들기가 형식 실패로 끝나면, 막힌 질문이 그대로 나갈 수 있었다.
     - 막는 즉시 버리도록 고쳤고, 검사를 추가했다.
  2. 블라인드 검수표에서 A 와 B 가 다르게 표시됐다.
     - A 가 되묻기에 답한 칸에는 「같은 질문 다시」 표시가 없고 B 에만 있었다. 표시만 보고 누가 A 인지 알 수 있는 모양이었다.
     - 두 쪽 모두 같은 방식으로 표시하게 고쳤다.

## [FLOW 1 RAW OUTPUT]
- **실제 AI A/B 출력: 확인 불가**(키 없음).
- 참고로, 같은 대화의 **A 운영 실측(실제 서버 · 실제 AI · 이전 보고와 같은 자료)**을 아래에 적는다.

| 시각(KST) | 동작 | 결과 | 나간 질문 |
|---|---|---|---|
| 04:20:47 | 다음 질문 받기 | question | 그 중에서 어떤 취미가 가장 즐거운가요? |
| 04:22:13 | 답 저장 → 다음 질문 | **failed:clue ×2** | (질문 생성 실패) |
| 04:22:21 | 다음 질문 받기 | question | 사람을 알아가는 데 어떤 점이 가장 중요하다고 생각해요? |
| 04:22:41 | 되묻기(규칙) | question | 사람을 알아가면서 어떤 점이 가장 즐거운 것 같아요? |
| 04:23:08 | 답 저장 → 다음 질문 | question | 진심으로 사람을 알아가는 데 어떤 점이 특별하다고 느끼나요? |
| 04:23:41 | 답 저장 → 다음 질문 | question | 사람을 진심으로 알아가는 과정에서 어떤 점이 가장 마음에 드나요? |
| 04:25:48 | 불만(문제제기) | **failed:self_flag ×2** | (질문 생성 실패) |

- [MOCK] FLOW1 의 A·B 표는 `PHASE1_MOCK_하네스결과_20260925.md` 에 있다.
  - 문장이 가짜라 품질 판단에 쓰지 않는다.
  - 서버 결정만 실제 코드 그대로다: 저장/비저장, 규칙 분류, LLM 호출 여부.

## [추가 FLOW RAW OUTPUT]
- **실제 AI A/B 출력: 확인 불가.**
- [MOCK] 에서 본 서버 결정 중 AI 응답과 상관없이 A 코드가 스스로 정한 것
  - **FLOW3-2 「취미생활?」(ACTUAL)**: A = `meta(rule)·비저장`, LLM 분류 호출 0.
    - 운영 FAIL #3 과 같은 결과다(GF-05, 코드 재현).
    - B 는 문자열 규칙이 없어 LLM 에 맡긴다.
  - **FLOW4-3 「아니 그게 아니라」(SYNTHETIC)**: A = 규칙상 correction.
    - 고칠 내용이 없어 LLM 호출 0이고, 고정 문장 「제가 잘못 짚었네요. 어떤 뜻이었는지 한 줄로 알려 줄래요?」가 나갔다.
    - 설계대로의 동작이다. 실패로 세지 않았다.
  - **FLOW4-8 「할말이없다 휴」(SYNTHETIC)**: A = 규칙상 fatigue, LLM 호출 0, 고정 문장.
    - B 는 LLM 을 1번 부른다. 질문이 와도 서버가 버린다.

## [객관적 FAIL]
- 기계적으로만 센다. 사람 판단은 넣지 않는다.
  - 오류(질문 생성 실패·읽기 실패)
  - 문제제기·되묻기·지친 말을 답으로 저장
  - 앞과 글자까지 같은 질문
  - 물음표 2개 이상
  - 금지어
- 표현만 바꾼 같은 뜻 반복은 여기서 세지 않는다. 블라인드 검수로만 본다(글자 휴리스틱 금지).

| 근거 | A | B |
|---|---|---|
| 실제 AI(이번) | 확인 불가 | 확인 불가 |
| 운영 실측(이전 · 실제 AI) | 질문 생성 실패 2/11턴(clue · self_flag) | — |
| 코드 재현(AI 와 상관없는 규칙 단계) | 「취미생활?」 강제 되묻기 1건 | 해당 규칙 없음 |
| [MOCK] 21턴 | 0 | 0 |

- MOCK 의 0 은 구조가 돈다는 뜻일 뿐이다. 품질 결과가 아니다.
- **[HEURISTIC / EXPERIMENT ONLY]** B 의 질문 의도 비교(정규화 뒤 같은 글자인지)
  - LLM 이 같은 뜻에 다른 이름을 붙이면 놓친다(미탐).
  - 오탐·미탐 수는 실제 AI 없이 측정할 수 없다: **확인 불가**.
  - B 에 새로 넣은 대화 품질 문자열 휴리스틱은 0개다.
  - 연락처 차단과 금지어는 기존 안전 규칙 그대로다.

## [Calls]
| 항목 | A [MOCK] | B [MOCK] | A 운영 실측(실제 AI) |
|---|---|---|---|
| 턴 | 21 | 21 | 11 |
| LLM 을 부른 턴 | 19 | 21 | 11 |
| 호출 합 | 19 | 21 | 15 |
| 호출 / LLM 턴 | 1.00 | 1.00 | 1.36 |
| 한 턴 최대 호출 | 1 | 1 | 2 |
| 재시도 | 0 | 0 | 6 |

- 호출 수 차이
  - A 는 규칙으로 정하는 턴(지친 말, 고칠 내용 없는 정정) 2개에서 LLM 을 부르지 않는다.
  - B 는 모든 턴에서 1번 부른다.
  - 연락처 입력만 LLM 0번이다.
- MOCK 재시도 0 은 가짜 AI 가 늘 통과하는 답을 주기 때문이다.
  - 실제 재시도율은 운영 실측(A 6회)만 있다.

## [Tokens]
- o200k_base 로 센 값이다. **API usage(과금 정본)는 확인 불가(MOCK).**
- **비용은 계산하지 않았다.** 공식 가격표와 실제 모델명을 확인하지 않았다.

| 항목 | A [MOCK] | B [MOCK] |
|---|---|---|
| system 프롬프트 / 호출 | 1,492 | 702 |
| 입력 JSON / 호출 | 301 | 279 |
| 입력 합 / 호출 | 1,793 | 981 (−45%) |
| 입력 토큰 합(21턴) | 34,061 | 20,611 (−39.5%, B 는 호출이 2번 많다) |
| 출력 토큰 합 | 1,748 | 1,277 — 가짜 문장이라 의미 없음 |

- 입력 구성(호출당 평균, system 제외)
  - A: facts 128 · already_asked 38 · hot_memory 35(저장된 답만) · last_question 14 · 나머지 ≤11
    - remaining·answered_count 포함.
  - B: facts 90 · recent 76(저장 안 한 말 포함) · user_stated 20 · last_question 15 · 나머지 ≤11
    - remaining·answered_count·주제·질문 순서 0.

## [Latency]
- **실제 AI A/B 지연: 확인 불가.** MOCK 지연은 판정하지 않는다.
- A 운영 실측(이전 기록 · 실제 AI · n 이 작아 p95 는 계산하지 않음)
  - LLM 1호출: 중앙값 2,100ms, 최소 1,700, 최대 4,040 (n=15).
  - 턴 전체: 중앙값 2,967ms, 최소 2,175, 최대 7,029 (n=11).
- 키가 생기면 하네스가 A·B 각각 다음 값을 적는다.
  - 턴 p50·p95·최대
  - LLM 호출 p50·최대

## [Blind Review Sheet]
- **실제 AI 출력이 없어 대표 검수는 할 수 없다: 확인 불가.**
- 양식은 준비했고, MOCK 으로 모양만 확인했다(21칸).
  - 입력마다 `USER / OUTPUT X / OUTPUT Y` 에 「☐ X가 낫다 ☐ Y가 낫다 ☐ 둘 다 별로다」와 메모 칸이 붙는다.
  - X·Y 가 A·B 중 무엇인지는 입력마다 무작위다.
  - 열쇠는 별도 파일이다(`--key`). 검수가 끝나기 전에는 열지 않는다.
  - 칸마다 출처(ACTUAL/SYNTHETIC)를 적는다.
- **Claude 는 승자를 고르지 않는다.**
- 실행 명령(키가 들어간 새 세션에서, `product` 폴더 기준)
  - `node spike/ab-20260925/run-ab.mjs --out 결과.md --json 결과.json --blind 검수표.md --key 열쇠.json`
  - NODE_PATH 에 typescript·js-tiktoken 이 있어야 한다.

## [Failure Intelligence]
- `GOLDEN_FAILURE_LIBRARY_v1_20260925.md` — 9건.
  - 칸 = Failure ID · 실제 사용자 상황 · 실패 유형 · 원인 Layer · A 결과 · B 결과 · 재현 여부 · 해결 후보 · 검증 상태.
- 원인 Layer 분포
  - Orchestration 7건(GF-01·02·03·05·06·07·08, 일부는 Context 와 겹침)
  - Context 1건(GF-04)
  - Product Contract 1건(GF-09)
  - Infrastructure 0건
  - Model: 분리 안 됨
- A(v27) 실패 확정
  - 운영 실측 5건(GF-01·02·03·06·07).
  - 코드 재현·확인 3건(GF-04·05·09 — GF-05 는 운영에서도 관측).
  - GF-08 은 v26 에서만 관측했다.
- B 는 전 항목 실제 AI 미검증. "해결됨"은 0건이다.

## [B WIN: 확인 불가]
- 실제 AI 로 B 를 한 번도 돌리지 못했다.
- MOCK 결과(입력 토큰 −39.5%, 객관 FAIL 0 대 0)로는 B 승리를 선언하지 않는다.

## [Model Comparison 필요: NO]
- 지금은 필요하다는 근거가 없다.
- 기준본의 분기는 "A·B 둘 다 실제 AI 로 약할 때만 모델 비교"다. 실제 AI A/B 가 0회라 그 조건을 확인하지 못했다.
- 실제 결과에서 둘 다 약하게 나오면 YES 로 바뀐다.

## [기술스택 교체 필요: NO]
- 운영 11턴에서 인프라 장애 증거가 0이다.
  - 부팅·인증·DB·OpenAI 응답 모두 정상이었다.
  - 두 실패 턴도 서버 검사가 후보를 떨어뜨린 코드 경로다.
- 드러난 실패는 대화 조립 층(Orchestration·Context)에 있다.

## [Conversation P0: FAIL 유지]

## [운영 변경: 0]
- 운영 함수·DB·RLS·Auth·Secret·모델·UI·배포 변경: 0.
- 바뀐 파일은 `product/spike/ab-20260925/` 3개(배포 경로 밖)와 이 폴더의 문서뿐이다.
  - B 검사 [MOCK] 14/14 통과.
  - 일부러 망가뜨리기 7/7 을 잡았다.
  - 제품 src 변경 0.

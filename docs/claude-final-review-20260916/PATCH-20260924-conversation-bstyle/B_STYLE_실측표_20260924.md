# B구조 「오늘 내 마음의 날씨는 어때?」 ↔ 현재 Conversation 실측표 (2026-09-24 · 읽기 전용)

## 출처

- B구조 화면
  - `src/pages/do-it/weather/page.tsx` — 라우트 `/weather`, 제목 「오늘 내 마음의<br/>날씨는 어때?」
  - `src/pages/do-it/weather-check/page.tsx` — 라우트 `/weather-check`, 마음 입력 화면
  - 배경 `src/pages/do-it/weather/components/WeatherBackdrop.tsx` + `src/pages/home/components/PastelBlobs.tsx`
  - 날씨 효과 `WeatherEffect.tsx`
  - 공통 `src/index.css` — `.echo-min-h-viewport` · `float-bg` · `--font-body`
- 현재 Conversation(`/doit/conversation`)
  - `src/doit/components/feature/core-conversation.css`
  - `doit-type.css`(글꼴)
  - `metal-silver.css`(주요 버튼)
- 라우팅
  - `/weather`·`/weather-check` 는 `src/router/config.tsx:79-80` 에 아직 등록돼 있다.
  - 앱 첫 흐름에서는 연결이 닫혀 있다(`src/pages/do-it/4/page.tsx:100`).

## 비교표

| 항목 | B구조 실제값 | 현재 Conversation 실제값 | 이식 여부(제안) |
|---|---|---|---|
| 화면 틀 | `section.echo-min-h-viewport`(100dvh) · 가운데 정렬 · 내용 `max-w-md`(448px) · `px-6`(24px) · 위 `safe-area+20~32px` · 아래 `safe-area+32px` | `.echo-dialogue` max-width 620px · padding 28px 24px 56px · min-height 100dvh | 이식: 폭 448px·세이프에어리어 여백 |
| 배경 | 고정 전체 화면 사진(`storage.helloreaddy.io/…c125683b…webp`) + 20초 떠다니는 확대(`float-bg`: scale 1.05, translate -1%) + 파스텔 번짐 5개(`PastelBlobs`: rgba(160,255,200,.15)·(160,220,255,.12)·(255,235,160,.10)·(255,180,180,.12)·(160,255,220,.10), 20~30rem, 22~30초) | 단색 `#090a0c` | 이식 후보. **단, 배경 사진은 바깥 서버(Readdy)에 있고 이 환경에서 막혀(connect_rejected) 그림 내용 확인 불가** → 사이트 안으로 옮기려면 대표가 원본 파일 제공 필요 |
| 날씨 효과 | `WeatherEffect`(날씨별 입자) | 없음 | **이식 안 함**(날씨 기능) |
| 본문 글꼴 | Pretendard(`--font-body`) | 본문 Pretendard 계열 · 안내 문장 고운돋움(doit-type.css) | 이식: Pretendard |
| 제목(질문) 글꼴·크기 | h1 `26px`(/weather) · `24px`(/weather-check) · `font-bold`(700) · `leading-snug`(1.375) · 흰색 · 가운데(/weather) / 왼쪽(/weather-check) | h1 clamp(28~38px) 500 · 줄 간격 1.55 · 자간 -.045em / 질문 `.echo-question` 21px 500 · 줄 간격 1.8 · #e4e7ec · **글꼴 고운바탕**(doit-type.css, 2026-09-23 대표 요청 "사람 냄새") | 부분 이식: 크기·굵기·흰색. **글꼴(고운바탕→Pretendard 굵게)은 v14.6 대표 결정과 충돌 → 대표 결정 필요** |
| 머리말 | `ECHO` 11px · 자간 .5em · 대문자 · white/55 | `.echo-eyebrow` 10px 600 · 자간 .04em · #a0a6b2 | 이식 가능 |
| 보조 문장 | 13.5px · white/55~60 · leading-relaxed(1.625) | `.echo-lead` 14px · 1.95 · #b5bac3 | 이식 |
| 입력창 | textarea `min-h 190px` · `rounded-[22px]` · 배경 white/7% · 테두리 white/12% · 흐림 14px · `px-5 py-5`(20px) · 15px · leading-relaxed · 안내 글 white/35 · 포커스 테두리 white/30·배경 white/10% · 글자 수 오른쪽 아래 11.5px white/40 | 배경 #101216 · 테두리 #adb9ca37 · radius 15px · padding 18px · 15px · 줄 간격 1.9 · 최소 120px · 포커스 #c2cfdd + 3px 그림자 | 이식(반투명·흐림·둥근 22px) |
| 주요 버튼 | 높이 56px(`h-14`) · `rounded-2xl`(16px) · **흰 바탕 · 글자 #0c1526** · 15px 600 · 누르면 scale .99 | 메탈 실버 그라데이션(metal-silver.css, 대표 v14.5 결정) · 높이 56px · radius 14px | 대표 결정 필요(흰 버튼 vs 메탈 실버) |
| 보조 버튼 | 테두리 white/25 · 흰 글자 15px 500 · 또는 밑줄 글자 13px white/55 | `.echo-secondary` #171a20 · 테두리 #c3ccda38 | 이식(투명 + 얇은 테두리) |
| 칩(보기) | 높이 44px · 둥근 알약 · white/9% · 테두리 white/14% · 13px white/80 | 목적 타일(`.echo-opening-tile`) #171a20 · radius 14px | 참고만(대화 화면에 칩 없음) |
| 뒤로 버튼 | 44px 원 · white/10 · 테두리 white/15 · 흐림 | 기존 뒤로 알약(앱 공통) | 기존 유지 |
| 진행 표시 | **없음**(B구조 화면에 진행률 없음) | 맨 위 「n / 5」 13px 600 + 2px 막대 | 참고할 B값 없음 → 약하게만 조정 |
| 등장 움직임 | 항목마다 0.7초 동안 아래 30px에서 떠오름, `cubic-bezier(.4,0,.2,1)`, 80→120→240…ms 차례 | 막대 너비 0.5초 · 대기 궤도 움직임 | 이식(질문 바뀔 때 한 번, 움직임 줄이기 존중) |
| 마우스 따라 움직임 | 8px 패럴랙스(컴퓨터) | 없음 | 이식 안 함(모바일 중심 · 과한 움직임 금지) |
| 카드 | 날씨 지표 카드 white/7 · 흐림 10px · rounded-2xl | 통합 카드·재시작 확인·쉬어가기 #14171c · radius 16~20px | 이식(반투명) |
| 보조 글자색 | white/40·55·60·65 | #939ba8 · #98a1af · #aeb4bf 등 | 이식(흰색 투명도 체계) |
| 줄 간격(letter-spacing) | 제목 기본값 · 머리말 .5em | 제목 -.045em | 부분 이식 |
| 그라데이션·오버레이 | 사진 위 오버레이 없음(파스텔 번짐만) | 카드 안 은은한 방사형 | 확인만 |

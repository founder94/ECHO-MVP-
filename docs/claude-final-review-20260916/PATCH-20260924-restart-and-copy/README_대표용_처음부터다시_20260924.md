# 처음부터 다시 · 연결 화면 버튼 · 옛 문구 정리 (2026-09-24)

근거: 대표 실기기 캡처(06:11 KST) "처음부터 시작하기는 아직 구현이 안된 거 같다 / 연결에서 질문 이어서 답하기 누르면 다음으로 안 넘어가고 다섯 가지 다 들었다는 문구 나온다".

**아직 아무도 실제로 써 보지 않았습니다.** 검사는 모두 가짜 서버 기준입니다.

## 원인
1. 「처음부터 다시」는 있었지만, 다섯 개를 다 답한 끝 화면에서는 화면 맨 위의 작은 글씨로만 있어서 잘 안 보였습니다.
2. 연결 화면 맨 아래 「질문에 이어서 답하기」 버튼이 **다섯 개를 다 답해도 그대로** 떠 있었습니다. 누르면 "다 들었어요" 화면만 나왔습니다.
3. 연결 기능이 열렸는데도 화면 다섯 곳에 "사람 연결은 아직 열리지 않았어요 / 준비 중" 같은 **옛 문장**이 남아 있었습니다. 프로필 확인 화면의 「공간 준비 소식 보기」는 아무것도 없는 준비 중 화면으로 보냈습니다.

## 바꾼 것 (화면만 · 서버·DB 변경 0)
- 끝 화면에 **「처음부터 다시 답하기」 버튼**을 크게 추가했습니다. 누르면 확인 창이 뜹니다: "지금까지 이야기는 그대로 남고, 첫 질문부터 새로 시작해요. 다섯 가지를 다시 답할 때까지는 새 연결 후보에서 잠시 빠져요."
- 연결 화면: 남은 것 중 **첫 번째**를 큰 버튼으로 보여 줍니다(예: 전화 인증하기). 다섯 개를 다 답했으면 「처음부터 다시 답하기」를 작은 버튼으로 둡니다.
- 옛 문장 정리: 끝 화면·프로필 확인·내 프로필·목적 고르기·가입 안내. 「공간 준비 소식 보기」 → 「연결까지 남은 것 보기」(연결 탭으로 이동).
- 연결 준비 카드의 "아래를 다 채우면" → "위 네 가지를 다 채우면"(목록이 문장 위에 있음).

## 검사 (가짜 기준)
전체 378개 중 376 통과 / 0 실패 / 미확정 2(예전부터 있던 2건). 타입 검사 0, 문법 검사 0, 보안 취약점 0. 브라우저 클릭 검사 61/61(새로 추가: 다 답했을 때 / 덜 답했을 때 연결 화면 버튼).
옛 사실("연결은 아직 안 열림")을 지키던 검사 2개는 새 사실에 맞게 바꿨습니다: "남은 것을 사실대로 말하고, 아직 안 열렸다는 옛말은 하지 않는다".

## 올릴 것
37차 앱 ZIP 1개(`1_APP_여기에올릴것_doitmobile.zip`, SHA-256 `0fcb9e54…`). 홈페이지는 이번에 바뀐 것이 없습니다(운영 do-it.company = 36차와 글자 단위 동일 확인).

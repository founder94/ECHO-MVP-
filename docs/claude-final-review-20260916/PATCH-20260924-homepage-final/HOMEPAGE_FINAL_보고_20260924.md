# ECHO Agent v1 + 홈페이지 최종 보고 (2026-09-24)

근거: 대표 최종 승인 PDF 「ECHO AGENT v1 / HOMEPAGE FINAL LOCK」(5쪽, 0번 ~ END LOCK).
범위: 홈페이지 문구·구성 최종화, Agent v1 은 그대로 두고 배포 후보만 다시 묶음. **운영 배포 0건.**

## 1. 바꾼 것 (화면만 · 서버 수정 0)

| PDF | 바꾼 곳 | 내용 |
|---|---|---|
| §0·§3 HERO | `src/components/DoItBrandHero.tsx` | 메인 「좋아하는 사람보다, / 편해지는 사람은 다를 수 있으니까.」 · 서브 「ECHO는 당신의 말을 조금씩 기억하며, / 어떤 관계가 자연스러운지 알아갑니다.」 · 버튼 「ECHO 시작하기」. 글자만 바뀌고 자리·크기·색·움직임은 그대로. 옛 「소개팅」 문구와 그 검사 예외를 없앴다. |
| §4 두 번째 구역 | `BrandSections.tsx` `BrandDifference` | 제목 「좋은 인연은 / 많이 보는 것보다, / 조금 더 알아가는 데서 시작되니까.」 + 세 줄 「당신의 말을 기억하고. / 당신과 어울리는 결을 알아가고. / 때가 되면, 한 사람을 보여드립니다.」. 셋째 줄에만 작은 「준비 중」(아래 §3 운영 사실). 번호 카드 5장은 뺐다. |
| §5 Trust | `BrandTrust` | 제목 「당신을 함부로 정의하지 않습니다.」 + 세 줄 + 마지막 「천천히 알아가는 것. / 사람에게도, AI에게도 필요하니까.」. 카드 4장은 뺐다. |
| §6 Just Try | `BrandJustTry` | 「JUST TRY」 · 「좋은 관계가 시작되는 데 거창한 용기는 필요하지 않을지도 모릅니다.」 · 「한 번의 대답. / 한 번의 선택. / 한 번의 만남.」 · 「작은 시도에서 시작됩니다.」 버튼·기능 설명은 없다. |
| §7 상태 문구 | `landing/page.tsx` 이야기 09 | 옛 「지금은 목적 선택과 프로필 준비까지. 사람 연결은 준비 중입니다.」 → **「지금은 당신의 이야기를 듣는 데서 시작합니다. / 다음 단계는 ECHO가 준비하고 있습니다.」** |
| §2·§10 Quiet Luxury | `brand-sections.css` | 기능 카드 틀(`.doit-brand-cards/.card`) 규칙을 빼고, 문장만 두는 규칙 4줄(`--quiet` 제목, `.doit-brand-verse`, `.doit-brand-verse-closing`)을 더했다. 모두 `.doit-editorial` 안에서만 쓴다. 전역·히어로 CSS 변경은 0이다. 새 색·그라데이션·움직임도 0이다. |

- 바꾸지 않은 것:
  - 히어로 그림·배치, 이야기 01~09 사진과 글, 시작하는 법·QR, 회사 소개, 대표 인사말, 앱 화면
  - 서버 `doit-understanding`(Agent v1)과 `doit-connect`
- 이 랜딩은 앱(app.do-it.company) 첫 화면에서도 쓴다. 그래서 **히어로 문구와 이야기 09 문구는 앱에도 같이 바뀐다.** 두 번째 구역·Trust·Just Try는 홈페이지에만 나온다(`!IS_APP_SITE`, 실측 확인).

## 2. Agent v1 (§8) — 다시 뜯지 않음

- 서버 파일 SHA-256 `113e3738…` 는 앞선 Agent v1 후보와 **같다**. 이번 작업에서 서버 수정은 0이다.
- 한 턴 흐름은 그대로다: 서버 요청 1번, LLM 1번, 재생성은 최대 1번. 앱 자동 재요청은 빠져 있다.
- 새 경로(v16 `turn`)는 옛 판정 장치(12자 분기·주제 순서·no_bridge·anchor)를 거치지 않는다. 이는 검사로 확인한 내용이다(`qa/conversation-v16.test.mjs`).
- §11 「처음부터 시작하기」는 v15.2 에서 이미 첫 답 전, 정상 대화, 오류 상태 모두에서 위쪽에 있다. 검사 221·222·223·224 가 통과했다. 이 기능은 새 회차만 시작하고 기록을 지우지 않는다.

## 3. §7 운영 사실 확인 (읽기 전용 · 2026-09-24)

- 운영 DB 읽기 결과: 회원 6명, 전화 인증 0명(profiles `verified` 0 · Auth 전화 확인 0), 연결(`doit_matches`) 0건.
- 연결 자격에는 전화 인증이 필수다. 그런데 문자 발송 업체(Twilio) 연동이 중단되어 있어, **지금은 누구도 연결을 받을 수 없다.**
- 그래서 PDF가 허락한 문장 「당신의 이야기가 쌓이면, 다음은 ECHO가 준비합니다.」도 운영보다 앞서간다고 판단했다. 이야기가 쌓여도 지금은 다음이 오지 않기 때문이다.
- PDF 지시(「앞서가면 더 보수적으로」)에 따라 **지금 되는 것(이야기 듣기)과 아직인 것(다음 단계 준비)만** 말하는 문장으로 바꿨다.
- 같은 이유로 두 번째 구역의 「때가 되면, 한 사람을 보여드립니다.」에도 작은 「준비 중」을 붙였다.

## 4. 검사 결과 (모두 로컬 · 가짜 서버/가짜 AI 기준)

| 항목 | 결과 | 비고 |
|---|---|---|
| 전체 검사 | 517개 중 512 통과 / 0 실패 / 미확정 5 | 미확정 5 = 전과 같은 목록(휴리스틱 공격 2 · 옛 STEP 1→7 흐름 3, LEGACY-01 포함) |
| type-check · lint | 0 · 0 | |
| build:app · build:brand | 0 · 0 | |
| 서버 엄격 타입(doit-understanding · doit-connect) | 0 · 0 | |
| npm audit | 취약점 0 | |
| 홈페이지 브라우저 360·390·430·1440 | **45/45** | 첫 화면 Hero+CTA 보임 · 가로 넘침 0 · 글자 잘림 0 · 구간 7개 · 카드 0 · 옛 문구 0 · 휴대폰 CTA → app.do-it.company/doit/start-journey · 컴퓨터 CTA → QR 상자 · 홈페이지 콘솔 오류 0 · 앱 첫 화면 같은 Hero · 앱 CTA → /doit/start-journey |
| 앱 화면 18개(360·390·430 × 6 경로) | 18/18 | 로그아웃 상태 |
| ZIP | 빌드 폴더와 차이 0 · 소스맵 0 · .env 0 · 비밀키 0 · Stripe 0 | |

### 검사에서 걸러 낸 것 (정직하게 적는다)

- **콘솔 오류**: 처음 돌렸을 때 오류가 났다.
  - 원인: 이 작업 환경이 cdnjs·jsdelivr(아이콘·Pretendard 글꼴 CSS)를 네트워크 정책으로 막고 있었다(ERR_TUNNEL_CONNECTION_FAILED). 운영 사용자는 이 파일을 실제로 받는다.
  - 처리: 검사에서는 빈 CSS로 채웠다.
  - CTA를 누른 뒤 1건이 더 나왔는데, 검사용 가짜 앱 문서의 favicon 요청이었다. 판정에서 빼고 결과 파일에 「참고」로 남겼다.
- **글자 잘림 「DO IT」**: 히어로 제목(h1)이 잘린 것으로 잡혔다. 실제로는 화면 읽기용 숨은 글자와, 일부러 잘라 보이는 지구 그림(기존 디자인)이었다. 보이는 글자는 잘리지 않았다. 이 경우만 빼도록 검사를 좁혔다.
- **글꼴**: 위 이유로 캡처는 Pretendard 가 아니라 기본 글꼴로 찍혔다. 운영에서의 실제 글꼴은 대표 실기기에서 확인해야 한다.

### 실제로 하지 않은 것 → PASS 아님

- 실제 OpenAI로 대화를 돌리지 않았다(키 없음). 그래서 Agent v1 의 실AI 동작은 **확인 불가**다.
- 운영 로그 기반 속도(p50·p75·p95)는 **측정 전**이다.
- 진짜 아이폰·갤럭시로 확인하지 않았다.
- 운영 배포 0건이다.

## 5. 배포 후보와 운영 대상

`CANDIDATES_SHA256.txt` 참고. 배포 순서(PDF §13): ① 서버 → ② 앱 → ③ 홈페이지. 배포할 때마다 지문을 대조하고, 다르면 PASS 금지.

| 순서 | 대상 | 후보 SHA-256 | 운영 현재 | 방법 |
|---|---|---|---|---|
| ① | Supabase `doit-understanding` | `113e3738cc48192c92e55db4741cb9e76d9de71e0612d7530db7ee21e07d430d` | 버전 26 / v15.2 `fd2e1eee…` | Supabase 관리 API로 로컬 파일 그대로 올림 → 내려받아 cmp |
| ② | Netlify `doitmobile` (app.do-it.company) | `a8d4e79043e2b08016b9783797488170fc47acec000851aea4f11f6259b7b5b6` | 6ab50bb9…(43차) | 대표 수동 drop(이 환경에서 Netlify 막힘) |
| ③ | Netlify `echo-mvp-doit` (do-it.company) | `061f7397e6ee5811180ba91d263a3881d4970e15ede307912f9865a0c80399f0` | 36차 | 대표 수동 drop → 내가 do-it.company 를 내려받아 글자 단위 대조 |

- 서버를 먼저 올려야 한다. 새 앱은 `turn` 을 부르는데, 운영 v15.2 에는 `turn` 이 없다.
- 새 서버는 옛 앱(43차)과 호환된다. 옛 동작을 그대로 두었기 때문이다.

## 6. STOP 항목 변경 0건

DB·Migration·RLS·Auth·Secret·모델·결제·가격·KEY·운영 데이터 변경은 0건이다. 운영 Edge 배포와 Netlify 배포도 0건이다. 운영 DB 는 개수 읽기 한 번만 했다(SELECT count).

# 연결 v1 — 전화 인증부터 사람끼리 이어지기까지 (2026-09-23)

근거: 대표 2026-09-23 "지금 어디까지 구현을 해야 되는 단계까지는 승인하니까 허용하고 끝까지 진행시켜" + 연결 원칙(대표 확정 2026-09-21).

**아직 아무도 실제로 써 보지 않았습니다.** 아래 검사는 모두 가짜 서버·가짜 AI 기준이고, 운영에서 진짜 두 사람이 이어진 적은 없습니다.

## 1. 이번에 생긴 흐름

1. **전화 인증** (`/doit/verify`) — 휴대폰 번호 → 문자 6자리 → 확인. 예전 "데모 본인인증"(1.5초 뒤 저절로 완료) 화면은 없앴습니다.
   - 인증됐는지는 화면이 정하지 않습니다. Supabase 로그인 서버가 확인한 기록을 연결 서버가 다시 보고 프로필에 "인증함"을 적습니다.
   - 언제든 「나중에 할게요」로 나갈 수 있습니다. 문자 서비스가 아직 안 켜져 있으면 "지금은 문자 인증을 준비하고 있어요"라고만 나옵니다.
2. **연결 자격** — 전화 인증 + 맞다고 한 말 5개 + 필수 사진 3장 + 소개 (예전과 같은 기준).
3. **후보 고르기** — 서버가 자격을 갖춘 사람 중 **같은 만남을 고르고 맞다고 한 말이 겹치는** 두 사람을 고릅니다. 차단한 사이·이미 결정한 쌍은 빠집니다.
4. **대표 승인** — 관리자 화면 `/doit/admin/mobile` → 왼쪽 메뉴 **「연결 승인」**. 「승인하고 첫 질문 보내기」 또는 「이 쌍은 넘기기」.
   - 승인 순간에 서버가 자격·목적·차단·겹침을 한 번 더 확인합니다.
   - 승인하면 AI가 두 사람에게 **같은 첫 질문**을 만듭니다. 질문이 규칙(한 문장·45자 안팎·개인정보 묻지 않기·쓰지 않는 단어 없음·둘의 말을 그대로 옮기지 않기)에 걸리면 기본 질문 "처음 만난 사람에게 가장 먼저 들려주고 싶은 내 이야기는 뭐예요?"를 씁니다.
5. **첫 질문 동시 공개(blind-first)** — 연결 탭 맨 위 「내 연결」. 둘 다 답하기 전에는 상대 이름·사진·소개·답이 **서버에서 아예 내려오지 않습니다**.
6. **이야기** — 둘 다 답하면 이름·대표 사진·소개·서로의 답이 열리고, 그 아래에서 글을 주고받습니다. 전화번호·이메일·링크·성적 표현은 보낼 수 없습니다.
7. **그만하기** — 「이 연결 그만하기」 → 「그만할게요」 또는 「차단하고 신고할게요」(신고는 기존 신고 목록 `user_reports` 에 들어감). 끝난 연결은 다시 이어지지 않습니다.

## 2. 운영에 올라간 것 (대표 승인 근거: 위 문장)

| 무엇 | 상태 | 확인한 곳 |
|---|---|---|
| DB 새 표 3개 `doit_matches`·`doit_match_answers`·`doit_match_messages` | 운영 적용 | Supabase → Database → Tables. 화면(로그인 사용자)에서는 읽기·쓰기 권한 0, 서버 함수만 씀 |
| 되돌리기 SQL | 파일만(실행 안 함) | `supabase/rollback/20260923120000_doit_connect_v1_rollback.sql` — 실행하면 연결 기록이 모두 지워짐 |
| 서버 함수 `doit-connect` | 버전 1 · ACTIVE · 로그인 확인 켜짐 · 로그인 없이 부르면 401 | 올라간 파일 = 이 폴더의 `index.ts` 와 글자 단위 동일, 지문(SHA-256) `d0afcca37f4bab16bdbebd5d74b95bac74244208bc530a68d48df1b4a8ee0854` |
| 대화 서버 `doit-understanding` | 바꾸지 않음(버전 20 그대로) | — |
| 기존 표·정책·결제·가격·히어로 | 바꾸지 않음 | — |

## 3. 대표님이 하실 일 (3개)

### ① 앱 압축 파일 올리기 (34차, 1개만)
홈페이지(do-it.company)는 이번에 겉으로 바뀐 게 없어서 **올리지 않아도 됩니다.**
1. app.netlify.com 에 로그인
2. 사이트 목록에서 **doitmobile** 을 누른다
3. 위 메뉴 **Deploys** 를 누른다
4. 화면 아래 "Drag and drop your project output folder here" 네모 칸에 `1_APP_여기에올릴것_doitmobile.zip` 을 끌어다 놓는다
5. 맨 위에 초록색 **Published** 가 뜨면 끝

### ② 문자 인증 켜기 (Supabase + 문자 발송 업체)
문자는 Supabase가 직접 보내지 않고 **문자 발송 업체(Twilio 등)** 를 거칩니다. 업체 계정·요금은 대표님 몫입니다(한국 번호 문자 요금·도달률은 **검증 필요**).
1. supabase.com/dashboard 에 로그인 → 프로젝트 **zyyhhxyupizcqhxqnxuu** 를 연다
2. 왼쪽 메뉴 **Authentication** → **Sign In / Providers** (화면에 따라 "Providers") 를 누른다
3. 목록에서 **Phone** 을 눌러 켠다(Enable)
4. **SMS provider** 칸에서 업체를 고른다(Twilio 또는 Twilio Verify 권장 — Supabase 문서에 나온 업체) → 그 업체 화면에서 받은 값(Account SID·Auth Token·Message Service SID 등)을 칸에 붙여 넣는다 → **Save**
   - 이 값들은 **저(Claude)에게 보내지 마세요.** 대표님이 Supabase 화면에만 넣습니다.
   - 켜기 전에는 인증 화면에 "지금은 문자 인증을 준비하고 있어요"만 나오고, 아무것도 멈추지 않습니다.
   - 확인 필요: Phone 을 켜면 "전화번호만으로 가입"도 함께 열리는지 대시보드에서 확인해 주세요. 우리 앱 화면에는 전화번호 가입 버튼이 없습니다.

### ③ 실기기로 한 바퀴 (QA 계정 0423doit@gmail.com 로)
1. 앱 → 하단 **연결** 탭 → **전화 인증** 줄을 누른다 → 번호 적고 문자 숫자 넣기 → "전화 인증을 마쳤어요" 가 나오는지
2. 연결 탭의 전화 인증 줄이 **했음** 으로 바뀌는지
3. 두 번째 계정이 있어야 연결까지 볼 수 있습니다. 두 계정이 모두 자격을 갖추면 관리자 화면 「연결 승인」에 후보가 뜹니다. (새 계정은 제가 만들지 않습니다.)

## 4. 검사 결과 (모두 가짜 기준)

- 서버 흐름 검사(가짜 DB·가짜 AI): **25/25** — 로그인 없음 401, 관리자 아니면 403, 인증 없는 사람·사진 부족·다른 목적·새 회차 이전 말은 후보 아님, 차단은 한쪽만 해도 막힘, 둘 다 답하기 전 상대 이름·소개·사진·답 0, 남의 연결에 답하기 404, 전화번호·링크 차단, 그만하기+차단+신고, 관리자 목록에 이야기 내용 0, 로그에 원문·번호 0.
- 일부러 고장 내기 검사: 보안 장치 10개를 하나씩 지웠을 때 검사가 **10개 모두** 잡음.
- 화면 검사: **10/10** (실제 사람이 치는 번호 모양 8가지·틀린 번호 8가지·오류 안내 등). 이 검사에서 "010-1234-567"(한 자리 모자람)을 받아 주던 **내 실수**를 찾아 고쳤습니다.
- 브라우저 클릭 검사(가짜 서버, 390px): **33/33**.
- 실제 앱 빌드로 `/doit/verify`·`/doit/connections` 열기: 화면 오류 0, 가로 넘침 0.
- 전체: **363개 중 361 통과 / 0 실패 / 미확정 2**(예전부터 있던 2건), 타입 검사 0, 문법 검사 0, 보안 취약점 0.

## 5. 남은 것·알고 있는 한계

- **알림 없음** — 연결이 생겨도 문자·푸시가 가지 않습니다. 연결 탭을 열어야 보입니다. 이야기도 30초마다 새로 불러오는 방식이라 실시간 채팅은 아닙니다.
- **"맞다고 한 말 5개"** — 다섯 질문에 답해도 「맞아요」를 5번 누르지 않으면 자격이 안 됩니다(앞서 말씀드린 점, 그대로).
- **약관** — 연결되면 상대에게 닉네임·대표 사진·소개·첫 답이 보입니다. 약관·개인정보 문서(초안)에 이 내용이 들어가야 합니다 — 대표님 검토 필요.
- **사진 주소** — 상대 대표 사진은 10분짜리 서명 주소로만 나갑니다. 주소 안에 저장 폴더 이름(상대 계정 번호)이 들어갑니다. 비공개 저장소라 그 번호만으로는 아무것도 열 수 없습니다.
- **관리자 화면 기존 경고** — Supabase 보안 점검의 기존 경고(is_admin·key_spend 함수 공개 실행, 유출 비밀번호 차단 꺼짐)는 이번 작업과 무관하게 전부터 있던 것입니다. 새 표 3개는 "정책 0개" 안내만 뜨는데, 화면이 못 읽게 일부러 잠근 것입니다.
- 사용자가 자기 인증 상태나 역할을 직접 바꿀 수 있는지 의심했었는데, **확인해 보니 막혀 있습니다**(열 단위 권한에 없음 + 역할 잠금 장치).

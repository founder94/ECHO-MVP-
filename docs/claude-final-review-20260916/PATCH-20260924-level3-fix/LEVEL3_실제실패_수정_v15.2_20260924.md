# LEVEL 3 실제 실패 수정 · v15.2 (2026-09-24) — PATCH READY, 운영 미배포

판정: **LEVEL 3 Conversation = FAIL** (v15.1 운영, 대표 실기기 2026-09-24 18:57 KST 무렵).
이 실패가 운영에서 해결되기 전에는 "Conversation P0 PASS"라고 판정하지 않는다.

## 실제 실패
- 원문: "그냥 편한친구 부담없이" (12자, 이번 회차 첫 답)
- 직전 질문: 첫 고정 질문(목적 타일 다음) — 서버 분류는 answer(AI 분류)
- 실제 화면: "다음 질문을 아직 만들지 못했어요. 적은 답은 저장돼 있어요. 한 번 더 눌러 주세요." + 「다음 질문 받기」

## Root Cause (운영 로그 + 코드)
- 운영 기록(읽기 전용, 2026-09-24 09:57:51~09:58:30Z):
  - `doit_records`에 이 답 1줄이 저장됐다(길이 12, 상태 confirmed).
  - `followup_generate` 요청 3번이 모두 `FOLLOWUP_EXHAUSTED:no_bridge` 로 끝나 502 를 돌려줬다. 두 번은 대표가 「다음 질문 받기」를 다시 누른 것이다.
  - 요청마다 후보 3개가 모두 `stage:compose · reason:no_bridge · strategy:CHANGE_DIRECTION` 로 떨어졌다. 모두 9개다.
  - 다른 검사(반복·정정·거절·판정·목적·톤·정보·상태)까지 간 후보는 0개다. 모두 no_bridge 한 곳에서 멈췄다.
- 파일: `supabase/functions/doit-understanding/index.ts`
- 함수: `pickStrategy` → `composeQuestion` → `checkCandidate`
- Gate: `no_bridge`(새 주제로 넘어갈 때 방금 답을 받아 주는 첫 줄이 있어야 한다)
- 실제 탈락 이유:
  1. 답이 12자 이하라 서버가 전략을 「새 갈래(CHANGE_DIRECTION)」로 정했다(`SHORT_ANSWER_MAX` = 12, 이하면 새 갈래).
  2. 새 갈래는 받아 주는 첫 줄(ack)이 필수다.
  3. 그런데 서버는 첫 줄의 근거를 **별도 칸 `basis` 의 글자 인용으로만** 확인했다. AI가 basis 를 조금만 바꿔 말하거나 물음표·반복 등 다른 이유가 있으면, 첫 줄이 **조용히 지워지고**(`ackOk=false → ack=""`) "첫 줄 없음"으로 떨어졌다.
  4. 다시 만들 때 AI에게 준 이유는 늘 "첫 줄(ack)이 없었다"였다. AI는 첫 줄을 썼는데도 같은 답을 되풀이했다. 그래서 세 번 모두 같은 이유로 떨어졌고, 사용자에게 "다시 눌러 주세요"를 맡겼다.
- 확정하지 못한 것:
  - 운영 로그에는 원칙대로 사용자 원문과 AI 후보 문장을 남기지 않는다.
  - 그래서 첫 줄이 지워진 세부 이유(basis 바꿔 말하기 / 물음표 / 반복 등) 중 어느 것이었는지는 운영 증거로 가를 수 없다.
  - v15.2부터는 그 세부 이유를 코드값(`detail`)으로 로그에 남긴다.

## 수정
- 변경 파일:
  - `supabase/functions/doit-understanding/index.ts` (v15.2)
  - `src/doit/components/feature/CoreConversation.tsx`
  - 검사 3개(아래)
- `doit-connect` 는 바꾸지 않았다(운영 버전 5와 같은 파일 `9c2bac14…`).
- 변경 내용(Gate를 끄거나 숫자를 바꾸지 않음. 특정 문장 예외·고정 질문 0):
  1. **첫 줄 근거 확인 대상 수정**
     - 화면에 실제로 나가는 것은 첫 줄 자체다. 그래서 첫 줄이 방금 답(또는 확인한 말)의 낱말을 담고 있으면 근거로 인정한다(`ackGrounded = basisGrounded || sharesWords(ack, …)`).
     - `sharesWords` 는 기존 앵커 검사가 쓰던 함수다. 어미 조각은 세지 않는다.
     - 지친 말·불만일 때의 첫 줄 규칙(되풀이 금지)은 그대로 둔다.
  2. **떨어진 이유를 정확히 알림**
     - 첫 줄을 버린 세부 이유를 다음 후보 요청에 그대로 알려 준다: 너무 김 / 물음표 / 틀 / 쓰지 않는 단어 / 반복 / 근거 없음.
     - 로그에는 코드값만 남긴다(`detail`).
  3. **빠져나갈 문**
     - 서버가 정한 새 갈래에서 다리를 **두 번** 못 만들면, 새 주제로 넘어가지 않는다. 대신 방금 답에 머무는 전략(EXPLORE_USER_MEANING)으로 마지막 후보를 만든다.
     - 연결·앵커·반복·거절·판정(목적·톤·정보) 검사는 그대로 모두 거친다.
     - 그래도 안 되면 지금처럼 명시적 실패를 알린다(고정 질문으로 덮지 않음).
- 영향: 다리 없이 새 주제로 넘어가는 질문은 여전히 나가지 않는다. 방금 답과 말 조각을 나누지 않는 질문도 여전히 떨어진다(not_anchored).

## 처음부터 시작하기
- 위치:
  - 대화 화면 진행 막대 바로 아래 알약 버튼 「처음부터 시작하기」
  - 전에는 기록이 1개 이상일 때만 보였다. 이제 **대화에 들어온 순간부터**(첫 답 전·오류 상태 포함) 보인다.
  - 맨 아래 바닥글에도 같은 버튼이 있다.
  - 다섯 칸이 끝난 화면에서는 끝 화면 안 버튼이 같은 일을 한다.
  - 「뒤로」(이전 화면)와는 다른 버튼이다.
- 확인(2단계): 「지금 대화를 여기서 끝내고 처음부터 다시 시작할까요? 지난 이야기는 지우지 않아요.」 → 「계속할게요」 / 「처음부터 시작할게요」
- 새 session 생성 방식:
  - 기존 회차 장치를 그대로 쓴다(DB 변경 0).
  - 로그인 정보 `user_metadata.doit_round_started_at` 에 새 시작 시각을 남기고, 고른 목적을 비운다(`startNewRound`).
  - 서버와 화면은 이 시각 이후의 기록만 이번 회차로 본다. 결과는 turn 0 → 첫 질문(목적 고르기)부터 시작이다.
- 초기화되는 상태(화면): 이번 회차 진행 수, 통합 카드 상태, 쉬어 가기 안내, 정정 대기, 정정 안내 여부, 떠 있던 질문, 고른 목적.
- 보존되는 데이터:
  - 지난 답 원문(`doit_records`), 이해·확인한 말(`doit_insights`), 대화 중 거절 기록(`followup_reject`), 요청 기록 — 삭제·수정 0건.
  - 지난 답은 화면의 「지난번 이야기」에서 다시 볼 수 있다.
  - 새 회차의 다음 질문 판단에는 새 회차 기록만 쓴다(기존 서버 계약). 거절한 의미는 회차와 상관없이 계속 막는다(v15.1 계약 그대로).

## 검사 (가짜 AI·가짜 서버 기준 — 실제 OpenAI·실기기 아님)
| | 내용 | 결과 |
|---|---|---|
| A | 대표 원문 "그냥 편한친구 부담없이"·새 갈래·basis 를 바꿔 말한 후보 → 다음 질문 | **PASS** (고친 코드 200 · 후보 1번) · 같은 입력으로 운영 v15.1 코드는 **502 재현** |
| A-2 | 첫 줄을 끝내 못 만드는 후보 → 두 번째 no_bridge 뒤 방금 답에 머묾 | PASS (답에서 이어지는 질문은 나감 · 무관한 질문·조각 없는 질문은 여전히 실패) |
| B | 다음 질문 오류 상태에서 「처음부터 시작하기」 | PASS (위쪽 버튼 보임·눌림 → 확인 → 새 회차 1회) |
| C | 정상 대화 중(첫 답 전 포함) 「처음부터 시작하기」 | PASS |
| D | 처음부터 시작 → 새 회차 시작 1회, 1차 선택만으로는 아무것도 안 바뀜 | PASS (화면 기준 · 목적 비우기 → 첫 질문은 기존 페이지 흐름) |
| E | 지난 원문 삭제 안 됨 | PASS (지우기·고치기 요청 0건 · 서버에 삭제 경로 없음) |
| F | 새로고침 뒤 새 회차 복원(시작 시각 이후 0개 → 첫 질문, 지난 답 남음) | PASS |

- 전체: 484개 중 479 통과 / 0 실패 / 미확정 5. 미확정 5개는 전과 같은 목록이다(휴리스틱 2·옛 흐름 3).
- 그 밖: type-check 0 · eslint 0 · 서버 엄격 타입 2개 0 · npm audit 0 · 앱 빌드 0 · 소스맵 0.
- 바꾼 옛 검사: 「새 갈래라도 첫 줄이 없으면 … 세 번 다 떨어지면 명시적 실패」.
  - 운영 실패를 만든 바로 그 동작을 지키고 있었다. 빠져나갈 문 기준으로 다시 썼다.
  - 「기록이 없으면 위쪽 버튼을 숨긴다」는 대표 결정과 반대라 뒤집었다.
- 실제 OpenAI·실기기로는 아직 확인하지 않았다.

## STOP
- 운영 재배포 필요 여부: **필요**
  - doit-understanding v15.2: 로컬 SHA-256 `fd2e1eee80ccf5648822245a074b935a6566ce831fd2c4933a163172cd3ffedd`, 186,093바이트. 운영은 버전 25(v15.1) 그대로다.
  - 앱 43차 ZIP 후보: SHA-256 `c1e666d5525f7fb274af1d0a19a981c165a919bd347d75ba210035a2618c9043`, 3,055,593바이트. 전달하지 않았다.
  - 두 가지 모두 v15.1 승인과 다른 새 변경분이다. 대표 승인 전에는 배포하지 않는다.
  - 순서: 서버 먼저, 앱 뒤. 서버만 올라가도 오늘 실패는 풀린다. 「처음부터 시작하기」 표시 변경은 앱이 올라가야 보인다.
- DB 변경 필요 여부: **없음** (DB·마이그레이션·RLS·Auth·Secret·모델·결제·가격·운영 데이터 변경 0)
- 되돌리기: 운영 버전 25 원본 = `PATCH-20260924-ai-conversation-v15` 커밋의 v15.1 파일(`b3384b79…`), 앱 = Netlify 배포 `6ab4f006…`.

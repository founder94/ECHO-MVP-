# ECHO FINAL100V4 · 최종 검증 보고 (2026-09-20)

실행한 검사만 적는다. 실행하지 않은 것은 "확인 불가"로 적는다.
이전 100회(v47 변경으로 중단된 14행)와 A/B/C 선행 게이트는 이 데이터셋에 합산하지 않았다.

## A. 기준점

| 항목 | 값 |
| --- | --- |
| test_run_id | final100v4 |
| 시작 / 종료 | 2026-09-20 03:20:51Z / 06:31:25Z (3시간 11분) |
| 제품 HEAD | d5a1551 (서버 소스 커밋 6ed213d) |
| get-step-question | v48 ACTIVE verify_jwt=on · ezbr df08ec7342c24c92… |
| echo-journey | v26 ACTIVE verify_jwt=on · ezbr 5bec72936a0ad37d… |
| 운영–동결 소스 SHA | 시작 5/5 일치 · 종료 5/5 일치 |
| 검사도구 SHA | run100v3 99498cce… · agg100v3 445775… · live 9116744… (시작=종료, 변경 없음) |
| 판정 규칙 | FINAL-3tier-20260920 |
| 자동 재시도 | AI_RETRY 미설정 · AI재시도 0회 (상태 파일 기록) |
| 제품 코드 변경 | 없음 |

## B. 운영 무결성

시작 버전 = 종료 버전(gsq v48 / ej v26), ezbr_sha256 동일, 운영본 내려받기 SHA 5/5 일치.
무결성 감시기가 5분마다 32회 대조해 전부 "동일". 동시 배포 감지 0건.
**판정: PASS**

## C. 실행 무결성

| 항목 | 값 |
| --- | --- |
| 계획 / 실제 시작 / 시작 실패 | 100 / 100 / 0 |
| 완주 | 80 |
| 중단 | 20 (NEVER_FINISHED 18 · BAD_JSON 2) |
| iteration 0~99 존재 | 예 (누락 0 · 중복 0) |
| conversation_id 매핑 | 100/100 (중복 conversation 0) |
| 유형 배분 | A~J 각 10회 |
| 유형별 완주 | A~F 10/10 · G 0/10 · H 0/10 · I 10/10 · J 10/10 |

**판정: PASS**

## D. 중단 → 재개

| 회차 | 상황 | 결과 |
| --- | --- | --- |
| 30회차 | 계획된 중단 실검사. 03:56:28 프로세스 종료 → 03:56:33 재기동 | i=30부터 재개 · 중복 0 · 누락 0 |
| 79회차 | 계획에 없던 검사 컨테이너 재시작(05:55, PID 1 생성 05:59:12) | 안전장치 작동 후 06:03:48 재개 · 중복 0 · 누락 0 |

79·80회차의 start 호출은 15밀리초 만에 응답 없이 끊겨 서버에 도달하지 못했다(실행되지 않은 회차).
`evidence-final100v4-notrun-rows.jsonl`로 분리 보존하고 성공으로 세지 않았으며, 같은 회차를 다시 열어 채웠다.
**판정: PASS**

## E. 서버 안정성

| 항목 | 값 |
| --- | --- |
| RATE_LIMITED | 1건(8회차) → 같은 회차 재개 후 정상 완주. 성공으로 세지 않음 |
| 401 / 403 / permission denied | 0 / 0 / 0 |
| 재로그인 | 1 |
| 클라이언트 timeout(http=0) | 2 (BAD_JSON 2건과 동일 사건) |
| 서버 오류(ERROR) | 0 |

**판정: PASS**

## F. NO_CANDIDATE

서버 로그 `no_candidate` 0건, 화면 NO_CANDIDATE 0건(gsq 0 / ej 0).
후보 차단은 556회 일어났지만 전부 재시도로 해결됐고, 원문 인용 구제(grounded_fallback)는 한 번도 필요하지 않았다.
`diversity_exhausted` 1건은 다른 앵커로 회복.
**판정: PASS**

## G. 정정 반영 (Correction Engine)

| 항목 | 값 |
| --- | --- |
| 조금 달라요(alittle) | 10회 중 10회 반영 |
| 직접 설명할게요(explain) | 10회 중 10회 반영 |
| 서버 correction_unreflected | 0건 |
| 화면 정정 무시 | 0건 |

이전 100회에서 FAIL이었던 #58 경로("돈보다 시간이 없는 게 더 힘들어요")는 이번 10회 전부 '시간'을 반영했다.
**판정: PASS**

## H. 직접 설명 우선

explain 10회 모두 사용자 문장을 그대로 인용해 다음 질문을 만들었고, 차단된 사례는 없다.
**판정: PASS**

## I. 거절 의미 (Rejected Semantic Block)

| 분류 | 건수 |
| --- | --- |
| REJECTED_MEANING_REAPPEARED (P0) | 0 |
| USER_SOURCE_AFTER_REJECTION (정상) | 1 |
| LEXICAL_REUSE_ONLY (관찰) | 20 |
| REVIEW_REQUIRED (수동 검토) | 5 |

거절 10회 모두 거절 문장에 내용어가 없었다("그게 아니에요. 제가 말한 건 그런 뜻이 전혀 아니에요").
그중 7회는 서버가 "제가 아직 제대로 이해하지 못한 것 같아요"로 안전하게 되물었다(서버 로그 understanding_unresolved 7).
**판정: P0 기준 PASS · REVIEW_REQUIRED 5건은 수동 검토 대기**

## J. 질문 품질

| 항목 | 값 |
| --- | --- |
| 되물음 턴 | 486 |
| 사용자 질문 무시 | 0 |
| asked_reply_failed | 24건 전부 basis=1 (basis=0 = 0) |
| 빈 질문 / 빈 화면 | 0 / 0 |
| 반말 | 0 |
| 잘린 응답 | 0 |
| 같은 질문 반복(표시) | 84 |

**판정: P0 기준 PASS · 반복 질문은 목표 미달(관찰)**

## K. AI 오류

| 항목 | 값 |
| --- | --- |
| AI_ERROR | 0 |
| openai_fetch_error / http / empty / truncated | 0 / 0 / 0 / 0 |
| BAD_JSON | 2 |
| attempts_deadline | 156 (재시도로 전부 회복) |

BAD_JSON 2건은 서버가 질문을 생성·저장한 뒤 클라이언트가 응답을 받지 못한 사건이다(http 상태 없음).
두 대화 모두 서버에 살아 있고 질문이 저장돼 있어, 사용자가 "다시 시도하기"를 누르면 이어진다.
제품 파싱 결함 증거는 없다(schema_fail 0, truncated 0).
**판정: 제품 원인 BAD_JSON = PASS · 사용자 화면 실패 2건은 "원인 확인 불가"로 유지**
(2026-09-20 정정: 앞서 "인프라 계열"로 단정했으나, 클라이언트가 응답을 받지 못한 원인을 확정할 증거가 없다.
확정된 것은 서버가 정상 처리를 완료했다는 사실과 제품 코드 결함 증거가 없다는 사실뿐이다.)

## L. 상태머신

상태 역행 0 · 단계 건너뜀 0 · 중복 저장 0 · 되물음 후 STEP 증가 0 · request_token 이상 0.
되물음 시 단계를 올리지 않는 규칙이 488회 작동했다(answer_hold).
**판정: PASS**

## M. 모든 FAIL / 관찰 사례

### FAIL로 집계한 2건 (사용자 화면 실패)

| iteration | case | conversation_id | 사용자 입력 | 결과 | 원인 |
| --- | --- | --- | --- | --- | --- |
| 61 | G | 49c9ebfd-d243-48d2-bd1e-d533aaa6733d | "왜 그렇게 생각했어?" | 응답 수신 실패 | **원인 확인 불가.** 확정 사실: 서버가 질문 생성 완료(04:36:41.898 question_ready attempts=3 ai_ms=6519)·DB 저장 완료(04:36:42.041), 해당 요청의 200 응답 기록 없음, OpenAI 오류 0 |
| 78 | H | e66318d4-0080-44da-b33c-b9fdbc4cc355 | 되물음 진행 중 | 응답 수신 실패 | **원인 확인 불가.** 확정 사실: 같은 초(05:55:22)에 다른 두 요청도 15ms 만에 실패, 서버는 05:55:27까지 메시지 저장 지속 → 검사 환경 측 종료 정황이 있으나 로컬 로그가 남지 않아 확정 불가 |

두 대화 모두 서버 상태 step1 유지, 메시지 3건·39건 저장됨 → 재시도로 복구 가능.

### 완주하지 못한 18건 (G·H 그룹)

G(AI 판단 질문)·H(해결방법 되물음) 유형은 26턴 내내 사용자가 질문만 던지는 시나리오다.
서버는 규칙대로 되물음에 단계를 올리지 않으므로(answer_hold) 여정이 끝나지 않는다.
사용자 질문 무시 0 · 되물음 후 STEP 증가 0이 이를 뒷받침한다. 제품 결함이 아니라 시나리오 특성이다.

## N. REVIEW_REQUIRED 5건 (대표 수동 검토)

5건 모두 E(강한 거절) 그룹이고 모양이 같다. 거절 이후 사용자가 직접
"일이 너무 많아서 잠을 못 자요" · "돈이 제일 크게 걸려요" · "주말에도 일 생각이 나요"를 새로 입력했고,
이후 화면은 그 사용자 원문을 근거로 물었다. 자동 검사에 걸린 낱말은 '주무시(고)' · '드시' · '고민'처럼
AI 어투나 일반 표현이라 의미 동일성을 기계가 확정할 수 없었다.

| iteration | conversation_id | 걸린 낱말 | 이후 화면 |
| --- | --- | --- | --- |
| 41 | 1ae2042f-d75d-4ccc-913c-5ce92479f10e | 드시 | "일 생각이 나실 때 어떤 기분이 드시나요?" |
| 42 | 83ed23e0-09e2-46dc-9dc4-bead97ebf875 | 주무시 | "요즘 돈 걱정이 많고, 일이 많아 잠을 잘 못 주무시며…" |
| 43 | 75ad68a7-b270-46e2-a16c-cbc5cbad41c3 | 주무시고 | "요즘 일이 많아 잠을 잘 못 주무시고, 돈 문제로 스트레스를…" |
| 48 | 5db21713-2e3b-4efa-8741-608c6de50680 | 주무시고 | "요즘 일이 많아서 잠을 잘 못 주무시고, 돈 문제로 고민이…" |
| 49 | 1b8ff8cd-407b-4535-81d8-82c0f014b846 | 고민 | "돈 문제와 관련해 어떤 고민이 있으신가요?" |

### 2026-09-20 수동 판정 결과 (대화 원문 전체 대조)

| iteration | 판정 | 근거 |
| --- | --- | --- |
| 41 | PASS | 거절 직후 서버가 "제가 아직 제대로 이해하지 못한 것 같아요"로 물러섬(새 의미 주장 0). 걸린 '드시'는 "어떤 기분이 드시나요"의 어미. 이후 '돈' 언급은 사용자가 STEP 3 답변으로 '돈이 제일 크게 걸려요'를 직접 다시 입력한 뒤 |
| 42 | PASS | 거절된 AI 의미는 "돈 문제로 **인해** 더 큰 스트레스"라는 인과·강도 해석. 재등장 지목 문장 "돈이 가장 큰 고민"은 사용자 원문 '돈이 제일 크게 걸려요'(STEP 2 답변)와 마음 날씨 '돈 걱정이 많아'의 재진술. 거절된 인과 주장은 사라졌고 새 정보 '주말에도 일 생각이 나요'가 반영됨 → 사용자 원문에 새 근거가 생긴 경우 |
| 43 | PASS | 거절된 의미는 "일이 많아서 → **그로 인해** 돈 문제도 걱정"이라는 인과. 새 요약은 인과를 빼고 병렬 서술 + 새 정보 추가. 근거 전부 사용자 원문 |
| 48 | 목표 미달 | 거절된 "돈 문제로 **스트레스**"와 새 요약 "돈 문제로 **고민**이 크심"은 의미가 같고 단어만 바뀜. 근거는 전부 사용자 원문이라 P0(AI 추론 재등장)는 아니지만, 거절 직후 실질 동일 요약을 다시 제시한 것은 사용자 체감 기준 미달 |
| 49 | 목표 미달 | 거절된 요약의 표현 '고민'을 거절 직후 첫 질문("돈 문제와 관련해 어떤 고민이 있으신가요?")의 전제로 그대로 사용. 근거는 사용자 '돈이 제일 크게 걸려요'라 P0는 아니며, 다음 요약에서 "아직 제대로 이해하지 못한 것 같아요"로 회복 |

**합계: PASS 3 / FAIL 0 / 목표 미달 2 · REJECTED_MEANING_REAPPEARED(P0) 최종 0건**

공통 관찰: 검사 시나리오의 거절 문장에 내용어가 없어("그런 뜻이 전혀 아니에요") 서버가 무엇이 틀렸는지 특정할 수 없었다.
10건 중 7건은 안전 응답으로 물러섰고 3건은 새 요약을 만들었다. 이 일관성 차이가 48·49의 목표 미달 원인이다.

## O. 성능 (이전 100회와 비교)

| 지표 | 이전 100회 | final100v4 |
| --- | --- | --- |
| p50 | 3,277ms | 3,239ms |
| p75 | 6,830ms | 5,510ms |
| p95 | 10,029ms | 8,325ms |
| 최대 | 13,821ms | 12,976ms |
| 6초 초과 | 31.6% | 21.8% |
| 9초 초과 | 14.3% | 1.9% |
| 10초 초과 | 5.0% | 0.6% |

악화 없음. 목표(p95 6초 이하)에는 미달.
**판정: 목표 미달 (악화 아님)**

## P. A/B/C 선행 게이트 (별도 데이터셋, 합산 안 함)

| 단계 | 회수 | 결과 |
| --- | --- | --- |
| A #58 정정 재현 | 10 | 정정 무시 0/10 |
| B NO_CANDIDATE 5종 ×3 | 15 | 동일 원인 NO_CANDIDATE 0/15 |
| C 캐너리 10종 | 10 | 질문 무시 0 · 빈 화면 0 · 반말 0 · 정정 무시 0 |

차이: 선행 게이트는 gsq v46에서, final100v4는 복구 후 v48에서 실행했다. 두 버전의 소스 SHA는 바이트 단위로 동일하다.

## Q. 제품 코드 변경

검사 기간 중 변경 없음. DB·RLS·Secret·모델·가격·결제·다른 Edge Function·Netlify 변경 없음.

## R. 최종 판정

| 영역 | 판정 |
| --- | --- |
| 서버 안정성 | PASS |
| NO_CANDIDATE | PASS |
| Correction Engine(정정) | PASS |
| 직접 설명 우선 | PASS |
| Rejected Semantic Block(거절) | PASS (수동 검토 5건 판정 완료: PASS 3 / 목표 미달 2 / P0 0) |
| 질문 품질 | PASS (반복 질문 84건은 목표 미달) |
| 상태머신 | PASS |
| 중복 방지 | PASS |
| 중단 → 재개 | PASS |
| RATE_LIMITED 처리 | PASS |
| 성능 | 목표 미달 (악화 없음) |
| final100v4 전체 | PASS — 단, 사용자 화면 실패 2건(원인 확인 불가)과 거절 처리 일관성 목표 미달 2건이 남아 있다 |

정확한 표현: **현재 동결 서버 기준 ECHO AI 핵심 검증 final100v4 PASS.**
실기기 UX, 전체 사용자 흐름, 운영 사용자 검증은 아직 아무도 해보지 않았다.

## S. 남은 문제

1. BAD_JSON 2건 — 응답 수신 실패, **원인 확인 불가**. 화면은 오류 + "다시 시도하기". 자동 재시도 정책은 별도 결정 대상.
2. ~~`callOpenAI()` 자체 timeout 부재~~ — **2026-09-20 정정: 잘못된 보고였다.** 운영 v48 실제 코드 대조 결과
   `OPENAI_TIMEOUT_MS = 6_000`, `OPENAI_TIMEOUT_MAX_MS = 9_000`, `AbortController` + `setTimeout(() => ctrl.abort(), timeoutMs)`,
   `fetch(..., { signal: ctrl.signal })`, `clearTimeout(timer)`가 모두 존재한다(ai.ts 40·66·74~76·91·122행).
   echo-journey v26도 동일(`QUESTION_TIMEOUT_MS = 6_000`, 리포트 전용 `REPORT_TIMEOUT_MS = 20_000`, AbortController·signal 적용).
   타임아웃은 구현되어 있으며, 이번 100회에서 OpenAI 오류 0건인 것도 이와 모순되지 않는다.
3. 내용 없는 거절 뒤 처리 일관성 — 10건 중 7건만 안전 응답으로 물러섰다(48·49 목표 미달).
4. 반복 질문 84건 — 배포를 막는 P0/P1 아님(아래 근거), 품질 목표 미달.
   유형별: D 7 · F 5 · G 30 · H 42. 이 중 72건은 26턴 되물음 시나리오(G·H)에서,
   12건은 정정 전후 요약이 두 번 제시되는 D·F 구조에서 났다. 여정 차단 0 · 빈 화면 0 · 사용자 질문 무시 0.
5. 03:05 운영 gsq v47 변경의 배포 주체 — 확인 불가.

## T. 대표가 지금 해야 할 일

**내용 없는 거절("그게 아니에요"만 누르고 설명을 안 한 경우) 뒤에 서버가 어떻게 해야 할지 한 가지만 정해 주세요.**
지금은 10번 중 7번은 "제가 아직 제대로 이해하지 못한 것 같아요"로 물러서고, 3번은 사용자 원문으로 요약을 다시 만듭니다.
둘 중 어느 쪽으로 통일할지 정해 주시면 그다음 작업 범위가 정해집니다. (코드는 아직 건드리지 않았습니다.)

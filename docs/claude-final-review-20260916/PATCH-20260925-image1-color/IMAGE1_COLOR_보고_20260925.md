# 대화 화면 글씨색·배경색 = 대표 이미지 그대로 (2026-09-25)

근거: 대표 「글씨색을 1번 이미지색으로 하고 배경톤도」 → 「비교하지 말고 글씨색과 배경색 내가 올린 이미지 그대로 반영해 통일시켜. 질문 구간부터야. 글씨색이랑 배경만」.
로컬 수정·로컬 검사만. 운영 배포 0.

## 먼저 밝히는 것 (내 실수)
- 처음 배경 색을 잴 때 이미지에 붙은 색 설명(ICC — 휴대폰 화면용 넓은 색 영역)을 무시하고 날값을 읽었다. 그래서 실제보다 흐린 색(예: 맨 위 왼쪽 #6ce2ad)을 옮겼다.
- Chrome 이 화면에 그리는 방식(색 변환 적용)으로 다시 재어 바로잡았다(같은 자리 #43e0a9). 앞서 말한 "평균 차이 5.4"도 이 잘못된 값 기준이었으므로 무효다.

## 바꾼 것 (파일 1개: `src/doit/components/feature/core-conversation.css`, 대화 경로 루트 `.echo-dialogue--pastel` 아래만)
1. **배경색**: 대표 이미지를 가로 5 × 세로 11 칸으로 재어(칸마다 가운데 값, 흰 글자·빛 방울 제외) 줄마다 가로 그라데이션으로 칠하고 40px 흐림으로 이었다. 값 = `배경색_11x5_sRGB.json`. 바깥 사진·주소 0.
2. **글씨색**: 이미지 화면의 원래 코드(`src/pages/home/components/HeroSection.tsx`) 값 그대로.
   - 제목·질문·내가 남긴 말·카드 제목·입력 글자 = 흰색 (`text-white`)
   - 안내·보조 글 = 흰색 70% (`text-white/70`, 「내 마음을 알면, 내가 보인다.」)
   - 머리말(「첫 질문」·「대화 중」) = 흰색 55% (`text-white/55`, 「ECHO」)
   - 선택한 목적 카드 설명 = 흰색 90% (`text-white/90`, 「진짜 나를 찾아줘」)
   - 글자 뒤 검은 그림자 = 없앰(이미지 글자에 그림자 없음). 진행 막대의 검은 그림자도 없앰, 채움은 흰색.
3. 실제 화면에서 보이는 글씨를 모두 읽어 확인(가짜 서버, 7가지 상태 × 360·390px): 흰색 / 흰색 70% / 흰색 55% 세 가지뿐.
   - 예외 1개: 밝은 은색 주요 버튼(「계속할게요」 등) 안의 어두운 글씨 — 버튼 바탕이 밝아 흰 글씨를 쓰면 안 보여 그대로 뒀다.

## 바꾸지 않은 것
글꼴·크기·굵기·문구·배치·버튼 모양·테두리 · 히어로·홈페이지·우주인 배경 · 시작 흐름 · 서버·Agent·Prompt·DB·Sound·결제.
남은 차이: 이미지는 굵은 Pretendard 글꼴, 대화 화면은 고운바탕·고운돋움 — 글꼴 변경은 대표 승인 전이라 손대지 않음.

## 검사 (모두 가짜 서버 기준 · 실기기 아님)
| 항목 | 결과 |
|---|---|
| type-check · lint · build:app · build:brand · npm audit | 0 · 0 · 0 · 0 · 취약점 0 |
| 전체 검사 | 530개 중 525 통과 / 0 실패 / 미확정 5 (전과 같은 목록) |
| 기능 회귀 클릭(360·390·430) | 60/60 |
| 히어로 픽셀 (360·390·430·1440) | 다른 픽셀 0 |
| 화면 오류 | 0 |

- 대비(정직하게): 밝은 파스텔 위 흰 글씨라 표준 대비 4.5:1 에는 닿지 않는다. 이미지 화면도 같은 조건이다.

## 앱 ZIP 후보 (전달 전 · 운영 배포 0)
- `1_APP_여기에올릴것_doitmobile.zip` SHA-256 `67ca0997ff7d8c8da32addb6cdda82662592705da080e04017e2708d8cedbaeb` · 3,056,364바이트 · 파일 144개(폴더 항목 제외) · 풀어서 빌드 폴더와 차이 0 · 소스맵 0 · .env 0
- 앞선 후보 `7c9ad608…` 를 대체한다. 홈페이지 ZIP 필요 없음.
- **대표 실기기 미확인 — "완료" 아님. Conversation P0 = FAIL 유지.**

## 캡처
`screens/390-질문.png` · `screens/390-첫질문.png`

## 추가 (같은 날) — 글씨를 흰색 하나로 통일
근거: 대표 「비교해도 안 맞아. 그냥 이미지 글씨색 똑같이 하면 된다」.
- 이미지 속 글씨를 직접 재었다(Chrome 색 변환 적용, 글자 속 가장 밝은 3% 가운데 값): 제목 `#ffffff` · 「시작하기」 `#ffffff` · 「진짜 나를 찾아줘」 `#f1fcf7` · 「내 마음을 알면…」 `#f5f9dc` · 「로그아웃」 `#eaffff`.
- 그래서 대화 화면 글씨를 **모두 `#fff` 하나**로 맞췄다(앞서 넣은 흰색 90·70·55% 구분 삭제). 검은 그림자 없음은 그대로.
- 예외 2개: 입력창 안 흐린 안내 글씨(「생각나는 대로 한 줄」)는 적은 글과 구분되게 흰색 70% · 밝은 은색 주요 버튼 안 어두운 글씨는 그대로.
- 화면에서 실제로 읽은 글씨 색(가짜 서버, 7상태 × 360·390px): `rgb(255,255,255)` 하나 + 은색 버튼 안 글씨.
- 검사(가짜 서버 기준): 530개 중 525 통과 / 0 실패 / 미확정 5 · type-check·lint·build 2·audit 0 · 기능 회귀 60/60 · 히어로 픽셀 차이 0.
- **앱 ZIP 후보 교체**: `42df9c26535dcdacb11f6881cfc7c04c2254838bbaa155b0c061544a9900c8ab` · 3,056,327바이트 · 파일 144개 · 풀어서 빌드 폴더와 차이 0 · 소스맵 0 · .env 0 (앞선 `67ca0997…` 대체).
- 캡처 `screens/` 두 장은 이 판으로 다시 찍었다. **대표 실기기 미확인 — "완료" 아님.**

# ECHO PASTEL UI · 운영 배포 기록 (2026-09-25)

근거: 대표 「PASTEL CONVERSATION UI · 운영 배포 승인」 — 승인 대상 = 앱 ZIP `42df9c26…` 하나, 대상 사이트 = doitmobile 하나.
이번 승인은 UI 배포만 포함한다. Agent v1.1 서버·Prompt·Model 배포는 포함하지 않는다.

## 1. 배포 전 마지막 대조 (2026-09-25, 이 환경에서 실제 파일로 확인)
| 항목 | 값 |
|---|---|
| 파일명 | `1_APP_여기에올릴것_doitmobile.zip` |
| SHA-256 | `42df9c26535dcdacb11f6881cfc7c04c2254838bbaa155b0c061544a9900c8ab` = 승인 지문과 일치 |
| 크기 | 3,056,327바이트 |
| 파일 수 | 144개(폴더 항목 제외) |
| .env | 0개 |
| 소스맵 | `.map` 파일 0개 · `sourceMappingURL` 0곳 |
| 비밀키 | 0개. `sk-` 0곳 · `service_role` 0곳 · `PRIVATE KEY` 0곳 · `OPENAI_API_KEY` 0곳. `sb_secret_` 1곳이 걸렸지만 키 값이 아니다. supabase 로그인 부품 코드가 키 종류를 앞글자로 가리는 비교문 `startsWith('sb_secret_')` 이다. 뒤에 붙은 글자는 0자다. |

- 새로 빌드하지 않았다. 승인된 파일 그대로다.

## 2. 대상 · 되돌리기
- 대상: Netlify `doitmobile`. 사이트 번호 `ff078012-fe79-4108-a210-201554ab0dea`, 주소 https://app.do-it.company
- **배포 전 운영 배포 번호(되돌릴 곳) = `6ab52bc9ddf4404ca8d43575`**
  - 상태: ready · 수동 drop
  - 게시: 2026-09-24T13:55:51Z = 22:55 KST
  - 기록: "94 new files"
  - 앱 44차로 추정
- 되돌리는 방법(대표):
  1) app.netlify.com/projects/doitmobile 을 연다.
  2) 위 메뉴 「Deploys」를 누른다.
  3) 목록에서 `6ab52bc9…` 줄을 누른다.
  4) 「Publish deploy」를 누른다.

## 3. 배포 시도 결과 — 이 환경에서 올릴 수 없음
- 이 작업 환경의 바깥 인터넷 규칙이 Netlify 연결을 막는다.
  - 막힌 곳: `api.netlify.com` · `doitmobile.netlify.app` · `app.do-it.company`
  - 기록: `$HTTPS_PROXY/__agentproxy/status` → `connect_rejected` · `gateway answered 403 to CONNECT`
  - 확인 시각: 2026-09-24T19:09Z
- Netlify 도구(MCP)는 읽기만 된다. 올리는 기능은 이 막힌 주소로 명령을 돌리는 방식이라 같은 이유로 실패한다(2026-09-22 기록과 같다).
- 그래서 **업로드는 대표 수동**이다. 승인된 ZIP 을 그대로 대표에게 전달했다.

## 4. 배포 후 확인 (대표 업로드 뒤에 채운다)
| 항목 | 결과 |
|---|---|
| 새 배포가 doitmobile 에 올라갔는지 | 대기 |
| 운영 주소가 새 배포를 가리키는지 | 대기 |
| 첫 화면·히어로 동일 | 확인 불가. 이 환경에서 앱 주소 403 |
| 대화 파스텔 배경 · 흰 글씨 · 입력/버튼/스크롤 · 콘솔 오류 | 확인 불가. 앱 주소 403 → 대표 실기기 |
| Galaxy / iPhone | 미실행 |

- 배포를 올려도 표준 대비(WCAG) 4.5:1 미달이라는 측정 사실은 그대로다.
  - 근거: `PATCH-20260925-text-legibility-stop` 보고서
  - 실기기에서 읽기 어려우면 FAIL 로 보고한다. 임의로 고치거나 다시 배포하지 않는다.
- **Conversation P0 = FAIL 유지.** Agent v1.1 실제 OpenAI 검증 전이다.
- 변경 0건: doit-understanding · Prompt · Model · DB/RLS/Auth/Secret · 결제 · KEY (홈페이지는 4-1 대표 결정으로 추가)

## 4-1. 홈페이지 추가 (대표 결정 2026-09-25)
- 대표가 「홈페이지는 왜 안 줘?」라고 물었다. 확인해 보니 운영 홈페이지가 옛 히어로 그대로였다.
  - 운영 홈페이지 do-it.company 를 실제로 내려받아 확인했다.
  - index.html SHA-256 `9cc7c890…` = 36차 그대로다.
  - 옛 히어로 문구 「지금 시작하기」·「사람은 프로필보다」가 있다. 새 히어로 문구 「편해지는 사람은」·「ECHO 시작하기」는 0곳이다.
  - Netlify `echo-mvp-doit` 현재 배포는 `6ab43fd08df24f4393e069b6` 이다(게시 2026-09-23T21:08:57Z = 9/24 06:08 KST).
- **내가 놓친 것**: 9/24 에 확정한 홈페이지 ZIP `061f7397…` 은 앱 44차와 함께 전달했지만 운영에 올라가지 않았다. 앱 업로드를 확인할 때 홈페이지도 대조했어야 했다. 그래서 지금은 앱 첫 화면만 새 히어로이고 홈페이지는 옛 히어로다.
- 대표 선택 「지금 같이 줘」 → 기존 홈페이지 ZIP 을 새로 빌드하지 않고 그대로 전달했다.
  - 파일: `2_BRAND_여기에올릴것_echo-mvp-doit.zip`
  - SHA-256: `061f7397e6ee5811180ba91d263a3881d4970e15ede307912f9865a0c80399f0`
  - 크기·개수: 2,953,610바이트 · 파일 101개
  - 비밀 정보: .env 0 · 소스맵 0 · 비밀키 0
  - 내용: 새 히어로 문구가 들어 있고, 대화 파스텔 코드는 0곳이다.
- 홈페이지 되돌릴 곳 = `6ab43fd08df24f4393e069b6`

## 5. 배포 후 기록 (대표 업로드 뒤에 채운다)
- **대표 수동 업로드 확인(Netlify 읽기 · 2026-09-25)**
  - doitmobile 현재 배포 = **`6ab576b13f833ae2d78b395a`**
    - 상태 ready · 수동 drop · production
    - 만든 시각 2026-09-24T19:14:57Z, 게시 19:15:25Z (= 9/25 04:15 KST)
    - 운영 주소 app.do-it.company 가 이 배포를 가리킨다.
  - 이전 배포 `6ab52bc9ddf4404ca8d43575` 는 교체됐다(되돌릴 곳).
  - Netlify 기록 「94 new files uploaded」와 대조했다.
    - 승인 ZIP `42df9c26…` 144개 파일 가운데 44차 ZIP `a8d4e790…` 에 없던 내용(sha1 기준)은 정확히 **94개**다.
    - 올라간 것이 승인 ZIP 이라는 강한 근거다. 다만 파일 내용을 직접 대조하지는 못했다(앱 주소·배포 고유 주소·Netlify 화면 사진 주소 모두 이 환경에서 403).
- 홈페이지 `echo-mvp-doit` = `6ab43fd08df24f4393e069b6` 그대로. 홈페이지 배포 0건.
  - 대표가 같은 승인서(「Homepage/Brand 사이트 배포 금지」)를 다시 보냈다. 그래서 4-1 의 홈페이지 ZIP 은 올리지 않는 것으로 되돌렸다. 대표가 따로 말하기 전까지 올리지 않는다.
- 화면 확인(히어로 동일 · 파스텔 · 흰 글씨 · 입력/버튼/스크롤 · 콘솔 오류)은 **확인 불가**다. 이 환경에서 앱 주소가 403 이다. 대표 Galaxy·iPhone 실기기로 판정한다.

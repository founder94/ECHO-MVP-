# ECHO Signature Sound — 실측 결과와 보류 기록 (2026-09-24)

근거: 대표 PDF 「SIGNATURE SOUND · HOMEPAGE + MOBILE WEB · FINAL UX LOCK」.
이후 대표 「CONVERSATION BACKGROUND ONLY」 §12 에 따라 **음악은 별도 작업 후보로 보류**했다.

- 앱 코드·빌드·ZIP 에 음악 변경은 0이다.
- 이 폴더는 기록만 담는다.
- 만든 코드 사본은 `parked-code/*.txt` 에 있다. 적용하지 않았다.

## §20 구현 전 실측

| 항목 | 원음 1 | 원음 2 |
|---|---|---|
| 파일 식별자 | `…65f5bc59-e582-437a-97af-88cc38b6d259.mp3` | `…02c258b7-43c4-40c2-b6f1-ac72d7d5be8d.mp3` |
| 크기 | 1,142,136바이트 | 1,813,428바이트 |
| SHA-256 | `e38d47dac045590effdb02e0a1c82281d3ac85d508c17da0e11b3c4f9f335afe` | `caa47c3da81fa470ec3d3623282ffcf747f792e3d43f536e441b8f032f68e13d` |
| 길이(브라우저 재생 정보 / 풀어서 잰 값) | 28.5534초 / 28.578초 | 45.3357초 / 45.375초 |
| 형식 | MP3 · 44.1kHz · 2채널 · 약 320kbps | 같음 |
| 최대 크기 · 잘림 | 0.978 · 잘린 표본 0 | 0.9926 · 0 |
| 앞·뒤 무음 | 0.718초 · 0.871초 | 0.674초 · 0.581초 |
| 태그(ID3) | 없음 | 없음 |

- **남/여 대응: 확정 불가.**
  - PDF 가 말한 `MUSIC_PATHS.female/male` 코드는 확인 가능한 모든 곳에서 0건이었다.
    - 저장소
    - 작업 공간
    - 받은 ZIP·문서 전부
  - 파일 태그도 없다.
  - 예전 코드(`OriginalMusicCard.tsx`)도 「원음 1 / 원음 2」로만 부르고 성별을 추정하지 않는다.
- **위치**
  - 원본은 대표가 이 세션에 올린 파일로만 있다.
  - 저장소 안에는 없다. 저장소가 공개라 올리지 않았다.
- **바깥 주소 의존: 있음.**
  - 지금 홈페이지·앱 첫 화면 이야기 09 의 `OriginalMusicCard` 가 `storage.helloreaddy.io` 두 주소에서 음원을 받는다.
  - 사용자가 눌러야 재생된다. 미리 받기는 metadata 만 한다.
  - 이 환경에서는 그 주소가 403 이다.
- **자동재생: 홈페이지 0**
  - 기존 카드는 누를 때만 소리가 난다.
- **「ECHO 시작하기」 위치**
  - `src/components/DoItBrandHero.tsx` 의 `.doit-brand-start` 이다.
  - 앱에서는 `landing/page.tsx` 의 `handleStart` 가 `/doit/start-journey` 로 보낸다.
  - 홈페이지에서는 앱 주소로 넘어간다(다른 주소라 사용자 동작이 이어지지 않는다 → 앱에서 첫 재생은 브라우저가 막을 가능성이 크다).
- **대화 경로**
  - `/doit/conversation` 이다(`ConversationOpening` → `CoreConversation`).
- **Relationship Open**
  - 코드는 있다(`doit-connect` 「서로 열렸어요」).
  - 운영 읽기(2026-09-24 15:05Z): 연결 0 · 승인 0 · 전화 인증 0.
  - 그래서 Part B 는 **DISABLED** 대상이다.
- **분석 이벤트**
  - `src/doit/lib/analytics.ts` 는 꺼진 채 설치돼 있다.
  - 허용 목록에 sound_* 는 없다. 추가하지 않았다.

## 보류한 구현안 (적용 0 · `parked-code/`)

- 제어기 `signatureSound.ts`
  - Part A = 원음 1. 짧아서 입장곡으로 골랐다. 대표 확인이 필요하다.
  - 반복 없음. 미리 받기 없음(`preload=none`).
  - 「ECHO 시작하기」 클릭 안에서 사파리 허락만 받는다(틀자마자 멈춤, 소리 0).
  - 대화 화면이 열리면 한 번 재생한다.
  - 새로고침·그냥 다시 들어옴에는 재생하지 않는다.
  - 끄면 이 기기에 기억한다.
  - 화면을 떠나거나 앱이 뒤로 가면 멈춘다.
  - 재생이 막히면 「소리 켜기」만 남긴다.
  - 볼륨 숫자·성별 선택·Part B 는 0이다.
- 소리 버튼 `SoundControl.tsx`
  - 44px 크기다.
  - `aria-pressed` · `aria-label` 을 넣었다.
- 가짜 오디오 검사 12개(`signature-sound.test.mjs`)
  - 보류 당시에는 돌리지 않고 파일만 있었다.
- 다시 시작하려면 대표 승인이 필요하다. 결정할 것은 아래와 같다.
  - Part A 곡
  - 원음을 사이트 안에 둘지(공개 저장소 문제)
  - 홈페이지 카드의 바깥 주소를 바꿀지(홈페이지 변경)

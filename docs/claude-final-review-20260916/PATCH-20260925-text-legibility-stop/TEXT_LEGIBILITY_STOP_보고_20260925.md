# ECHO TRANSPARENT SURFACE · TEXT LEGIBILITY 보고 — 목표 동시 충족 불가 · STOP (2026-09-25)

근거: 대표 「TRANSPARENT SURFACE · TEXT LEGIBILITY FINAL PATCH」(로컬 수정/검사만 · 운영 배포 금지).

- **제품 코드는 0건 변경했다.**
  - 테두리·그림자 후보는 검사용 CSS 로만 화면에 덧씌워 비교했다.
  - 결론이 「동시 충족 불가」라 제품에는 넣지 않았다(지시 8번).
- 투명 판(지금 상태)은 그대로다.
  - 검정·회색 바탕 0 · 흐림 0 · 배치 변경 0
- 운영 배포 0, 서버·Agent·Sound·홈페이지 변경 0이다.

## 비교한 것 (파스텔 대화 화면의 판 안 글씨에만 덧씌움)

- 대상 selector: `.echo-dialogue.echo-dialogue--pastel :is(textarea, .echo-secondary, .echo-restart-pill, .echo-opening-tile, .echo-brief, .echo-done, .echo-synthesis, .echo-restart, .echo-pause, .echo-insight, .echo-editor, .echo-reactions button:not(:first-child))`
  - 입력창 안내 글에는 `textarea::placeholder` 로 그림자만 준다. 안내 글에는 테두리가 적용되지 않는다.
- 모든 단계에 `paint-order: stroke fill` 을 썼다. 테두리를 글자 뒤로 그려 글자 모양을 덜 깎는 설정이다.

| 단계 | text stroke | shadow |
|---|---|---|
| S0(지금) | 없음 | 없음 |
| K1 | 0.3px rgb(0 0 0/.6) | 없음 |
| K2 | 0.5px rgb(0 0 0/.75) | 0 0 1px rgb(0 0 0/.5) |
| K3 | 0.75px rgb(0 0 0/.85) | 0 0 1px rgb(0 0 0/.6) |
| K4 | 1px #000 | 0 0 2px rgb(0 0 0/.7) |
| K5(자막 비교용) | 1.5px #000 | 0 0 2px #000 |

## 측정 방법 (실제 캡처 · 가짜 서버 기준 · 실기기 아님)

- 조건: 휴대폰처럼 화면 배율 3배, 360·390·430px, 5개 상태.
  - 상태: 목적 카드 선택 · 질문 · 입력 중 · 맨 아래(입력창·하단 카드·처음부터) · 오류/재시도
- 판 안 글자 82곳을 쟀다. 한 곳마다 캡처를 네 장 찍었다.
  - 그대로 · 글자 칠만 지운 것 · 글자·테두리·그림자를 모두 지운 것 · 글자를 자홍색으로 칠한 것
- ① **표준(WCAG) 대비**: 글자색과 글자 뒤 파스텔(테두리·그림자 제외)을 비교한다. 글자 둘레 1~3기기픽셀 중 하위 10% 값이다.
- ② **테두리 기준 대비**: 글자색과 글자 바로 둘레 1기기픽셀(테두리·그림자 포함)을 비교한다. 하위 10% 값이다. WCAG 공식 계산은 아니고, 글자 가장자리가 얼마나 또렷한지 보는 참고치다.
- 구간은 글자 뒤 바탕의 평균색으로 나눴다(민트 / 노랑 / 코랄).

## 결과

| 단계 | 민트 표준 최저 | 노랑 표준 최저 | 코랄 | 테두리 기준 4.5 미달(82곳 중) |
|---|---|---|---|---|
| S0 지금 | 1.03 | 1.00 | 표본 0 | 73 |
| K1 0.3px | 1.03 | 1.00 | 표본 0 | 73 |
| K2 0.5px | 1.03 | 1.00 | 표본 0 | 73 |
| K3 0.75px | 1.03 | 1.00 | 표본 0 | 73 |
| K4 1px + 2px | 1.03 | 1.00 | 표본 0 | 15 |
| K5 1.5px | 1.03 | 1.00 | 표본 0 | 15 |

- **표준(WCAG) 대비는 어떤 단계에서도 1.00~1.43 으로 그대로다.**
  - 테두리·그림자는 글자와 배경의 대비를 바꾸지 않는다.
  - 흰 글씨 + 밝은 파스텔 + 투명 판 조합으로는 4.5:1 에 **닿을 수 없다.**
- **테두리 기준**으로는 K4(검은 1px 테두리 + 2px 그림자)에서야 대부분 4.5 를 넘는다.
  - 목적 카드 제목 11.37 · 설명 7.45 · 하단 카드 6.65 · 입력 글자 10.30 · 안내 글 8.51
  - K3 까지는 전부 미달이다(중앙값은 올라가도 하위 10% 는 1.1~2.8).
- **원래 해상도 캡처로 보면**(`테두리_단계비교_원본해상도_390.png`):
  - K2·K3 는 글자가 속이 빈 윤곽처럼 보이고 여전히 흐리다.
  - K4 는 검은 윤곽이 뚜렷해 **자막처럼 보인다**. 지시 5번 기준 FAIL 이다.
  - K5 는 확실한 자막이다.
- 남은 미달(K4·K5 도 그대로인 15곳)
  - 「처음부터 시작하기」 12곳: 알약 안의 ↺ 그림(선 그림이라 테두리가 안 붙음) 둘레까지 측정 범위에 들어간 탓으로 보인다. 글자만 따로 재지 못해 **확인 불가**다.
  - 오류 문장 3곳: 판 밖, 파스텔 위 글자라 이번 비교 대상이 아니다. 기존 그림자로 2.65 다.
- 보내기 버튼(↑ 그림)은 짙은 색 그림이라 이미 11.7 이상이다.
- **코랄 구간: 표본 0 → 확인 불가.**
  - 판이 넓어 바탕 평균색이 민트·노랑으로 분류됐다.
  - 코랄 부분만 따로 잰 숫자는 없다.

## 판정

- **목표 동시 충족 불가 — STOP.**
  - 흰 글씨 유지 + 투명 판 유지 + 표준 대비 4.5:1 은 셋 다 동시에 맞출 수 없다.
  - 테두리로 가장자리를 또렷하게 하면 K4 부터 자막처럼 두꺼워진다(지시 5번 FAIL).
- 지시대로 검정 판은 되살리지 않았다. 테두리도 제품에 넣지 않았다.
- 앱 ZIP `7c9ad608…` 는 운영 후보로 **확정하지 않는다**(지시 10번).
- 대표가 고를 수 있는 방향(내가 정하지 않음):
  - (가) 판에 아주 옅은 바탕을 다시 준다(투명 포기).
  - (나) 판 안 글씨만 어두운 색으로 한다(흰 글씨 포기).
  - (다) K4 수준 테두리를 받아들인다(자막 느낌 감수).
  - (라) 지금대로 둔다(가독성 FAIL 감수).

## 캡처

- `테두리_단계비교_원본해상도_390.png`: 목적 카드 부분, 배율 3배 원래 해상도
- `테두리_단계비교_목적카드_390.png` · `테두리_단계비교_하단카드_390.png`: 390 전체
- `테두리비교_360.jpg` · `테두리비교_390.jpg` · `테두리비교_430.jpg`: K2·K3·K4 × 목적 카드·맨 아래
- 측정 스크립트 `qa/stroke.mjs` · 결과 `qa/stroke-sweep.json`

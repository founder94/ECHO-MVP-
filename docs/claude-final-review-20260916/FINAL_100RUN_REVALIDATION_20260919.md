# ECHO SERVER CORE — 최종 코드 실AI 100회 재검증 (2026-09-19)

최종 운영 코드 **gsq v44 / ej v24** 를 동결하고 실행했다. 실행 중 코드를 한 번도 바꾸지 않았다.
검사도구 **자동 재시도 OFF**. 실측값만 적는다.

---

# 1부 · 대표께

## ① 한 줄 결론

**B — 제품 P0 8건 발생. 해당 P0만 최소 수정이 필요하다.**
다만 **사용자 질문 무시 0건 · 빈 화면 0건 · 반말 0건 · 거절 의미 재등장 0건**으로, 이번 검증의 핵심 목표는 지켰다.

## ② 100회 결과

- 시도 **100회** (A~J 각 10회)
- **실제 시작 89회** · 시작 못 함 11회 ← **검사도구 문제**(아래 ③)
- **완주 64회**
- 중단 18회 = 시작 못 함 11 + **제품 경로 중단 7**

## ③ 시작 못 한 11회는 서버가 제대로 막은 것

서버에는 **"10분에 10번까지만 시작"** 보호 장치가 있다(`START_RATE_MAX=10`, HTTP 429).
검사가 워커 재시작으로 여러 번 끊겼다 이어지면서 제 쪽 속도 조절이 초기화됐고, 그 직후 11번이 규칙대로 거절됐다.
**제품은 정상 동작했다. 검사도구 결함이다.** 거절된 11회는 대화가 시작되지 않았으므로 품질 판정에서 뺀다.

## ④ 가장 중요한 결과 — 사용자 질문

되물음 **493턴** 중 **답 없이 질문만 돌려준 경우 0건.**
서버가 답 후보를 못 만든 경우가 36번 있었지만, **36번 전부** 사용자가 한 말을 인용해 먼저 답했다(`basis=1` 36 / `basis=0` 0).

## ⑤ 남은 제품 P0 8건

| 무엇 | 몇 건 |
| --- | --- |
| 질문 후보가 전부 막혀 대화 종료 | **5** (STEP 3 ×3, STEP 6 ×1, followup ×1) |
| 정정했는데 다음 질문이 반영 못 함 | **1** |
| AI 응답 문제로 대화 종료 | **2** (AI_ERROR 1, BAD_JSON 1) |

## ⑥ 지난번보다 좋아진 것

| 항목 | 지난 100회 | **이번 100회** |
| --- | --- | --- |
| 사용자질문무시 | 6 | **0** |
| 정정무시 | 5 | **1** |
| 빈 화면 | 6 | **0** |
| AI 응답 끊김(AbortError) | 40 | **1** |
| 대화 종료(제품 경로) | 15 | **7** |

## ⑦ 다음 행동 (하나)

**남은 P0 두 가지만 최소 수정한다** — ① STEP 3·6 후보 전부 차단 5건, ② 정정 미반영 1건.
(수정은 별도 승인 후.)

---

# 2부 · CTO 상세

## 2-1. 코드 동결 증거

| 파일 | 시작 SHA-256 (01:01 UTC) | 종료 SHA-256 (06:01 UTC) | 결과 |
| --- | --- | --- | --- |
| `get-step-question/index.ts` | `b79390f722295734…990e` | 동일 | **불변** |
| `get-step-question/rules.ts` | `893e5d7521c5e97a…0537` | 동일 | **불변** |
| `get-step-question/ai.ts` | `96abc56cbc036c2c…fa63` | 동일 | **불변** |
| `echo-journey/index.ts` | `6f13317108db602e…0bb8` | 동일 | **불변** |
| `echo-journey/question-quality.ts` | `599dad5f727b888c…9190` | 동일 | **불변** |

시작·종료 모두 **운영에서 직접 내려받아** 비교했다. 5/5 동일. 운영 파일 수 5 (추가 0 · 누락 0).

## 2-2. 검사도구 자동 재시도 = 0 (증거)

- `run100v2.mjs`: `const AI_RETRY = process.env.AI_RETRY === '1';` — `AI_RETRY` 가 `'1'` 일 때만 재시도한다.
- 100회 실행 명령 어디에도 `AI_RETRY` 를 설정하지 않았다 → **재시도 경로 미실행.**
- 진행 로그에 `AI재시도` 카운터가 한 번도 찍히지 않았다.
- 근거: 실제 화면 `StepQuestionScreen.tsx` 는 `in_progress`(동시성 충돌)에만 재요청하고,
  그 외 실패는 `setPhase('error')` → **"잠시 연결이 원활하지 않아요." + "다시 시도하기" 버튼**(233~247행).

## 2-3. 100회 항목별 실측

| 항목 | 값 |
| --- | --- |
| 총 시도 | **100** |
| 실제 시작 | **89** |
| 완주 | **64** |
| 중단(전체) | **18** |
| 중단(제품 경로) | **7** |
| 인증 실패 | **0** |
| RATE_LIMITED@start (검사도구) | **11** |
| 서버 오류(5xx) | **0** |
| AI_ERROR | **1** (`#67 G askback:step1`) |
| BAD_JSON | **1** (`#62 G askback:step1`) |
| AbortError (서버) | **1** |
| HTTP 오류 (서버) | **0** |
| 빈 서버 응답 | **0** |
| 잘린 응답 | **0** |
| timeout (무한대기) | **0** |
| **사용자질문무시** | **0** (되물음 493턴 기준) |
| `asked_reply_failed` | **36** — 전부 `basis=1`, `basis=0` **0** |
| `reply_without_question` | **264** |
| `correction_ignored` 차단 | **4** |
| `correction_unreflected` | **1** |
| **정정무시 (화면)** | **1** (`#58 F`) |
| **거절 의미 재등장** | **0** |
| NO_CANDIDATE | **5** — gsq **1** / ej **4** |
| `diversity_exhausted` (신규 구제) | **6** |
| `understanding_unresolved` (구제) | **8** |
| 빈 화면/빈 응답 | **0** |
| 반말 | **0** |
| 중복(반복) 질문 | **88** |
| 단계 비정상 증가 | **0** |
| 장문 | **0** |
| 맥락무시(일반론) | **589** |
| AI 턴(화면 문장) | **1,574** |
| `question_ready` (정상 생성) | **945** |

## 2-4. 사용자 질문 — `asked_reply_failed` vs 사용자질문무시

| 구분 | 값 | 뜻 |
| --- | --- | --- |
| `asked_reply_failed` | **36** | 서버가 3번 시도해도 모델 답 후보를 못 만듦 |
| 그중 `basis=1` | **36 (100%)** | 사용자 원문을 인용해 먼저 답함 — salvage 성공 |
| 그중 `basis=0` | **0** | salvage 실패 없음 |
| **사용자질문무시(화면)** | **0** | 되물음 493턴 중 답 없이 질문만 나간 경우 없음 |

→ **서버 생성 실패(36)와 화면 실패(0)를 구분한 결과, 화면에서는 모두 답이 전달됐다.**

## 2-5. 정정 처리

| 요구 | 결과 | 근거 |
| --- | --- | --- |
| 최신 정정 우선 | **부분 PASS** | `correction_ignored` 4회 차단 작동, 1회 미반영 |
| 사용자 원문 보존 | **PASS** | 과차단(P0-06) 0건 |
| 잘못된 AI 의미 폐기 | **PASS** | 거절 재등장 0건 |
| 정정 내용 반영 | **목표 미달** | 화면 정정무시 **1건** |
| 명시적 거절 의미 재등장 차단 | **PASS** | **0건** |

### 재발한 1건 (`#58` F 직접 설명)

- 정정: `"제가 직접 설명할게요. 돈보다 시간이 없는 게 더 힘들어요"`
- 직후 질문: `"돈이 제일 크게 걸려요라고 말씀하셨네요. / 돈 문제와 관련해서 어떤 부분이 가장 걱정되시나요?"`
- 경로: `correction_unreflected` 1건 — 규칙은 차단했으나 3번 시도 안에 정정을 다룬 후보를 못 만들어 구제로 통과
- 수정 후 10회(2026-09-18)에서 1건 → **이번 100회에서도 1건.** 재발 확인.

## 2-6. NO_CANDIDATE — gsq / ej 분리

| 대화 | 그룹 | STEP | 서버 | 화면 결과 |
| --- | --- | --- | --- | --- |
| #10 | B 구체적 설명 | step3 | ej | 대화 종료 (3문장 뒤) |
| #14 | B 구체적 설명 | step6 | ej | 대화 종료 (6문장 뒤) |
| #24 | C 애매함/모름 | step3 | ej | 대화 종료 (3문장 뒤) |
| #41 | E 강한 거절 | followup | gsq | 대화 종료 (3문장 뒤) |
| #96 | J 기존 해석 반박 | step3 | ej | 대화 종료 (3문장 뒤) |

- 서버 집계: `[gsq] no_candidate` **1** · `[ej] no_candidate` **4** — 클라이언트 5건과 일치
- 마지막 구제는 실제로 실행됐다: `diversity_exhausted` **6건**(구제 성공), `understanding_unresolved` **8건**(구제 성공)
- 즉 **구제가 작동한 14건은 살아났고, 구제로도 못 살린 5건이 종료됐다.**
- **P0에서 제외하지 않는다.** 사용자 화면에는 대화가 끝난 것으로 보인다.

## 2-7. AI 지연 — 단계별 분리

| 단계 | 실측 |
| --- | --- |
| AI 호출 실패 | `openai_fetch_error` **1** (`name=AbortError`) |
| HTTP 오류 | **0** |
| 빈 응답 / 잘린 응답 | **0 / 0** |
| 서버 처리 | 시도 예산 내 재생성. `attempts_deadline` 312회 |
| 최종 HTTP 결과 | `AI_ERROR` 1건, 클라이언트 JSON 파싱 실패 1건(BAD_JSON) |
| 프론트가 받는 상태 | `ok:false` + 코드 |
| 사용자에게 보이는 결과 | **오류 화면 + "다시 시도하기" 버튼** (자동 복구 없음) |

**분류(근거 있는 것만):**
- 제품 규칙 결함 — **아니다.** 서버 규칙이 후보를 막은 것이 아니다.
- 외부 API 지연 — **그렇다.** `AbortError` 1 / `openai_http` 0.
- 검사도구 결함 — **아니다.** 자동 재시도를 껐으므로 화면과 같은 조건이다.
- **사용자 화면 실패로 이어짐 — 그렇다. 2건을 P0-09 로 센다.**

지난 100회(AbortError 40) 대비 **1건**으로 줄었다. 다만 두 회차의 상류 지연 상태가 달라
`callTimeoutMs`(남은 예산만큼, 상한 9초) 수정의 단독 효과로 단정하지는 않는다 — **기여 가능성 있음, 인과 확정은 확인 불가.**

## 2-8. 성능 (최종 코드, 자동 재시도 OFF)

| 지표 | 값 |
| --- | --- |
| 표본 | **3,153** |
| p50 | **3,277ms** |
| p75 | **6,830ms** |
| p95 | **10,029ms** |
| 최대 | **13,821ms** |
| 6초 초과 | **996건 (31.6%)** |
| 9초 초과 | **451건 (14.3%)** |
| 10초 초과 | **159건 (5.0%)** |
| timeout | **0건** |

v17 기준선: p50 4,548 / p75 5,260 / p95 6,930ms → **p50 개선, p75·p95 악화.**

## 2-9. gsq / ej 각각

| 항목 | get-step-question | echo-journey |
| --- | --- | --- |
| 최종 버전 | **v44 ACTIVE** | **v24 ACTIVE** |
| `verify_jwt` | true | true |
| 운영=로컬 SHA | 3/3 일치 | 2/2 일치 |
| NO_CANDIDATE | **1** (followup) | **4** (step3 ×3, step6 ×1) |
| `asked_reply_failed` | 36 (전부 basis=1) | 0 |
| `reply_without_question` | 264 | 0 |
| `correction_unreflected` | 1 | 0 |
| `diversity_exhausted` | — (미탑재) | **6** |
| `understanding_unresolved` | 8 | — |

## 2-10. STOP 항목

DB · RLS · Migration · Secret · AI 모델 · Toss · 가격 · Netlify · Readdy · 운영 사용자 데이터
→ **전부 변경 없음. 승인 없이 변경한 STOP 항목 없음.**
이번 세션에서 배포한 Edge Function **0건**(코드 동결). 변경한 파일은 검사도구 2개뿐:
`LIVE/run100v2.mjs`(자동 재시도 OFF 스위치 + 이어돌리기 START), `LIVE/supervise.sh`(신규).
**제품 코드는 한 줄도 바꾸지 않았다.**

## 2-11. 최종 판정

# **B — 제품 P0 발생 → 해당 P0만 최소 수정 필요**

| 판정 후보 | 선택 | 이유 |
| --- | --- | --- |
| A (P0 = 0 → Core 동결) | ✗ | 제품 경로 P0 **8건** |
| **B (P0만 최소 수정)** | **✓** | 원인이 특정된 2계열 |
| C (외부 장애로 무효) | ✗ | 100회 정상 실행. AbortError 1건뿐 |

**제품 P0 8건:** P0-09 진행 불가 7건(NO_CANDIDATE 5 + AI 지연 2) · P0-04 정정 무시 1건
**PASS 항목:** 사용자질문무시 0 · 빈 화면 0 · 반말 0 · 거절 재등장 0 · 단계 비정상 증가 0 · 인증 실패 0 · 장문 0 · 서버 5xx 0

## 2-12. 다음 행동 (하나)

**남은 P0 2계열만 최소 수정한다.**
1. `[ej] no_candidate` 4건 — STEP 3·6 에서 `diversity_exhausted` 구제로도 못 살린 경로
2. `correction_unreflected` 1건 — 정정 좁히기가 3번 안에 성공하지 못하는 경우

**수정은 별도 승인 후 착수한다.**

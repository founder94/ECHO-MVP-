# ECHO 운영 전체 연결 실측 (2026-09-25 · READ-ONLY)

근거: 대표 「운영 전체 연결 실측 · READ-ONLY FIRST」.
- 대표가 iPhone 에서 「시작하기」를 눌렀더니 검정 「다음에 와도, 처음부터 쓰지 않도록.」 화면이 떴다(12:26 KST 캡처).
- 이 문서의 변경 = 코드 0 · DB 0 · RLS 0 · Secret 0 · 배포 0 · 운영 데이터 0.
  - 쓴 것은 읽기뿐: SQL SELECT 3번 · Edge 함수 목록 · 공개 Auth 설정 GET 1번 · 운영 ZIP 사본 검색.
  - 문자는 1통도 보내지 않았다.

## 0. 먼저 밝히는 것 (내 실수)
- **운영 앱 `42df9c26…` 에는 Agent v1.1 의 화면 쪽 변경이 섞여 있다.**
  - 무엇: 대화 요청마다 최근 대화 3줄(`recent`)을 서버로 함께 보낸다(커밋 `0cce434`).
  - 왜 섞였나: 파스텔 작업을 v1.1 작업본 위에서 했다. ZIP 을 전달할 때 이 사실을 말하지 않았다.
  - 확인 방법: 운영 ZIP 사본 `core-conversation-CBZQyra9.js` 에 `recent:` 가 있다. 44차 ZIP 에는 0곳이다.
  - 운영 서버 doit-understanding v27(`1aab6423…`)은 `recent` 를 한 번도 읽지 않는다(글자 0곳). 그래서 대화 동작은 44차와 같다.
  - 다만 승인서가 말한 "UI 만"을 벗어난 변경이다.

## 1. START ROUTING — 「시작하기」 실제 경로 (코드 + 운영 빌드 사본으로 확인)
```
히어로 「ECHO 시작하기」 (src/components/DoItBrandHero.tsx:64, onClick=start → onStart)
 └ 랜딩 handleStart (src/pages/do-it/landing/page.tsx:50) → 앱에서는 navigate('/doit/start-journey')
    └ StartJourney (src/doit/pages/do-it/start-journey/page.tsx)
       ├ 로그인 안 됨 → step = "consent" (page.tsx:271, 운영 빌드에서 VITE_A_STRUCTURE_SERVER_ENABLED=true 가 박혀 `x('consent')` 로 컴파일됨)
       │   └ SignupConsent (src/doit/app/plan-a/screens/SignupConsent.tsx) = 「다음에 와도, 처음부터 쓰지 않도록.」 ← 대표 캡처 화면
       │       └ 「내 계정으로 계속하기」 → /login (page.tsx:543) → 로그인 뒤 start-journey 로 돌아와 복원
       └ 로그인 됨 → restoreFromServer
           ├ 목적 없음 → /doit/conversation?from=journey  (파스텔 대화)
           └ 목적 있음 → step "conversation-choice" 「오늘은 무엇부터 할까요?」(검정) → 「다섯 가지 질문 보기」 → /doit/conversation (파스텔)
```
- 판정: **A(의도된 계정 안내 게이트) + C(로그아웃 상태 때문)**
  - B(옛 화면)가 아니다.
  - D(잘못된 연결)도 아니다. 코드 주석이 "로그인 전에는 계정 안내로 바로 간다"를 의도로 적어 두었다(page.tsx:270).
- iPhone 이 로그아웃이었다는 근거(운영 DB 읽기)
  - auth.sessions 에서 iPhone 세션의 마지막 생성 = 2026-09-20 12:08Z 이다.
  - 최근 24시간 iPhone 세션은 0개다.
  - 아이폰에서 홈 화면에 추가한 앱은 사파리와 로그인 저장소가 따로라서 로그아웃 상태로 열릴 수 있다. 이것은 일반적인 사실이고, 대표 기기에서 확인한 것은 아니다.
- 대표 FINAL 흐름과 다른 점
  - 로그인 전 사용자는 대화에 닿기 전에 검정 화면 두 개를 거친다: 계정 안내 → 로그인.
  - 돌아온 사용자도 검정 「무엇부터 할까요?」 한 화면을 거친다.
  - 파스텔은 대표 지시(「질문 구간부터」)대로 `/doit/conversation` 에만 적용돼 있어, 이 화면들은 검정이다.

## 2. PASTEL UI
- 코드: `src/doit/components/feature/core-conversation.css` 의 `.echo-dialogue--pastel`.
  - 붙은 곳: CoreConversation · ConversationOpening · 대화 화면의 기다림/로그아웃 구간.
- 운영 배포: doitmobile `6ab576b1…`(9/25 04:15 KST) = 승인 ZIP `42df9c26…`.
  - 근거: 바뀐 파일 94개가 Netlify 기록과 일치한다.
  - ZIP 안 `core-conversation-CIk5UbUa.css` 에 `echo-dialogue--pastel` 이 있다.
- 운영에서 대화에 닿았는지(서버 기록, 개수만, 원문 읽지 않음)
  - 배포 뒤 19:20~19:23Z(04:20~04:23 KST)에 다음 질문 만들기 5번 · 답 저장 3번 · 넘기기 1번이 있었다.
  - 즉 로그인한 누군가가 새 빌드의 대화 화면에 닿았다.
  - 어느 기기였는지, 화면이 어떻게 보였는지는 이 기록으로 알 수 없다.
- 실기기: 대표 캡처 전이라 확인 불가.

## 3. PHONE AUTH
| 항목 | 실측 |
|---|---|
| 진입 경로 | `/doit/verify` (src/doit/routes.tsx:44) · 연결 탭의 전화 인증 줄 |
| 요구 조건 | 연결 후보 자격에 필수. `doit-connect` 에서 `if (!phoneVerified) missing.push("phone")` (index.ts:403) |
| 발송 함수 | `supabase.auth.updateUser({ phone })` (src/doit/lib/phoneVerify.ts:56). Supabase Auth 가 문자를 보낸다 |
| 확인 함수 | `supabase.auth.verifyOtp({ type: 'phone_change' })` (phoneVerify.ts:61) |
| 저장 | Auth `phone_confirmed_at` → 서버 `doit-connect phone_sync` 가 `profiles.verification_status='verified'` 로 맞춤 (index.ts:550) |
| Provider | 운영 공개 Auth 설정(`/auth/v1/settings`, 읽기): **`external.phone = false` = 전화 로그인 꺼짐**. `sms_provider: "twilio"` 로 표시되지만 이것은 설정 칸 값일 뿐이고 Twilio 계정이 연결됐다는 뜻이 아니다 |
| Secret 존재 | **확인 불가.** 문자 업체 값은 Auth 설정 안에 있어 관리 API 로만 보이는데, 이 환경의 관리 토큰은 401 이다. 값은 읽지도 출력하지도 않았다 |
| 실제 SMS | 0. auth.users 6명 중 번호 있음 0 · 번호 확인됨 0 · profiles verified 0 · 연결 0 |
| 우회 | 앱 쪽으로는 우회 불가. anon·authenticated 에게 `profiles.verification_status` 와 `role` 칸의 UPDATE·INSERT 권한이 0이다(information_schema 로 확인) |
| 판정 | 구현됨 = 예 · 연결됨 = 예(화면 → Auth → 서버 → 자격) · **운영 검증됨 = 아니오**. Phone 공급자가 꺼져 있어 지금 누르면 발송이 실패하고 "준비하고 있어요" 안내가 뜨는 경로다 |

## 4~6. SIGNATURE SOUND
- 저장소(git)에 mp3 파일 0개.
- 운영 앱에 있는 음악은 **랜딩 이야기 09 구간의 `OriginalMusicCard`**(src/pages/do-it/hero/components/OriginalMusicCard.tsx) 하나다.
  - 곡 2개, 바깥 주소 `storage.helloreaddy.io`
  - 사용자가 직접 누르는 재생 카드다.
  - 이름표는 「원음 1 / 원음 2」다. 코드 주석이 "성별 확인 전이므로 파일 순서로 성별을 추정하지 않는다"라고 적어 두었다 → **male/female 매핑 없음**.
- 곡 실측(2026-09-24, 작업 공간 사본)
  - 원음 1 `…65f5bc59….mp3`: 1,142,136바이트 · SHA-256 `e38d47da…` · 28.55초
  - 원음 2 `…02c258b7….mp3`: 1,813,428바이트 · SHA-256 `caa47c3d…` · 45.34초
- Part A(「시작하기」 → 대화 진입 → 재생)
  - 소리 제어기(`signatureSound.ts`·`SoundControl.tsx`)는 9/24 범위 정정 때 작업 공간에 **보관만** 했다. 커밋 0, 배포 0.
  - 그래서 CTA 연결 없음 · iOS/Galaxy 별도 처리 없음 · 전체 소리 끄기 없음.
  - 카드 자체에는 재생/정지, 자동재생 차단(NotAllowedError) 처리가 있다.
  - 실제 재생: 확인 불가. 이 환경에서 바깥 주소가 막혀 있다.
- Part B
  - 보관한 코드 안에 구조만 있다.
  - Relationship Open 은 운영에서 열린 적이 없다(연결 0 · 전화 인증 0) → DISABLED.

## 7. RELATIONSHIP AGENT
- 운영 doit-understanding = **버전 27**(Agent v1, 승인 지문 `1aab6423…`, 수정 2026-09-24T13:45Z). 이번에 바뀐 것 없음.
- 앱은 `turn` 한 턴 경로를 부른다.
- v1.1 서버(`c8c0cf85…`)는 로컬만 있다. 앱 쪽 `recent` 는 0장 참고.
- 실제 AI: 배포 뒤 대화 기록 5번이 있다(위 2장). 품질 판정은 하지 않았다.
- **Conversation P0 = FAIL 유지.**

## 8. 지금 실제 사용자가 할 수 있는 것
- 로그인한 사람: 파스텔 대화에서 다섯 가지 질문에 답하고, 사진·소개를 채울 수 있다.
- 할 수 없는 것: 전화 인증 · 연결 · 시작 음악(Part A).
- 로그인 안 한 사람: 검정 계정 안내 → 로그인부터 거쳐야 한다.

## 9. 수정이 필요한 P0 (최소, 대표 결정 필요 · 아무것도 고치지 않음)
1. **시작 흐름**: 로그인 전 사용자가 대화 전에 검정 계정 안내 화면을 한 번 더 거친다. 대표가 셋 중 하나를 정해야 한다.
   - 이 화면을 빼고 바로 로그인으로 보낸다.
   - 이 화면에도 파스텔을 입힌다.
   - 지금 그대로 둔다.
2. **전화 인증**: Supabase Phone 공급자가 꺼져 있고 문자 업체 연결이 중단된 상태다(Twilio 중단 결정). 그래서 연결 자격자는 구조적으로 0명이다. 대표가 둘 중 하나를 정해야 한다.
   - 문자 업체를 연결한다.
   - 연결 자격에서 전화 인증 조건을 바꾼다.
3. **Conversation AI 품질**(기존 P0): 실제 AI 로 Galaxy·iPhone LEVEL 3 를 확인하기 전까지 FAIL 이다.

- P0 가 아닌 것(보류)
  - Signature Sound Part A: 운영에 없음. 대표 승인 시 별건으로 한다.
  - `recent` 섞임: 동작 영향 0. 기록만 한다.

## 10. 추가 — 갤럭시 실측 (대표 「갤럭시도 확인해」, 운영 DB 읽기만)
- 새 배포(04:15 KST) 뒤 대화 기록 9건은 **모두 갤럭시에서 나왔다.**
  - 근거: 안드로이드 크롬 로그인 세션이 04:20:29 KST 에 마지막으로 갱신됐다. 대화 기록과 같은 계정이다.
  - 같은 시간대에 다른 기기 세션은 없다.
  - 아이폰 세션은 다른 계정이다. 마지막 갱신 9/24 05:39 KST.
- 서버 처리(운영 v27, 실제 OpenAI). 사용자 원문은 읽지 않았고, AI 가 만든 질문만 읽었다.

| 시각(KST) | 동작 | 결과 | AI 질문 |
|---|---|---|---|
| 04:20:42 | 다음 질문 | 성공 4.1초 | 그 중에서 어떤 취미가 가장 즐거운가요? |
| 04:22:13 | 답 저장 → 다음 질문 | **실패 AI_ERROR (0.05초)** | — |
| 04:22:17 | 다음 질문 (다시) | 성공 4.3초 | 사람을 알아가는 데 어떤 점이 가장 중요하다고 생각해요? |
| 04:22:40 | 넘기기(말 종류 = 되묻기) | 성공 | 사람을 알아가면서 어떤 점이 가장 즐거운 것 같아요? |
| 04:23:08 | 답 저장 → 다음 질문 | 성공 | 진심으로 사람을 알아가는 데 어떤 점이 특별하다고 느끼나요? |
| 04:23:41 | 답 저장 → 다음 질문 | 성공 | 사람을 진심으로 알아가는 과정에서 어떤 점이 가장 마음에 드나요? |

- 판정(실제 서버·실제 AI 기준)
  - 마지막 질문 4개가 모두 「사람을 (진심으로) 알아가는 … 어떤 점이 …」 같은 틀이다. 같은 뜻의 재등장을 막는 기준에 걸린다 → **FAIL**.
  - 「어떤 점이 특별하다고 느끼나요」는 무거운 추상 물음이다. 대표가 정한 말투 기준(가볍게)과 맞지 않는다.
  - AI_ERROR 가 1번 났다. 0.05초 만에 실패해 AI 까지 가기 전에 멈춘 것으로 보이지만, 원인은 확인하지 못했다.
- 화면(파스텔·흰 글씨 가독성)은 기록으로 알 수 없다 → 대표 캡처가 필요하다.
- **Conversation P0 = FAIL 유지.** 아무것도 고치지 않았다(STOP).

# Codex 인계 — 최종본 배포 지시 (2026-09-17, 대표 승인 완료)

> ⚠️ **폐기된 옛 가격 표시 (2026-09-26 대표 결정)**: 이 문서의 4,900원은 **폐기된 옛 구조(legacy)** 이며 현재 가격이 아닙니다. 현재 가격은 미확정입니다. 과거 기록 보존을 위해 본문은 그대로 둡니다.

이 문서 하나만 보고 실행하면 된다. 추정으로 "완료"라고 쓰지 않는다. 실행한 것만 보고한다.

## 0. 넘기는 파일
| 파일 | 용도 | SHA-256 |
|---|---|---|
| `ECHO_CLAUDE_NETLIFY_DRAG_20260917.zip` | **Netlify 드래그 배포용 정적 빌드**(102파일: `index.html`, `_redirects`, `_headers`, `favicon.svg`, `assets/`) | `308e8e5392f47bc475dd196557bfdab6ffde285e7ca91993b29c303ea4c9c4d8` |
| `ECHO_CLAUDE_TRUST_FIRST_20260917.zip` | 검수용(서버 코드 원문·diff·검사·운영 회수본). **배포 대상 아님** | 최종 보고의 값 |

배포 전에 ZIP 의 SHA-256 을 다시 계산해 위 값과 같은지 확인한다. 다르면 배포하지 않는다.

## 1. 서버(Supabase) — Claude 가 대표 승인 범위 안에서 처리 완료
| 함수 | 버전 | 배포본 ↔ 로컬 | 내용 |
|---|---|---|---|
| echo-journey | v12 → **v14 ACTIVE** (KST 09-17 07:30, ezbr `21ce08f0…`) | `index.ts` 바이트 동일 / 짝 파일 끝 줄바꿈 1바이트 차이(내용 동일) | 되물음은 단계를 진행하지 않음, 고정 회피·잘린·무관한 답변 차단, 거절 해석이 답변·요약에 되살아나지 않음, 확인된 기억 사용, 리포트 이후 대화, 시도 예산 11초, 토큰 사용량 로그 |
| get-step-question | v21 → **v25 ACTIVE** (KST 09-17 07:48, ezbr `17df317b…`) | `index.ts` 바이트 동일(cmp 클린) | 같은 규칙 + 이해 확인 4버튼 결과를 `doit_insights` 에 기억으로 저장 |
| echo-payment(v2) / admin-dashboard(v1) / admin-conversations(v2) / openai-chat(v2) / doit-understanding(v4) | 변경 없음 | — | — |
| DB 스키마 / RLS / 마이그레이션 / 시크릿 / Toss | **변경 없음** | — | 4,900원 단건, `PAYMENT_GATE` 그대로 |

중간에 번호가 몇 개 늘어난 경위와 v22 의 주석 3바이트 오배포·정정 기록은 `DEPLOYMENT_PLAN_STOP.md` 첫 장에 그대로 적어 두었다. 최종 운영본은 v14 · v25 이고 둘 다 로컬과 바이트 대조를 마쳤다.

프론트와 서버의 응답 계약은 그대로다(필드 추가만). 배포 순서는 서버 앞뒤 어느 쪽이어도 안전하다.

## 2. Netlify 배포 순서
1. 사이트 `echo-mvp-doit`(ID `7a4934db-aff3-437d-815a-ffbf49d4b819`) > Deploys > "Drag and drop" 에 `ECHO_CLAUDE_NETLIFY_DRAG_20260917.zip` 을 올린다. **정적 빌드 ZIP** 이므로 Netlify 가 다시 빌드하게 두지 않는다.
2. 상태가 `ready` 가 되면 배포 ID 를 기록한다.
3. https://do-it.company 를 새로고침해 `index.html` 이 `assets/index-PTm_2znC.js` 를 부르는지 확인한다.
4. 문제가 보이면 직전 배포 `6aaa6f4662ecb9daa734d6da` 를 "Publish deploy" 로 되돌리고 화면 사진과 배포 ID 를 Claude 에게 전달한다.

## 3. 실기기 확인 (이게 끝나야 "운영 확인 완료")
| # | 기기 | 확인할 것 | 통과 기준 |
|---|---|---|---|
| 1 | 갤럭시 Chrome | 홈 | 초록 배경이 중간에서 직각으로 끊기지 않는다 / 헤더가 겹치지 않는다 |
| 2 | 아이폰 Safari | 아무 입력칸 | 눌러도 화면이 확대되지 않는다 |
| 3 | 둘 다 | `/payment?c=…` | "결제 준비 중" 비활성 버튼 + 안내 문구. **결제창이 열리면 실패** |
| 4 | 둘 다 | `/step/7` | 답변이 저장된 뒤에만 White Door 로 넘어간다 |
| 5 | 둘 다 | STEP 3~7 중 아무 단계에서 `어떻게 해야 좋을까?` 라고 되묻기 | **그 물음에 먼저 답하고, 단계 번호는 그대로**여야 한다 |
| 6 | 둘 다 | 이해 확인에서 "그게 아니에요" → 이후 질문·리포트 | 거절한 뜻이 다시 확정 사실로 나오지 않는다 |
| 7 | 둘 다 | 리포트 화면(구매자 계정) | 맨 아래 "리포트를 보고 더 이야기하기" 가 보이고 대화가 이어진다. 미구매 계정은 리포트 화면 자체가 열리지 않는다 |

## 4. 배포 뒤 보고 형식(그대로 채워서)
```
[Netlify 배포 보고]
ZIP SHA-256 재계산: (값) / 문서값과 일치: 예·아니오
배포 ID: (값) / 상태: (ready 등) / 시각(KST):
운영 index.html 이 부르는 JS: assets/index-________.js
1 갤럭시 홈 배경 직각 잘림: 있음·없음 / 헤더 겹침: 있음·없음
2 아이폰 입력칸 확대: 있음·없음
3 결제 화면 "결제 준비 중"(비활성): 예·아니오 / 결제창 열림: 예·아니오
4 STEP 7 저장 전 White Door 이동: 있음·없음
5 되물었을 때: 답을 먼저 했다 예·아니오 / 단계 번호 그대로 예·아니오 (화면 사진)
6 "그게 아니에요" 뒤 같은 뜻 재등장: 있음·없음
7 리포트 "더 이야기하기": 보임·안 보임 / 대화 이어짐: 예·아니오
되돌림 실행 여부: 예·아니오 (이유)
```

## 5. 하지 말 것
- `.env`·키·토큰을 ZIP·보고서에 넣지 않는다. Toss 결제 설정·`PAYMENT_GATE`·가격(4,900원)을 바꾸지 않는다.
- Supabase 함수·DB·RLS 를 건드리지 않는다(서버는 Claude 가 승인 범위만 처리했다).
- 홈 히어로 문구·디자인 자산을 바꾸지 않는다.
- 월 구독·다른 월 가격 작업은 **보류**다. 손대지 않는다.
- 실기기 확인 전에 "운영 확인 완료"라고 쓰지 않는다.

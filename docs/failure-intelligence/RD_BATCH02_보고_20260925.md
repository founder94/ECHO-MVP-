# R&D 작업 묶음 02 보고 — 저장 MASTER 반영 · 행동 장부 · AI 조언자 실패 · Golden 판정 · 실AI 대안 경로 (2026-09-25)

근거: 대표 「저장·기억·실패 자산화 MASTER LOCK」(2026-09-25) + R&D 헌장. 운영 변경 0.

## 1. 결론
- 저장 MASTER 를 정본에 반영했다(`STORAGE_MASTER_20260925.md`).
- 실패 53 → **62건**
  - AI 조언자 실패 7건(F-ADVISOR): ACTUAL 5, CANDIDATE 2
  - B 위험 후보 2건: HYPOTHESIS
- 행동 상태 장부를 새로 만들었다: COMPLETED 3 · DECIDED 7 · BLOCKED 5 · PENDING 6.
- 고정 입력 34개를 **실행 가능한 Golden 검사 20 spec**(PASS/FAIL 기준, 사전 고정)으로 바꿨다.
- 실AI A/B: **확인 불가**(REAL_AI = BLOCKED_BY_ENVIRONMENT).
  - Claude 환경을 거치지 않는 대안 경로(GitHub Actions + 저장소 Secret)를 준비했다.
  - Secret 생성은 HARD STOP 이라 대표 승인을 기다린다.
- Conversation P0 = FAIL 유지.

## 2. 실제 수행 작업
- 실패 기록 칸 추가: 증거 수준(ACTUAL·CANDIDATE·HYPOTHESIS) · Mock 결과 · 사용자 결과 · 관련 실패(Graph 에서 자동 생성).
- 데이터 검사 추가
  - 대표 진술만으로 ACTUAL 표시 금지
  - 빈칸 금지(모르면 UNKNOWN)
  - 실사용 근거 없는 사용자 PASS 금지
- 이 과정에서 **내 표시 오류 2건을 찾아 정정했다**
  - GF-34·GF-40 은 코드로 확인된 결함인데 출처를 「대표 진술」로만 적었다.
  - 원본은 두고 정정 메모를 달았다.
- Failed Solutions 21건에 5칸 추가: 왜 골랐나 · 어떻게 · 나아진 것 · 망가뜨린 정상 사례 · 다시 쓸 조건.
- AI 조언자 실패 증거 파일을 새로 만들었다(`evidence/ADVISOR_SESSION_20260925.md`).
  - 이 세션에서 내가 대표에게 실제로 보낸 문장을 옮긴 것이다.
- `golden-specs.json`: Failure·Flow·턴마다 다음을 적었다.
  - 기대 행동 · 금지 행동 · 기계 판정 · 사람 판정 질문 · 역검사 · 출처
  - 기계 판정은 저장·오류·반응 유무·실제로 나간 고정 문장 7개 대조뿐이다(품질 점수 없음).
  - MOCK 에서는 모델 층을 판정하지 않는다(N/A).
- 하네스 추가 기능
  - **실AI 전 사전 고정 대조**: A·B·입력·판정 기준 중 하나라도 다르면 실행하지 않는다(종료 코드 3).
  - `api_cost_usd`: 공식 단가를 넣었을 때만 계산한다.
- `.github/workflows/echo-ab-real.yml` 을 만들었다.
  - 이 브랜치에서 `run-request.json` 이 바뀔 때만 실행된다.
  - 키는 GitHub 저장소 Secret `OPENAI_API_KEY_AB_TEST` 에서만 읽는다. 없으면 BLOCKED 를 기록한다.
  - 회귀 검사 → 실AI A/B → 결과 첨부(7일) 순서다.
  - 결과는 저장소에 커밋하지 않는다.

## 3. 새 Failure
| ID | 내용 | 증거 |
|---|---|---|
| GF-54 | 대표가 끝낸 키 발급·전달을 다시 요구(폐기·새 발급 2회) | ACTUAL |
| GF-55 | 막힌 환경 Edit 경로를 3번 다시 안내 | ACTUAL |
| GF-56 | 키 취급 안내가 「붙이지 마라 → 쓰려 함 → 지워라」로 바뀜 | ACTUAL |
| GF-57 | API 비용 상태를 먼저 알리지 못함 | CANDIDATE(대표 진술) |
| GF-58 | 대표를 복붙·승인 중계자로 씀(「승인 주세요」로 턴 종료) | ACTUAL(자기 기록) |
| GF-59 | 틀린 보고(「정정무시·미확정 사실화 사례 없음」) | ACTUAL |
| GF-60 | 하위 수정에 매몰돼 구조 대안을 늦게 검토 | CANDIDATE |
| GF-61·62 | B 위험: 다른 의도 이름 미탐 · 의도 소진 | HYPOTHESIS |

- GPT·Perplexity 판단 실패는 이 저장소에 근거 기록이 없어 넣지 않았다.

## 4. 새 회사 자산
- `STORAGE_MASTER_20260925.md`
- `ACTION_LEDGER.md` ← `data/action-ledger.json`
- `evidence/ADVISOR_SESSION_20260925.md`
- `golden-specs.json`
- `.github/workflows/echo-ab-real.yml`
- Taxonomy: Type 32 · Family 16

## 5. 해결된 것
- 대화 품질: 0.
- 과정 쪽에서 막은 것
  - 완료·결정·막힘을 장부로 대조한다.
  - 결과를 본 뒤 판정 기준을 바꾸면 실AI 실행이 멈춘다.
  - 대표 진술만으로 ACTUAL 을 표시하면 검사가 잡는다.

## 6. 해결되지 않은 것
- 실AI A/B · Conversation P0 · UNRESOLVED 28건 · 대표 결정 대기 4건(장부 P-03~06).

## 7. Failed Solutions
- 새 항목은 없다. 21건 모두에 「망가뜨린 정상 사례·다시 쓸 조건」을 채웠다.

## 8. 검증 수준
- 가짜 AI 기준 · AI 호출 0.
- 검사 **30/30 통과**
  - Failure Intelligence 9
  - 회귀 7
  - B-1.0 14
- 역검사 **6/6** 잡음
  - 대표 진술 ACTUAL 허용 · 장부 상태 검사 제거 · not_saved 항상 PASS · MOCK 에서 모델 층 판정 · 판정 기준 몰래 변경 · 사전 고정 불일치 실AI 차단(종료 코드 3)
- `fi-build --check` 통과. 워크플로 YAML 읽기 확인.

## 9. 실제 AI 결과
- 0회 · BLOCKED_BY_ENVIRONMENT.
- [MOCK] 1회 실행 규모(검사 34턴, o200k 로 셈 — 실제 AI 에서는 달라질 수 있음)
  - 호출: A 32 · B 34
  - 입력 토큰: A 56,817 · B 32,631
- 비용은 공식 단가가 없어 확인 불가다.

## 10. 사용자 검증
- 0.

## 11. 운영 변경
- 0. main 병합 0.
- 워크플로 파일은 이 브랜치에만 있다. 실행 조건 파일이 없어 아직 한 번도 실행되지 않았다.

## 12. 위험
- 저장소가 공개(PUBLIC)다. 실행되면 Actions 로그·첨부를 GitHub 사용자가 볼 수 있다.
  - 담기는 것: 고정 입력(이미 저장소에 공개됨) + AI 답.
  - 키는 GitHub 가 가린다.
- 실행 한 번에 OpenAI 호출 약 66번(가짜 AI 기준 수)이 든다. 금액은 공식 단가 확인 전이라 모른다.

## 13. 다음 행동 하나
- **HARD STOP 승인 요청**: GitHub 저장소 Secret `OPENAI_API_KEY_AB_TEST` 생성(대표만 가능).
- 승인되고 Secret 이 생기면 내가 `run-request.json` 을 push 해 실AI A/B 를 한 번 돌린다.
- 그다음 블라인드 검수표를 한 번에 전달한다.

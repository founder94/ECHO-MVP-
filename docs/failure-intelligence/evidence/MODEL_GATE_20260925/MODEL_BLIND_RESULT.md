# MODEL GATE 블라인드 결과 — 같은 B-1.0, 모델 4개 (2026-09-25)

- 근거: 대표 블라인드 검수 17칸, 07:42~07:50Z. 대표 「다골랐어」.
- 선택 원본: `model-picks/M01~M17.json` — 페이지 db `mpicks` 를 그대로 옮겼다.
- 열쇠: `model-blind-key.sealed.json`
- 집계는 `product/spike/ab-20260925/model-blind-score.mjs` 로 했다.
  - 이 스크립트는 선택 원본과 열쇠만 읽는다.
  - 출력은 `model-blind-score.json` 에 남겼다.
- 대표 선택을 고치거나 해석하지 않았다. 「모두 별로」도 그대로 셌다.
- 운영 변경은 0이다.

## 1. 집계
| | gpt-4.1-mini | gpt-4.1 | gpt-4o | gpt-4o-mini(기준) | 모두 별로 |
|---|---|---|---|---|---|
| 전체 17칸 | **5** | 4 | 3 | 2 | **3** |
| 실제 입력 14칸(예문 FLOW4 3칸 제외) | **5** | 2 | 2 | 2 | **3** |

- 어느 모델도 절반을 넘지 못했다. 가장 많이 뽑힌 모델도 5/17이다.
- 「모두 별로」는 3칸이다. 세 칸 모두 실제 입력이다.
- 실행 1회, 검수자 1명이다. 차이(5·2·2·2)로 모델 우열을 확정할 수 없다.

## 2. 칸별 공개
| 칸 | 흐름 | 무엇 | 선택 | 모델 | 고른 답에 질문 |
|---|---|---|---|---|---|
| M01 | FLOW1#2 | 대표 실제 항의 「나 진심이라고 적은거 같은데」 | ② | gpt-4o | 없음(반응만) |
| M02 | FLOW1#5 | 대표 실제 항의 「적었자네」 | ③ | gpt-4.1-mini | 있음 |
| M03 | FLOW2#1 | 「활동」 점프 | ① | gpt-4.1 | 있음 |
| M04 | FLOW2#2 | 「활동」 점프 뒤 되물음 | ① | gpt-4.1-mini | 있음 |
| M05 | FLOW3#2 | 「취미생활?」 | ③ | gpt-4.1-mini | 있음 |
| M06 | FLOW3#3 | 거절 · 질문 방향 제안 | ② | gpt-4o | 있음 |
| M07 | FLOW3#4 | 정상 답(취미 셋) 뒤 반응 | ④ | gpt-4.1-mini | 있음 |
| M08 (예문) | FLOW4#2 | 거절 「왜 또 물어봐?」 | ③ | gpt-4.1 | 없음(반응만) |
| M09 (예문) | FLOW4#4 | 정정 「활동 말고 …」 | ④ | gpt-4o | 있음 |
| M10 (예문) | FLOW4#5 | 거절 「그 질문 말고」 | ② | gpt-4.1 | 있음 |
| M11 | FLOW5#5 | 「활동」 점프 뒤 항의 「활동?질문이 머이래」 | 모두 별로 | — | — |
| M12 | FLOW6#1 | AI 에게 직접 질문 | ③ | gpt-4o-mini | 있음 |
| M13 | FLOW6#2 | AI 에게 직접 질문 | ③ | gpt-4o-mini | 있음 |
| M14 | FLOW6#3 | AI 에게 직접 질문 · 항의 | ② | gpt-4.1-mini | 없음(반응만) |
| M15 | FLOW7#1 | 「활동」 점프 | ② | gpt-4.1 | 있음 |
| M16 | FLOW7#2 | 「돈때문에」(같은 질문 반복 칸) | 모두 별로 | — | — |
| M17 | FLOW7#3 | 거절 · 반영 요구 | 모두 별로 | — | — |

- 칸마다 네 답의 원문은 `MODEL_BLIND_REVIEW.md` 에 있다.

## 3. 실패별로 본 것(대표 선택만 근거)
1. **「활동」 점프(M03·M04·M15 · 항의 M11)**
   - 고른 답 14개 중 질문에 「활동」이 들어간 답은 **0개**다.
   - M03·M15 에서는 네 답 가운데 「활동」을 묻지 않은 답이 gpt-4.1 하나뿐이었다. 대표는 두 번 모두 그 답을 골랐다.
   - 대표 피드백(A-17)과 모델 실행 숫자(gpt-4.1 「활동」 0)가 같은 쪽을 가리킨다.
   - 다만 「활동」 뒤 항의(M11)는 네 모델 모두 반응만 하고 질문이 없었다. 결과는 「모두 별로」다.
2. **대표 실제 항의(M01·M02·M14)**
   - 칸마다 다른 모델이 뽑혔다(4o · 4.1-mini · 4.1-mini).
   - M01·M14 에서는 질문 없이 반응만 한 답이 뽑혔다. M01 에서는 네 답 중 반응만 한 답이 그것 하나였다.
3. **거절·반영 요구(M17)**: 네 모델 모두 「모두 별로」였다.
   - 네 답은 이랬다.
     - 인정하고 「돈 말고도 친구에게 바라는 점」을 묻는 답(①)
     - 「더 잘 반영할게요」 뒤 돈 문제를 다시 묻는 답(④)
     - 사과만 한 답(②)
     - 「편하게 이야기하고 싶으신 것 같네요」(③)
   - 사용자가 요구한 것은 「너가 내 내용을 반영해서 다음 질문을」이었다. 어느 모델도 대표가 고를 만큼 해내지 못했다.
4. **「돈때문에」(M16)**
   - 네 답 모두 「돈 문제·고민」으로 받아 돈을 물었다. 결과는 「모두 별로」다.
   - 대표가 무슨 뜻으로 쓴 말인지는 추정하지 않는다.
5. **AI 에게 직접 질문(M12·M13·M14)**
   - 기준 gpt-4o-mini 가 3칸 중 2칸에서 뽑혔다.
   - 기준 모델이 뽑힌 칸은 이 두 칸뿐이다.
6. **거절·정정 — 예문(M06·M08·M09·M10)**
   - 4o · 4.1 · 4o · 4.1 로 나뉘었다.
   - 예문 칸이라 실사용 판정에는 쓰지 않는다.
7. **질문 없이 반응만 한 답**
   - 뽑힌 수는 3칸이다(M01 · M08 예문 · M14).
   - run1 에서는 반응만 한 B 가 두 칸 모두 졌다(GF-70). 이번에는 반응만 한 답이 이긴 칸이 있다.
   - 「반응만 = 진다」는 일관되지 않는다. GF-70 의 근거를 「혼재」로 고쳐 적는다.

## 4. 모델 몫 / 구조·계약 몫
| 실패 | 모델을 바꾸면 달라졌나 | 몫 |
|---|---|---|
| 「활동」 점프(GF-69·GF-71) | 달라졌다. gpt-4.1 만 점프 0이고, 대표는 점프 없는 답만 골랐다 | **모델 몫 — 가설 강화.** 1회 실행이라 확정은 아니다. 질문 방향 계약(서비스 취지)도 남아 있다 |
| 항의·반영 요구에 대한 대응(M11·M17) | 네 모델 모두 「모두 별로」 | **구조·계약 몫(가설).** 모델 교체로 풀리지 않았다 |
| 짧은 답 해석(M16 「돈때문에」) | 네 모델 모두 「모두 별로」 | 모델만으로 풀리지 않았다. 원인은 미분리 |
| 항의를 답으로 저장(GF-67) | 기계 숫자 3·3·2·1. 모든 모델에 남는다 | 구조·계약 몫(가설) |
| AI 에게 직접 질문(ANSWER FIRST) | 기준 모델이 2/3 | 모델 우열 없음 |

## 5. 판정
- **모델 승자는 없다.** 가장 많이 뽑힌 모델도 5/17이고, 실제 입력 기준 나머지는 2·2·2다.
- **어느 모델도 P0 를 통과하지 못했다.** 「모두 별로」 3칸은 네 모델 공통 실패다.
- **Conversation P0 = FAIL 유지 · VERIFIED 0 · USER_VERIFIED 0 · 운영 모델 변경 0.**
- 기계 판정과 대표 판정은 부분만 맞는다.
  - gpt-4o: 기계 PASS 25로 높았지만 블라인드에서는 3칸(실제 입력 2칸)이었다.
  - gpt-4.1-mini: 기계 PASS 24였고, 블라인드에서 가장 많이 뽑혔다.

## 6. 다음 행동 하나(결정)
- **구조 Gate — 항의·반영 요구에 대한 대응 계약(REPAIR)을 한 변수로 시험한다.**
  - 모델을 바꿔도 풀리지 않은 칸(M11·M17 · 항의 저장)이 남은 실패의 중심이다.
- 이 실험을 하려면 대표 구조 LOCK(A-16: B-1.1 즉시 제작 금지)을 이 실험 하나에 대해 풀어야 한다.
  - LOCK 해제는 대표 결정이다(장부 P-13).
- 풀리면 이렇게 진행한다.
  - B-1.0 → B-1.1 에서 바꾸는 것은 항의·반영 요구 대응 하나다.
  - 모델은 사전 등록된 기준 gpt-4o-mini 로 고정한다. 승자를 고르지 않기 위해서다.
  - Golden·판정 기준은 그대로 두고, 사전 등록 뒤 실AI 로 실행한다.
  - 대표 블라인드는 M11·M17 계열 칸부터 본다.
- 그 전까지 A·B·Prompt·Guard·intent·문자열 차단 변경은 0이다.

# core-0.1 재생 결과 — Golden 34 · OpenAI 4개 모델 · 같은 모델의 B-1.0 과 짝 비교 (2026-09-25)

- 실행: GitHub Actions run 36111514762(commit 133ff56). 사전 등록 `FROZEN_INPUTS.json` core_gate 와 일치했다(「일치: 예」).
- 원자료: `evidence/CORE_REPLAY_20260925/core-result-excerpt.md`
  - 합계·판정 표는 로그 그대로다.
  - 턴별 표는 인용한 줄만 옮겼다. 전체는 Actions 로그에 있다.
- 기준: [REAL] 실제 OpenAI, 1회, 기계 판정.
  - 질문의 자연스러움·이어짐은 이 숫자로 판정하지 않는다. 대표 휴대폰 대화로 본다.
- 운영 변경은 0이다.
- 결과를 본 뒤 core·프롬프트를 고치지 않았다.

## 1. 숫자
| 항목 | 4o-mini B-1.0 → core | 4.1-mini B-1.0 → core | 4.1 B-1.0 → core | 4o B-1.0 → core |
|---|---|---|---|---|
| Golden 기계 PASS/FAIL(26칸) | 20/6 → 22/4 | 24/2 → 23/3 | 25/1 → 22/4 | 25/1 → 24/2 |
| 호출 수(34턴) | 39 → 34 | 36 → 36 | 34 → 34 | 34 → 35 |
| 재시도 | 5 → 0 | 2 → 0 | 0 → 0 | 0 → 0 |
| 항의·되묻기·지친 말을 저장 | 3 → 0 | 3 → 3 | 2 → **5** | 1 → 0 |
| 답·정정을 저장 안 함 | 7 → 9 | 3 → 2 | 2 → 2 | 3 → 4 |
| 「활동」이 든 질문(관측) | 5 → 0\* | 3 → 3 | **0 → 0** | 4 → 2 |
| 같은 흐름 안 글자까지 같은 질문 | 2 → 1 | 2 → 1 | 0 → 1 | 0 → **5** |
| 입력 토큰(API usage) | 37,546 → 34,390 | 35,027 → 35,295 | 33,221 → 34,924 | 32,659 → 35,209 |
| 턴 지연 p50 / p95(ms) | 1,476/3,173 → 1,372/2,450 | 1,367/2,479 → 1,373/2,807 | 1,270/1,720 → 1,120/2,163 | 1,078/1,465 → 1,288/2,545 |

- \* gpt-4o-mini 는 core 에서 34턴 중 24턴의 `question` 칸이 비었다. 그중 여러 턴은 질문을 반응 문장 안에 썼다.
  - 서버는 이 턴들을 「질문 없음」으로 셌다. 「활동」 0도 이 때문에 과소 집계다.
  - 반응 문장 안에 「활동」 질문이 적어도 3번 있다(FLOW2#1·#2, FLOW3#1).
- 「저장」은 B 와 뜻이 다르다. core 는 원문을 늘 남기고, 「저장」은 매칭 정보로 남긴 것이 있다는 뜻이다.
- 비용: 공식 단가를 확인하지 못해 계산하지 않았다.

## 2. 읽는 법 — 과장 금지
1. **구조를 바꿨다고 기계 숫자가 좋아지지 않았다.**
   - 4o-mini 에서만 PASS 가 늘었다(+2).
   - 4.1(−3), 4.1-mini(−1), 4o(−1)에서는 줄었다.
   - 한 턴 1번 호출, 재시도 0, 읽기 실패 0은 지켜졌다.
2. **모델마다 약점이 다르게 드러났다.**
   - gpt-4o-mini(현재 운영 추정 모델): core 의 출력 약속을 지키지 않는다(질문 칸 빔 24/34 · 여러 턴은 질문을 반응 안에 씀 · 답을 기억으로 못 남김 9). core 에 맞지 않는다.
   - gpt-4o: 같은 질문을 되풀이한다(5번).
     - FLOW1 에서 「어떤 행동이 진심을 보여주는 것 같으세요?」를 항의 뒤에 두 번 연속 물었다.
     - 「활동?갑자기?」 뒤에 다시 「활동」을 물었다.
   - gpt-4.1-mini: 「활동?갑자기?」를 「활동적인 편인가요」로 잘못 읽었다.
   - gpt-4.1: 「활동」 질문이 두 구조 모두 0이다.
     - AI 에게 한 질문에 먼저 답했다(FLOW6#2).
     - 「너가 어떤 취미가 있냐고 나한테 물어봐야」에 「어떤 취미를 즐겨요?」로 바로 따랐다.
     - 「반영해서 물어봐」에 들은 말(진실된 마음·돈)을 짚고 이어 물었다.
     - 그러나 항의 5번을 답으로 저장했다(「활동?갑자기?」·「딥하네」·「활동?질문이 머이래」·「할말이없다 휴」 등). 「네가」 같은 반말도 섞였다.
3. **항의를 답으로 저장하는 문제(GF-67)는 core 에서도 모델 분류에 달려 있다.**
   - 4o-mini·4o 는 0, 4.1 은 5였다.
4. 판정: **Conversation P0 = FAIL 유지** · VERIFIED 0 · 어느 모델도 확정하지 않는다.
   - 기계 판정은 이어짐·자연스러움을 재지 않는다. 대표 휴대폰 대화가 core-0.1 의 1차 판정이다.

## 3. 모델 후보(결정 아님 · 근거만)
- **휴대폰 시험 = Claude(빠른 등급)**
  - 운영 변경 없이 쓸 수 있는 유일한 길이다.
  - 세부 모델명은 페이지가 알 수 없고, 등급만 기록된다.
- **OpenAI 운영 후보를 고른다면 먼저 볼 것 = gpt-4.1**
  - 근거:
    - 두 구조·두 번의 실행에서 「활동」 점프 0
    - 질문에 먼저 답함
    - 방향 제안을 따름
    - 지연 p50 1.1초
  - 약점: 항의 저장 5, 반말 섞임
  - 다음은 gpt-4.1-mini다.
- gpt-4o-mini 는 core 의 출력 약속을 못 지켜 후보에서 뺀다(관측 1회).
- 운영 모델 변경은 대표 결정이고, 지금 하지 않는다.

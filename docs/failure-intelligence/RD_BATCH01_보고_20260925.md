# R&D 작업 묶음 01 보고 — ECHO Failure Intelligence OS (2026-09-25)

근거: 대표 「ECHO AI R&D AUTONOMOUS EXECUTION CHARTER · FINAL」. 운영 변경 0.

## 1. 결론
- **실패 기록 자산화: 목표 달성(문서·데이터·검사 기준).**
  - 실패 21건 → 53건. 실패한 해결책 13건 → 21건. 관계 23개. 고정 입력 26개 → 34개.
- **제품 Conversation P0: FAIL 유지.**
- **실제 AI A/B: 확인 불가** (`REAL_AI = BLOCKED_BY_ENVIRONMENT`).
- 새로 확인한 사실(실제 문장 재생, AI 호출 0)
  - 운영 v27 규칙이 대표의 실제 질문 「ai가 오타기 날수도 있어?」를 되묻기로 강제한다(GF-53).
  - B-1.0 의 의도 장부는 옛 흐름의 실패한 해결책(FS-17 의도 태그 소진)과 모양이 같다.

## 2. 실제 수행 작업
- 과거 기록 전수조사를 했다(문서·보고서·커밋 기록).
  - 새 실패 후보 25개를 찾아 원문과 줄 번호로 하나씩 대조했다. 인용 36개를 모두 확인했다.
- 실패 기록을 데이터로 옮겼다(정본 `docs/failure-intelligence/data/*.json`).
  - Library · Failed Solutions · Graph 의 `.md` 는 이 데이터에서 생성한다(`fi-build.mjs`).
  - 손으로 고치면 검사가 잡는다.
- 칸 추가: Failure Family · 사용자 피해(감정·정신·시간·물질, 근거 없으면 UNKNOWN) · 원인 확신 · 방어 수준 · Golden 연결 · git 커밋 근거.
- 증거 없는 승격을 막는 데이터 검사를 넣었다.
  - REAL_AI_VERIFIED·USER_VERIFIED·VERIFIED 는 실제 근거 파일이 없으면 실패한다.
- Failure Graph 를 만들었다(관계 7종, 관계마다 증거 수준).
- Failure Compiler 프로토타입(CANDIDATE)을 만들었다.
  - 사람이 말로 적은 실패를 검사 명세 후보로 바꾼다.
  - 지어낸 인용·분류 밖 값은 걸러 내 기록하고, 결과는 항상 사람 승인 필요.
  - 승인자 없이는 Golden 으로 못 옮긴다.
- Replay R4 를 추가했다(정상 사례 역검사, 규칙 층).
- 고정 입력에 FLOW6(ANSWER FIRST)·FLOW7(대표 실제 입력)을 추가했다.
  - 「할말이없다 휴」 출처를 정정했다(SYNTHETIC → ACTUAL).
  - 실제 AI 결과가 나오기 전의 변경이라 `FROZEN_INPUTS.json` 에 이유와 함께 이력을 남겼다(A·B 코드 변경 0).
- 문서를 갱신했다: Taxonomy(Type 25 · Family 15 · 방어 수준 · REAL_AI_SCRIPTED 출처) · Verified Defense Registry(새 이름) · Model Capability Registry · README(OS 12구성과 각 증거 수준).

## 3. 새 Failure (32건 · GF-22~53)
- 대표 실기기·운영 기록(ACTUAL): 사용자 질문 무시·말 따라 하기(09-16) · 반말·깨진 날씨 문장(09-17) · 짧은 답 뒤 막다른 길(09-17) · 「진실된마음」 뒤 생뚱맞은 질문(09-22) · 캐묻기·빈 구제 질문(09-21) · 주제 판정 형식 오류(09-22) · 처음부터 다시 불가(09-22) · 대화 끝 없음(09-22) · 고정 안전문장 3/4·항의 저장(09-24) · 빈 함수·모델 이름 오타(09-14) · 알 수 없는 배포(09-20) 등
- 실제 AI + 스크립트 입력(REAL_AI_SCRIPTED): **정정무시**(두 정정 → 같은 질문, 100회 중 5 → 1) · 사용자 질문을 질문으로 되받음(29/29 차단) · 반복 질문 88/맥락무시 589(자동 판정) · 지연 p95 10,029~10,569ms
- 코드: **미확정 사실화**(AI 가 리포트 확정 여부 결정) · 없는 기능 약속 · 가짜 진행 · **GF-53**(v27 규칙이 AI 에게 한 질문을 되묻기로 강제, 실제 문장 재생)
- **내 실수**
  - 앞선 보고에서 「정정무시·미확정 사실화는 날짜·원문 있는 실제 사례를 저장소에서 못 찾았다」고 했다. 이번 전수조사에서 찾았다(GF-25·GF-30). 앞선 말은 틀렸다.
  - 「할말이없다 휴」를 예문(SYNTHETIC)으로 잘못 표시했었다. 실제 대표 입력이다.

## 4. 새 회사 자산
- `docs/failure-intelligence/`
  - data 5종(failures·failed-solutions·failure-graph·families·types)
  - FAILURE_GRAPH.md
  - VERIFIED_DEFENSE_REGISTRY.md
  - 이 보고서
- `product/spike/failure-intelligence/`
  - `fi-lib.mjs`(읽기·검증·생성)
  - `fi-build.mjs`
  - `failure-compiler.mjs`
  - `fi.test.mjs`
- `product/spike/ab-20260925/`: Replay R4 · 고정 입력 v1.2 · FROZEN 이력

## 5. 해결된 것
- 제품 대화 품질 기준으로 해결된 것: **0건.** 운영 변경이 없고, 실제 AI 검증이 없다.
- 자산·도구 기준으로 해결된 것
  - 실패 데이터와 문서가 어긋나는 문제: 생성·검사로 막음.
  - 증거 없는 승격: 데이터 검사로 막음.
  - 실패한 해결책의 되풀이 추적: Graph 의 BROKEN_BY/REGRESSION_OF 로 3개 반복 모양을 드러냄.

## 6. 해결되지 않은 것
- Conversation P0 · UNRESOLVED 24건. 운영 v27 의 GF-01·02·03·05·06·53 이 여기에 포함된다.
- 옛 STEP 흐름 고정 문장(GF-35).
- 알 수 없는 배포 주체(GF-43).
- 타로 403(GF-48).
- 없는 기능 문구(GF-49).
- Outcome Feedback 미구현(실사용자 결과 없음).

## 7. Failed Solutions (새 8건 · FS-14~21)
- 애매하면 answer 기본값 → 되물음 저장(FS-14)
- v34 정정 수정 → 막다른 길 2→5(FS-15)
- 거절 문장 낱말 금지(FS-16)
- 의도 태그 12종 전체 비교 → 소진(FS-17)
- ack 에 anchor 글자 강제 → 따라 하기(FS-18)
- BURDEN 정규식(FS-19)
- 인용 글자 일치 → 띄어쓰기 탈락, 같은 모양이 v16 clue 로 재등장(FS-20)
- 탈출구가 시간 예산 밖(FS-21)
- 되풀이된 모양(Graph): 실패 → 대책(규칙·검사·기본값) → 반대 방향의 새 실패
  - GF-10 → FS-08 → GF-08
  - GF-18 → FS-14 → GF-11
  - GF-36 → FS-20 → GF-02(재등장)

## 8. 검증 수준
- [MOCK]·[REPLAY]만 있다.
- 검사(가짜 AI 기준 · AI 호출 0): **28/28 통과**
  - Failure Intelligence 8
  - 회귀 6
  - B-1.0 14
- 역검사(일부러 망가뜨리기)
  - Failure Intelligence 8/8 을 잡았다: 근거 없는 승격 · VERIFIED 조건 · 근거 파일 · 지어낸 인용 · 승인 없는 Golden · 생성 문서 손수정 · ID 중복 · 분류 밖 유형
  - 회귀 7/7(이전 묶음)
- `fi-build --check` 통과.
- 모든 스크립트 문법 검사 통과.
- 제품 src 변경 0 이라 제품 lint·type-check·build 는 다시 돌리지 않았다.

## 9. 실제 AI 결과
- **확인 불가 — REAL_AI = BLOCKED_BY_ENVIRONMENT.**
- `run-ab.mjs --require-real` 은 종료 코드 2 로 차단을 기록한다(가짜 결과로 대신 돌지 않음).

## 10. 사용자 검증
- 0건. 실사용자 대상 새 실험은 하지 않았다(HARD STOP 18).

## 11. 운영 변경
- **0.** DB·RLS·Auth·Secret·모델·Edge Function·Netlify·결제·가격·KEY·UI 모두 그대로다.
- 파일 삭제 0. 옛 `VERIFIED_ENGINES.md` 는 지우지 않고 새 파일을 가리키게 두었다.

## 12. 위험
- 운영 v27 은 알려진 실패를 그대로 가진 채 운영 중이다(GF-53 포함).
- B-1.0 의 의도 장부는 미탐(다른 이름)과 소진(FS-17 과 같은 모양) 위험을 함께 가진다. 실AI 전에는 판정할 수 없다.
- 자동 판정기 수치(맥락무시 589)는 사람 검수가 아니다.

## 13. 다음 행동 하나
- 실제 AI 실행 환경이 확보되면, 고정된 하네스를 그대로 돌리고 블라인드 검수표를 대표에게 한 번에 전달한다.
  - 명령: `run-ab.mjs --require-real … --blind … --key …`
